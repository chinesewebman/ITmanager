# 网络运维监控平台 - AI辅助分析模块

> **v3 修订（2026-09-09）**：本模块**重定位**——原「自建 LLM 问答」设计**已废弃**（实测 0 行代码、0 端点、0 页面），改为 **ITmanager 作为 HolmesGPT 的数据源**。依据 `docs/v3-架构优化需求.md` R1。原设计保留在附录作历史。

## 九、AI 辅助分析（v3：对接 HolmesGPT）

### 9.0 结论先行

| | 原设计 v1（已废弃） | v3 方案 |
|---|---|---|
| 形态 | 自建 AI 服务层，拼 prompt 调 LLM | 对接 HolmesGPT（agentic RCA） |
| 能力 | 单轮问答 | 多轮工具调用 + 证据链 |
| 自建成本 | prompt / token / 缓存管理全自建 | 只提供只读 API |
| 数据源 | 仅 ITmanager 库内 | Zabbix + Splunk + Wazuh + NetBox + ITmanager |

**理由**：HolmesGPT 是 CNCF Sandbox 项目，50+ 内置 toolset、agentic 循环，能力代差明显；自建是重复造轮子且更弱。

### 9.1 ITmanager 在 AI 架构中的位置

```
HolmesGPT（根因侦探）
   │ 只读工具调用
   ├─► Zabbix    指标 / 告警
   ├─► Splunk    日志
   ├─► Wazuh     端点
   ├─► NetBox    资产 / 专线
   └─► ITmanager 业务上下文   ← 本模块的价值所在
        · 诊断时间线（告警 + 工单 + 网络变更 聚合）
        · MTTR / MTTD / SLA KPI
        · Runbook 推荐
        · 资产 360 视图（含告警 / 工单关联）
```

### 9.2 toolset 最小端点集（必须包含）

| 端点 | 提供什么 | 为什么别处拿不到 |
|---|---|---|
| `GET /api/v1/diagnostics/assets/{id}/timeline` | 跨源时间线 | 需要 ITmanager 的聚合 SQL |
| `GET /api/v1/runbooks/recommend` | 按资产类型 + 严重性推荐 SOP | 业务知识，只在 ITmanager |
| `GET /api/v1/assets/{id}` | 资产 360 视图 | NetBox 无工单 / 告警关联 |
| `GET /api/v1/alerts` | 告警列表（只读） | 交叉验证 |
| `GET /api/v1/kpis` | MTTR / MTTD / SLA | 业务口径 |

> 端点名以实际路由为准，落地前核对 `backend/internal/api/routes.go`。

### 9.3 安全边界（硬约束）

1. **只读 token**：为 HolmesGPT 单独签发 API Key，最小 scope，**不复用 admin 账号**
2. **写操作不暴露**：ack / 建单 / 删除等端点不进 toolset
3. **数据外流**：用云端 LLM 时告警与资产信息会发出 → 建议本地 Ollama 或内网 DeepSeek
4. **审计**：所有 toolset 调用走 `audit_logs`

### 9.4 配置示例（HolmesGPT 侧）

```yaml
toolsets:
  itmanager:
    type: http
    enabled: true
    description: "ITmanager 运维操作台（业务上下文）"
    config:
      endpoints:
        - hosts: ["https://itmanager.yourdomain.local"]
          paths: ["/api/v1/"]
          methods: ["GET"]
          auth:
            type: bearer
            token: "{{ env.ITMANAGER_TOKEN }}"
    llm_instructions: |
      使用 itmanager_request 查询运维业务上下文。
      优先查诊断时间线 /diagnostics/assets/{id}/timeline 理解故障全貌。
      本工具集**只读**，不要尝试任何写操作。
```

---

## 九·附录：原设计（v1，已废弃，保留作历史）

> 以下为 v1 自建 LLM 方案，**v3 不再实施**。保留原因：可追溯设计演进；若未来 HolmesGPT 不可用，可作备选参考。

### 9.1 AI 模块概述

#### 9.1.1 功能定位

| 功能 | 说明 | 实现方式 |
|------|------|----------|
| **智能问答** | 自然语言查询系统信息 | LLM API |
| **故障分析** | 分析告警，提供根因建议 | LLM API |
| **日志分析** | 智能解析日志内容 | LLM API |
| **报表解读** | 自动生成数据分析报告 | LLM API |

#### 9.1.2 LLM 选型

考虑到企业使用，建议支持多种 LLM 提供商：

| 提供商 | 模型 | 说明 |
|--------|------|------|
| **OpenAI** | GPT-4, GPT-3.5 | 通用能力强 |
| **Azure OpenAI** | GPT-4, GPT-3.5 | 企业版，数据安全 |
| **Claude** | Claude 3 | 长文本处理 |
| **本地模型** | Llama, ChatGLM | 数据完全本地 |

### 9.2 系统集成

#### 9.2.1 集成架构

```
┌─────────────────────────────────────────────────────────────┐
│                    AI 模块架构                                │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│   ┌─────────────────────────────────────────────────────┐   │
│   │                   自建业务平台                       │   │
│   │                   (API服务)                         │   │
│   └──────────────────────────┬────────────────────────────┘    │
│                              │                                 │
│                              ▼                                 │
│   ┌─────────────────────────────────────────────────────┐   │
│   │                   AI 服务层                          │   │
│   │  ┌────────────┐  ┌────────────┐  ┌────────────┐   │   │
│   │  │  Prompt   │  │  Token   │  │  缓存     │   │   │
│   │  │  管理     │  │  管理     │  │  管理     │   │   │
│   │  └────────────┘  └────────────┘  └────────────┘   │   │
│   └──────────────────────────┬────────────────────────────┘    │
│                              │                                 │
│                              ▼                                 │
│   ┌─────────────────────────────────────────────────────┐   │
│   │                   LLM 提供商                         │   │
│   │   OpenAI  │  Azure  │  Claude  │  本地模型       │   │
│   └─────────────────────────────────────────────────────┘   │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 9.3 提示词设计

#### 9.3.1 故障分析提示词

```
你是一个资深运维工程师，擅长分析系统故障。请根据以下信息分析故障原因：

告警信息：
- 告警名称：{alert_name}
- 告警级别：{severity}
- 设备信息：{device_info}
- 告警详情：{alert_message}
- 发生时间：{trigger_time}

历史相似告警：
{history_alerts}

请分析：
1. 可能的故障原因
2. 建议的排查步骤
3. 需要的操作
```

### 9.4 API 设计

```python
# AI 分析 API

@app.post("/api/v1/ai/analyze")
async def analyze_alert(alert_id: str):
    # 1. 获取告警信息
    alert = get_alert_from_zabbix(alert_id)

    # 2. 构建提示词
    prompt = build_prompt("fault_analysis", alert)

    # 3. 调用 LLM
    response = await llm.chat(prompt)

    # 4. 返回分析结果
    return {
        "alert_id": alert_id,
        "analysis": response,
        "suggestions": parse_suggestions(response)
    }
```

---

*本文档为拆分后的"AI辅助分析"模块，原章节：十*
*优化：支持多 LLM 提供商，适配企业需求*
