# 网络运维监控平台 - 用户权限管理

> **v3 修订块** · 状态 **draft** · 日期 2026-09-09 · 依据 `docs/v3-架构优化需求.md` · 分支 `docs/v3-optimize`
>
> 本章 v3 定位：**ITmanager 的权限只管操作台自己**。操作台登录与授权由 ITmanager 自身 RBAC（`users` / `roles` / `permissions` / `user_roles`）承担；
> NetBox / Zabbix / GLPI 的 LDAP/AD 配置**属于各系统自己的事，ITmanager 不代管**，ITmanager 只做单点跳转或独立账号（见 §6.0）。
> **GLPI 未部署**（工单 SoT = ITmanager 自身，降级为可选只读参考），§6.3.3 保留作未来参考。

## 六、用户权限管理详细设计

### 6.0 两层权限分工（v3）

> 一句话：**操作台自己的登录授权，ITmanager 管；被集成系统的账号，各系统自己管。**

#### 6.0.1 分工表

| 层 | 谁负责 | 状态 | 说明 |
|------|--------|------|------|
| ITmanager 操作台登录 + 授权 | **ITmanager 自身 RBAC** | ✅ 已实现 | JWT + API Key 双模式；表见 §6.0.2 |
| NetBox 账号 / LDAP 组 | **NetBox 自己** | 外部系统 | ITmanager 不代管；§6.3.1 仅供 NetBox 管理员参考 |
| Zabbix 账号 / LDAP | **Zabbix 自己** | 外部系统 | ITmanager 不代管；§6.3.2 同上 |
| GLPI 账号 / LDAP | **GLPI 自己** | ⛔ **未部署** | 保留作未来参考（§6.3.3） |
| 单点登录跳转（SSO） | — | ⚠️ **未实现** | §6.1.1 的 CAS / OAuth2 / SAML 属可选设计，代码中未见实现（待确认） |

#### 6.0.2 ITmanager 自身 RBAC（已实现）

| 表 | 作用 | 代码状态 |
|------|------|----------|
| `users` | 账号、状态、登录失败锁定、首次登录强改密 | ✅ `backend/internal/models/user.go:13`；`POST /api/auth/login`；`backend/internal/api/handlers/user_handler.go` |
| `roles` | 角色定义（`code`：admin / ops_admin / ops_user / auditor / readonly；`user` 是 000013 兜底值，**不在本表**） | ✅ 表结构 + `backend/cmd/admin-bootstrap/main.go` 关联；**运行时授权判定实际走 `users.role` 单列**（权威词表见 §6.2.2） |
| `permissions` | 资源 + 动作 + scope | 表结构已落地；**运行时未见读取代码（待确认）** |
| `user_roles` | 用户-角色关联（含 `expires_at`） | ✅ 表结构 + `admin-bootstrap` 写入；运行时判定未走此表（待确认） |
| `api_keys` | 服务账号 / 工具接入（`permissions` 最小 scope、`ip_whitelist`、`rate_limit`、`expires_at`） | ✅ `backend/internal/models/user.go:48`、`backend/internal/api/handlers/api_key_handler.go` |

**认证方式**：ITmanager 自身用 **JWT + API Key 双模式**（`README.md:81`、`12-优化建议.md:107`）。

> ✅ **缺陷 D-6 + R-2（已修复，2026-09-09）**：鉴权已改为**能力矩阵**（`backend/internal/middleware/roles.go`，单点判定）。`RequireRole("admin")` 的角色字面量比较已全部替换为 `RequireCapability(...)`，词表漂移的 5 处已收敛到权威口径（见 §6.2.2）。决策记录见 `docs/adr/0005-角色词表与权限矩阵.md`，完整挂载清单见 `docs/FIX-PLAN-AUTHZ.md` §3.3。
>
> **覆盖**：非 GET 路由**全部**分类（挂能力或列入白名单），由 `TestRoutes_非GET路由都已分类` 用 `r.Routes()` 反向兜底 —— 新增路由忘挂门禁会让测试失败。
>
> **仍需注意**：① `GET /api/notification-channels` 响应体含渠道明文凭据，已收紧到 `manage`（**不做** config 脱敏，属独立任务）；② 存量 `user` 角色在新矩阵下只能读（fail-safe 预期方向，用 `cmd/set-role` 提权）；③ JWT 内嵌 role 且不回查 DB，角色变更最长 24h 后才在旧令牌生效；④ API Key 的 scope 仍只按 HTTP 方法判定，无法表达「GET 但属 manage」。
**LDAP/AD 集成未实现**：原计划 P4.9「实现 LDAP 认证」已改为 JWT（`tasks.md:92`、`开发计划.md:21`）——§6.1 的 LDAP/AD 描述应理解为**未实现的备选方案**。

> ✅ **缺陷 D-4（已修复，2026-09-09）**：`users.role` / `users.deleted_at` 由迁移 `000013_schema_align.up.sql` 补齐，`role` 从 `user_roles`/`roles` 回填（存量 admin 不会降级）。`TestDBSmoke_LoginQuery` 用迁移建的库实测登录查询通过。
>
> 历史记录（修复前）：`users` 表在 `schema.sql:30` 与 `backend/migrations/000001_init.up.sql:11-42` 中均无 `role`/`deleted_at`，而模型依赖它们 → 用迁移新建的库登录必失败。
>
> 反向漂移同样存在：`users.mfa_enabled` / `mfa_secret` 建表有列、模型无字段、无 Go 代码。

#### 6.0.3 与 AI 层的边界

HolmesGPT 等 AI 层通过**只读 API Key** 访问 ITmanager，**不得复用 admin 账号**；写操作端点（ack / 建单）不进 toolset。约束详见 `10-安全合规.md` §10.5。

### 6.1 认证方案：LDAP/AD 集成

> **v3 标注**：ITmanager 自身认证为 **JWT + API Key**（已实现）；本节的 LDAP/AD 集成为**未实现的备选**（P4.9 改为 JWT，见 §6.0.2）。

#### 6.1.1 认证架构

| 组件 | 说明 |
|------|------|
| **LDAP/AD** | 企业目录服务 (OpenLDAP / Windows AD) |
| **LDAP认证模块** | 系统内置 LDAP 认证 |
| **SSO (可选)** | CAS / OAuth2 / SAML |

#### 6.1.2 支持的目录服务

| 服务 | 支持 | 说明 |
|------|------|------|
| **Windows AD** | ✅ 原生 | 企业常用 |
| **OpenLDAP** | ✅ 原生 | 开源方案 |
| **389 Directory** | ✅ 配置 | Fedora目录服务 |
| **Apple Open Directory** | ✅ 配置 | macOS服务器 |

### 6.2 权限模型

#### 6.2.1 RBAC + LDAP 组映射

```
┌─────────────────────────────────────────────────────────────┐
│                      权限检查流程                             │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│   用户登录 ──► LDAP验证 ──► 获取组 ──► 映射角色            │
│                         │                  │                  │
│                         ▼                  ▼                  │
│                    验证密码         分配权限                  │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

#### 6.2.2 默认角色

**权威词表（2026-09-09 起，单点在 `backend/internal/middleware/roles.go`）**：

| 角色 | 能力 | LDAP组（未实现，仅规划） |
|------|------|--------------------------|
| `admin` | 全部（read / write / manage / audit / identity） | IT-Admin |
| `ops_admin` | read / write / manage / audit | IT-Ops-Admin |
| `ops_user` | read / write | IT-Ops |
| `auditor` | read / audit | IT-Audit |
| `readonly` | read | IT-Readonly |
| `user` | read（迁移 000013 兜底值，等同 readonly） | — |

> **遗留别名（deprecated，只读入不写出）**：`operator` → `ops_user`，`viewer` → `readonly`。保留是为了存量 dev 库与未过期 JWT（24h）不静默降权，**v4 清理**。
>
> **能力语义**：`read` 是**地板** —— 「未被更高能力覆盖的端点默认放行」，**不是**「所有 GET」（`GET /api/users` 属 `identity`、`GET /api/audit-logs` 属 `audit`、`GET /api/notification-channels` 属 `manage`）。`manage` 与 `write` 的分界 = **不可逆或涉及凭据**。空/未知角色 fail-safe 到只读。完整矩阵与路由清单见 `docs/adr/0005-角色词表与权限矩阵.md`。
>
> **分配路径**：`cmd/set-role`（env 驱动，直连 DB，含「拒绝降级最后一个 admin」防自锁）；`cmd/seed` 的 `operator` 用户名保留但角色已改写为 `ops_user`。
>
> **v3 标注**：LDAP 组映射（`IT-Admin` 等）**未实现**（待确认）；上表「API用户」不是角色，而是 `api_keys` 机制（见 §6.0.2）。

### 6.3 各系统认证配置

> **v3 说明**：本节各系统的 LDAP 配置由**各系统管理员自行执行**，ITmanager 不代管、不代配；ITmanager 侧只需按 §6.0.1 做跳转或独立账号。

#### 6.3.1 NetBox LDAP 配置

```python
# netbox/netbox/configuration.py

AUTHENTICATION_BACKENDS = [
    'netbox.authentication.LDAPBackend',
    'django.contrib.auth.backends.ModelBackend',
]

# LDAP 服务器配置
LDAP_SERVER_URI = "ldap://ldap.company.com"
LDAP_BIND_DN = "cn=admin,dc=company,dc=com"
LDAP_BIND_PASSWORD = "your-password"

# 用户搜索
LDAP_USER_SEARCH = LDAPSearchUnion(
    LDAPSearch(
        "ou=users,dc=company,dc=com",
        ldap.SCOPE_SUBTREE,
        "(uid=%(user)s)"
    ),
    LDAPSearch(
        "ou=admins,dc=company,dc=com",
        ldap.SCOPE_SUBTREE,
        "(sAMAccountName=%(user)s)"
    ),
)

# 组映射
LDAP_GROUP_SEARCH = LDAPSearch(
    "ou=groups,dc=company,dc=com",
    ldap.SCOPE_SUBTREE,
    "(objectClass=groupOfNames)"
)

# NetBox 组同步
LDAP_GROUP_PARAMS = {
    "netbox_admin": {
        "name": "NetBox Admin",
        "is_superuser": True,
    },
    "netbox_operator": {
        "name": "NetBox Operator",
        "is_staff": True,
    },
}
```

#### 6.3.2 Zabbix LDAP 配置

```
# Zabbix Web 界面配置
# Administration > Authentication > LDAP

设置:
- LDAP host: ldap://ldap.company.com
- Port: 389
- Base DN: dc=company,dc=com
- Search attribute: uid
- Bind DN: cn=admin,dc=company,dc=com
- Bind password: ********

用户映射:
- Zabbix user: uid={username},ou=users,dc=company,dc=com
```

#### 6.3.3 GLPI LDAP 配置

> ⛔ **GLPI 未部署**，本节保留作未来参考（工单 SoT = ITmanager 自身，GLPI 降级为可选只读参考）。

```
# GLPI 安装配置
# 设置 > 认证 > LDAP目录 > 添加

服务器配置:
- 名称: Company LDAP
- 主机: ldap://ldap.company.com
- Base DN: dc=company,dc=com
- Root DN: cn=admin,dc=company,dc=com
- 连接密码: ********
- 筛选条件: (objectClass=person)
- 用户属性: uid

同步选项:
- 实时同步: 是
- 动态组: 是
```

### 6.4 多因素认证 (MFA)

#### 6.4.1 支持的MFA方式

| 方式 | 系统支持 | 说明 |
|------|----------|------|
| **TOTP** | NetBox / Zabbix | Google Authenticator |
| **短信** | GLPI | 需要短信网关 |
| **邮件** | 各系统 | 验证码 |
| **硬件Key** | NetBox | YubiKey 等 |

> **v3 标注**：上表均为**各系统自身**的 MFA 能力；ITmanager 自身 MFA **未实现**（`users.mfa_enabled` / `mfa_secret` 建表有列，但无 Go 代码，模型亦无对应字段）。

### 6.5 会话管理

> **v3 标注**：Token 有效期 24h 与实现一致（`backend/internal/config/config.go:153` 默认 `auth.jwt.expire=86400`）。
> 「刷新Token 7天」「会话超时 30分钟」在代码中未见实现（`user_sessions` 表已归入设计未实现清单 `docs/schema-planned.sql:24`）；「登录IP限制」目前仅在 `api_keys.ip_whitelist` 上实现（`backend/internal/middleware/auth.go:147`），用户登录侧未实现（待确认）。

#### 6.5.1 会话配置

| 参数 | 默认值 | 说明 |
|------|--------|------|
| Token有效期 | 24小时 | 可配置 |
| 刷新Token | 7天 | 长时间访问 |
| 会话超时 | 30分钟 | 无操作超时 |
| 密码策略 | 复杂度要求 | 8位+大小写+数字 |

#### 6.5.2 安全控制

- 登录失败锁定: 5次失败后锁定15分钟
- 密码过期: 90天 (可配置)
- 登录IP限制: 支持白名单

---

*本文档为拆分后的"用户权限管理"模块，原章节：十一*
*优化：集成 LDAP/AD 统一认证，整合 NetBox/Zabbix/GLPI*
