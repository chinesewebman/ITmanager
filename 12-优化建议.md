# 网络运维监控平台 - 优化建议

> 最近更新: 2026-09-09 — 建议清单按 v3 R1–R6 重排
> v3 依据: [docs/v3-架构优化需求.md](docs/v3-架构优化需求.md)（基线 `git describe` = v2.1.2-12-gdad2b6f）
> 详细开发计划见 [开发计划.md](开发计划.md)，测试现状见 [TESTING.md](TESTING.md)

## 一、v3 建议清单（按 R1–R6 重排）

依据需求文档 §3 六个改造点重排。状态口径：**已完成 / 待办 / v3 不做**（「v3 不做」= 需求文档 §5 明确不做，设计归档保留可追溯）。

| 改造点 | 建议条目 | 状态 | 备注 |
|--------|----------|------|------|
| **R5** 文档与仓库卫生 | 版本号三处对齐 `git describe` | 已完成 | 2026-09-09 已对齐 v2.1.2-12-gdad2b6f |
| R5 | `schema.sql` 拆 live / `docs/schema-planned.sql` | 已完成 | 16 张已实现表留 live，39 张设计表移出 |
| R5 | ADR-0002 的 gRPC 部分作废（补 ADR-0003） | 已完成 | P-6：进程内调用本无 RTT；AlertService gRPC 保留但冻结 |
| R5 | `.gitignore` 误伤源码修复（`server` → `/server`） | 已完成 | P-7：原先静默忽略 `backend/cmd/server/` |
| **R2** 专线归 NetBox | 专线台账（P2P + MPLS VPN）以 NetBox Circuit 建模，ITmanager 只读渲染 | 待办 | 几十条专线零纳管；**不做 AI 也立刻见效** |
| R2 | 自建 `lines` 系列四表 CRUD | v3 不做 | 与 NetBox Circuits 重复；定义归档 |
| **R1** AI 重定位 | 自建 LLM 问答 / 知识库（`ai_*` / `kb_documents`） | v3 不做 | 归档；改接 HolmesGPT |
| R1 | HolmesGPT toolset 最小端点集 | 待办 | 必须含 timeline + runbook/recommend |
| **R3** vCenter 纳管 | vCenter → NetBox sync（不直连），ITmanager 从 NetBox 读 VM | 待办 | 1000+ VM 入账；先 dry-run |
| **R4** 告警划界 | 人工抑制规则 + 值班升级（ITmanager 侧） | 已完成 | 已实现主线 |
| R4 | Keep 为告警唯一写入方；ack 改调 Keep API | 待办 | v3 只划界**不部署** Keep |
| R4 | 自建跨源去重 / 关联 | v3 不做 | 归 Keep |
| **R6** 工单 SoT 归位 | `05-运维工单.md` 重写 + `docs/adr/0004-*.md` | 已完成 | ITmanager `tickets` 为唯一真值；GLPI 降级为可选只读参考 |
| R6 | ITIL 审批流 / SLA 引擎 / 满意度 | v3 不做 | 对应表 0 行 Go 代码；单团队运维不需要 |
| R6 | **D-1** `tickets` 三套 schema 不一致 | **待办（阻断级）** | 迁移 24 列 / 模型 24 列，交集仅 10 列；`POST /tickets` 必然失败 |
| R6 | **D-2** 工单号碰撞（`Count()%26`） | 待办 | 同日第 27 张与第 1 张同号；须在 D-1 之后修 |
| R6 | **D-3** `alerts.ticket_id` 悬空 | 待办 | 无写入方；应指向 `tickets.id` |
| R6 | **D-4** `users` 表缺 `role`/`deleted_at`（模型有、迁移无） | **待办（阻断级）** | 用迁移新建的库连登录都过不去 |
| R6 | **D-5** `audit_logs` 列名漂移 | 待办 | 写入失败仅 `slog.Warn` → 静默丢审计 |
| R6 | **D-6** `RequireRole` 未挂载 | 待办 | 路由级 RBAC 形同虚设 |
| R6 | **D-7** API Key `permissions` 未校验 | 待办 | 鉴权分支只按用户 role 放行 |
| R6 | `09-AI辅助.md` toolset 端点 `/api/v1` → `/api` | 已完成 | 路由组无 `/v1`；KPI 实为 `/api/dashboard/kpis` |
| 既有（非 R1–R6） | 采集性能 10万+指标/秒 | 已完成 | Zabbix 集成 |
| 既有 | 查询性能 P95 < 500ms | 部分 | 连接池 + 索引已做，Redis 缓存待评估 |
| 既有 | 告警延迟 < 5s | 已完成 | |
| 既有 | Dashboard 单条 SQL / 熔断器 / User List 分页 | 已完成 | 2026-06-16 |
| 既有 | 系统可用性 99.9%（多实例部署） | 待确认 | v3 未纳入 R1–R6 |
| 既有 | 数据可靠性 RPO < 5分钟（备份） | 待办 | 见 §16.3 |
| 既有 | 告警可达性 99.99% | 已完成 | 钉钉 + 邮件双通道 |
| 既有 | 安全防护（DDoS/SQLi/XSS/CSRF/暴力破解） | 部分 / 已完成 | 见 §十五 |
| 既有 | 限流 / 认证 | 已完成 | |
| 既有 | 监控覆盖（基础设施/应用/DB/中间件） | 部分 / 待办 | 见 §16.1 |
| 既有 | 应急响应流程 | 待办 | 属 P7 交付物 |
| 既有 | 备份策略（全量/增量/实时） | 待办 | 见 §16.3 |

---

## 二、v2 时期建议（历史保留）

> 以下 §十四 / §十五 / §十六 为 2026-06 时期建议**原文**，保留可追溯；与 v3 的对应关系见 §一。被 v3 取代的条目已就地标注「归档」。

## 十四、优化建议汇总

### 14.1 性能优化

| 优化项 | 目标 | 方案 | 状态 |
|--------|------|------|------|
| **采集性能** | 支持10万+指标/秒 | Zabbix 原生支持 | ✅ 已完成 (Zabbix 集成 Phase 2) |
| **查询性能** | P95 < 500ms | Redis 缓存 | 🟡 部分：连接池 + 索引，Redis 缓存待评估 |
| **告警延迟** | < 5秒 | 实时推送 | ✅ 已完成 (Zabbix 集成 + 告警管理) |

**2026-06-16 增量**：
- ✅ **Dashboard 单条 SQL 聚合** (Bug #27/29)：5 次 count → 1 条查询
- ✅ **熔断器** (httpx)：half-open race 修复，ctx 取消不再误触熔断
- ✅ **User List 分页** (Bug #26)：避免全表扫描

### 14.2 可用性优化

| 优化项 | 目标 | 方案 | 状态 |
|--------|------|------|------|
| **系统可用性** | 99.9% | 多实例部署 | 🟡 待部署配置 (P6.4-6.7)；v3 未纳入 R1–R6，**待确认** |
| **数据可靠性** | RPO < 5分钟 | 定时备份 | 🔴 待跟进 |
| **告警可达性** | 99.99% | 多通道通知 | ✅ 已完成 (钉钉 + 邮件双通道) |

---

## 十五、安全与抗攻击设计

### 15.1 安全防护

| 防护类型 | 措施 | 状态 |
|----------|------|------|
| **DDoS防护** | 限流、IP黑白名单 (Nginx) | 🟡 应用层限流已做，Nginx 层待部署 |
| **SQL注入** | 参数化查询 (ORM) | ✅ GORM 全程参数化 |
| **XSS防护** | 输入过滤、输出编码 | 🟡 React 默认转义，待加 CSP 头 |
| **CSRF防护** | Token验证 | 🟡 JWT + SameSite cookie，待加强 |
| **暴力破解** | 登录失败锁定 | ✅ **2026-06-16 修复**：FailedLogin atomic CAS (Bug #1) |

**2026-06-16 安全修复**：
- ✅ **登录失败锁定 race 修复** (Bug #1)：atomic CAS 在 sync.Mutex 内
- ✅ **弱密码拒绝** (handler 层)
- ✅ **改回旧密码拒绝** (handler 层)
- ✅ **rate_limit 越界修复** (handler 层)
- ✅ **IP 越界修复** (handler 层)
- ✅ **uuid 静默生成修复** (handler 层)
- ✅ **type=garbage 拒绝** (handler 层)
- ✅ **Name UNIQUE 索引** (Bug #8)：生产 + test schema 同步

### 15.2 抗攻击设计

```
┌─────────────────────────────────────────────────────────────┐
│                    安全防护架构                               │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│   用户请求 ──► WAF ──► 限流 ──► 认证 ──► 业务处理        │
│                  │        │        │                         │
│                  ▼        ▼        ▼                         │
│              阻断攻击   限制频率   验证身份                   │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

**2026-06-16 落实**：
- ✅ **限流** (`internal/middleware/`)：28 tests 覆盖 CORS / Auth / Metrics / Recovery / NoRoute / RequestID / E2E
- ✅ **认证** (JWT + API Key 双模式)：熔断器 race 修复，登录锁定 atomic CAS
- 🟡 WAF / DDoS 防护：待 Nginx 部署配置

---

## 十六、运维保障体系设计

### 16.1 监控覆盖

| 监控对象 | 监控内容 | 工具 | 状态 |
|----------|----------|------|------|
| **基础设施** | CPU、内存、磁盘、网络 | Zabbix | 🟡 Zabbix 集成完成，待部署 Zabbix server |
| **应用服务** | 响应时间、错误率 | Zabbix + Prometheus | 🟡 Prometheus 指标已暴露 (metrics middleware) |
| **数据库** | 连接数、慢查询 | Zabbix | 🔴 待跟进 |
| **中间件** | 队列长度、缓存命中率 | Zabbix | 🔴 待跟进 |

**2026-06-16 指标**：
- ✅ **Prometheus metrics** (`/metrics` 端点)：httpx 熔断器状态、handler 延迟、错误率
- ✅ **structured logging** (zap + logrus)
- 🟡 Grafana dashboard：待搭建

### 16.2 应急响应

| 级别 | 定义 | 响应时间 | 响应人员 |
|------|------|----------|----------|
| P0 | 系统不可用 | 5分钟 | 值班+主管 |
| P1 | 核心功能受损 | 15分钟 | 值班+工程师 |
| P2 | 非核心功能异常 | 1小时 | 工程师 |
| P3 | 轻微问题 | 4小时 | 工程师 |

**2026-06-16 状态**：应急响应流程待编写（属 P7 交付物）

### 16.3 备份策略

| 备份类型 | 频率 | 保留时间 | 方式 | 状态 |
|----------|------|----------|------|------|
| **全量备份** | 每周 | 30天 | pg_dump | 🔴 待跟进 |
| **增量备份** | 每日 | 7天 | Borg | 🔴 待跟进 |
| **实时同步** | 持续 | - | 数据库主从 | 🔴 待跟进 |

**2026-06-16 状态**：备份策略待实施（属 P6 部署 + P7 验收）

---

## 2026-06-16 测试覆盖增量

| 包 | 覆盖率 | 增量 |
|----|--------|------|
| internal/api | 85.5% | +50 handler tests + 21 routes tests + 28 middleware tests |
| internal/apierr | 88.5% | 19 tests |
| internal/apikey | 100% | 17 tests |
| internal/config | 94.3% | 30 tests |
| internal/httpx | 87.8% | 全覆盖 |
| internal/metrics | 90.2% | 全覆盖 |
| internal/migrate | 72.9% | 7 tests |
| internal/middleware | 36.5% | 待提升 (跟 api 共享测试) |
| internal/models | 36.1% | 13 hook tests |
| internal/service | 43.4% | 28 tests |
| internal/integration | 39.5% | 13 E2E tests |
| cmd/admin-bootstrap | 66.1% | 17 tests (新) |
| cmd/migrate | 54.3% | 10 tests (新) |
| cmd/seed | 73.7% | 12 tests (新) |
| pkg/logger | 58.9% | - |
| **总覆盖率** | **61.2%** | **+30%** (vs 2026-02-15) |

**Frontend**：
- pages/ : 14 tests (Login / Dashboard / Assets / Racks / Alerts / Tickets)
- hooks/ : 8 tests (queryKeys factory)
- stores/ : 31 tests (8 zustand stores)
- **总 frontend** : **53 tests pass**

---

*本文档为拆分后的"优化建议"模块，原章节：十八、十九、二十*
*优化：基于开源组件的优化建议*
