# 网络运维监控平台 - 系统架构设计

## 七、系统架构设计

> **v3 修订（2026-09-09）**：本章架构更新为**三层分工**（见 §7.1）。原「自建全栈平台」架构标注为 v1 历史（§7.1.1）。依据 `docs/v3-架构优化需求.md`。

### 7.1 总体架构（v3：三层分工）

```
┌────────────────────────────────────────────────────────────────┐
│  ① 人的操作台 —— ITmanager（本项目）                            │
│  React18 + TS + AntD5                                          │
│  拓扑 · 诊断时间线 · 告警确认 · Runbook · 复盘 PDF · 值班 · KPI │
└──────────────────────────▲─────────────────────────────────────┘
                           │ 只读消费
        ┌──────────────────┴───────────────────┐
        │                                      │
┌───────┴───────────────┐        ┌─────────────┴──────────────┐
│ ② 资产真值 —— NetBox  │        │ ③ AI 分析层                │
│ DCIM / IPAM /         │        │ Keep（告警枢纽）           │
│ Circuits（专线）      │        │ HolmesGPT（根因侦探）      │
└───────────────────────┘        └────────────────────────────┘
        ▲                                      ▲
        │ 同步                                 │ 只读工具调用
   vCenter → NetBox                      Zabbix / Splunk / Wazuh
   (bb-Ricardo/netbox-sync)

统一约束：AI 层对本项目一律只读；写操作（确认告警、建单）必须由人在 ITmanager 里点。
```

**分工原则**

| 层 | 拥有什么 | 不做什么 |
|---|---|---|
| ① ITmanager | 人的**工作流**：看、确认、排班、复盘 | 不存资产真值、不做 AI 推理 |
| ② NetBox | 资产**真值**：设备 / 机柜 / IP / 专线 / VM | 不做工作流、不做告警 |
| ③ Keep + HolmesGPT | 告警**收敛**与**根因** | 不做资产台账、不自动执行写操作 |

#### 7.1.1 原架构（v1，已废弃，保留作历史）

```
┌─────────────────────────────────────────────────────────────────────┐
│                         前端展示层 (Web UI)                         │
│              React + TypeScript + Ant Design                        │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                          API网关层                                   │
│                     Nginx + Go RESTful API                         │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         服务层 (自建业务)                            │
│     ┌────────────┐ ┌────────────┐ ┌────────────┐               │
│     │  资产服务   │ │  告警服务   │ │  报告服务   │               │
│     └────────────┘ └────────────┘ └────────────┘               │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         集成层 (开源组件)                           │
│   ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐  │
│   │   NetBox   │ │   Zabbix   │ │    GLPI    │ │   Graylog  │  │
│   │  (资产管理) │ │  (监控告警) │ │  (工单)    │ │  (日志)    │  │
│   └────────────┘ └────────────┘ └────────────┘ └────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         数据存储层                                   │
│   ┌────────────┐  ┌────────────┐  ┌────────────┐               │
│   │ PostgreSQL │  │   Redis     │  │   Local    │               │
│   │ (主数据库)  │  │ (缓存/队列)  │  │  (文件)    │               │
│   └────────────┘  └────────────┘  └────────────┘               │
└─────────────────────────────────────────────────────────────────────┘
```

### 7.2 组件选型汇总（v3）

| 层级 | 组件 | 选型 | 说明 |
|------|------|------|------|
| **资产管理（SoT）** | NetBox | ⭐ 推荐 | DCIM / IPAM / 机柜图 |
| **专线台账** | NetBox Circuits | ⭐ v3 新增 | P2P 原生；MPLS 每腿一条 Circuit 终止在 Provider Network |
| **VM 纳管** | vCenter → NetBox | ⭐ v3 新增 | 社区版 `bb-Ricardo/netbox-sync`（官方集成是商业版） |
| **监控告警** | Zabbix | ⭐ 推荐 | SNMP / Agent 监控，唯一采集源 |
| **AI 分析层** | Keep + HolmesGPT | ⭐ v3 新增 | Keep = 告警枢纽；HolmesGPT = 根因（替代自建 AI 模块） |
| **运维工单** | GLPI | ⭐ 推荐 | ITIL 工单系统 |
| **日志管理** | Graylog | 可选 | 日志集中管理 |
| **备份** | BorgBackup | 可选 | 增量备份 |

> v3 说明：ITmanager **不自建**资产真值、采集器、LLM 推理；只做人的操作台。详见 `docs/v3-架构优化需求.md`。

### 7.3 技术选型

#### 7.3.1 后端技术栈

| 组件 | 技术选型 | 理由 |
|------|----------|------|
| **语言** | Golang | 高性能、并发好 |
| **框架** | Gin | 高性能HTTP框架 |
| **ORM** | GORM | Go生态成熟 |
| **API网关** | Nginx | 负载均衡、反向代理 |

#### 7.3.2 前端技术栈

| 组件 | 技术选型 |
|------|----------|
| **框架** | React 18 + TypeScript |
| **UI库** | Ant Design 5 |
| **图表** | ECharts |
| **状态管理** | Zustand |
| **构建工具** | Vite |

### 7.4 数据流设计

```
┌─────────────────────────────────────────────────────────────────────┐
│                         数据流                                        │
├─────────────────────────────────────────────────────────────────────┤
│                                                              │
│  [网络设备/服务器] ──SNMP/Agent──► [Zabbix] ──┐             │
│                                                     │             │
│  [VMware] ─────────────API──────────► [Zabbix] ──┼──► [告警]  │
│                                                     │     │       │
│  [发现任务] ────────Nmap──────────► [NetBox] ────┘     │       │
│                                                             │       │
│  [告警] ──────────────────────────► [GLPI] ─────────────┘       │
│                                                             │       │
│  [系统日志] ──────Filebeat────────► [Graylog] ─────────────────┘  │
│                                                              │
└─────────────────────────────────────────────────────────────────────┘
```

### 7.5 模块设计

#### 7.5.1 资产服务 (Asset Service)

```
职责：资产管理全生命周期

核心API:
- POST   /api/v1/assets              创建资产
- GET    /api/v1/assets              查询资产列表
- GET    /api/v1/assets/:id          查询资产详情
- PUT    /api/v1/assets/:id          更新资产
- DELETE /api/v1/assets/:id          删除资产
- GET    /api/v1/racks               机柜列表
- GET    /api/v1/racks/:id/devices   机柜设备

数据来源:
- NetBox API (主数据源)
- Zabbix (监控状态)
- 手动录入
```

#### 7.5.2 告警服务 (Alert Service)

```
职责：告警数据聚合、展示

核心API:
- GET    /api/v1/alerts              告警列表
- GET    /api/v1/alerts/:id          告警详情
- PUT    /api/v1/alerts/:id/ack      确认告警
- PUT    /api/v1/alerts/:id/resolve  解决告警
- GET    /api/v1/alerts/stats        告警统计

数据来源:
- Zabbix API
```

#### 7.5.3 工单服务 (Ticket Service)

```
职责：工单数据聚合、展示

核心API:
- GET    /api/v1/tickets             工单列表
- GET    /api/v1/tickets/:id        工单详情
- POST   /api/v1/tickets             创建工单

数据来源:
- GLPI API
```

### 7.6 系统规模适配

针对 **3机房、几十机柜、1000+VM、几十台物理机** 规模的建议：

| 组件 | 配置建议 |
|------|----------|
| **NetBox** | 1台 4C8G |
| **Zabbix** | 1台 8C16G (含PostgreSQL) |
| **GLPI** | 1台 4C8G |
| **Graylog** | 可选，1台 4C8G |
| **自建API** | 1-2台 2C4G |

---

*本文档为拆分后的"系统架构设计"模块，原章节：三*
*优化：整合 NetBox + Zabbix + GLPI 选型*
