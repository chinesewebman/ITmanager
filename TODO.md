# 网络运维监控平台 - 开发待办

## 当前状态 (2026-09-09, `git describe` = v2.1.2-12-gdad2b6f)

> **真实基线**：`v2.1.2-12-gdad2b6f`（最新 tag **v2.1.2**，最后提交 **2026-07-01**）。v2.2 / v2.3 系列（B/C 系列）已在 v2.1.2 之后合入，尚未打 tag。
> **下一步：v3 架构优化**（R5 → R2 → R1 → R3 → R4），依据 [docs/v3-架构优化需求.md](docs/v3-架构优化需求.md)。
>
> 历史状态 (2026-06-17, v1.0.2)：v1.0.0 → v1.0.1 → v1.0.2 三连发布（10 PR + 一键部署 + README badges）。详见 [CHANGELOG.md](CHANGELOG.md)。
> 优化方向见 [docs/优化路线图.md](docs/优化路线图.md)。

### v2.1.2 之后已合入（B/C 系列，未打 tag）

- [x] **B1-4 TRAPS.md** (`e7c1a0e`) — 集中 27 个项目 trap
- [x] **B4 资产软退役 + IP 释放** (`223c11e` 后端 + `20723a3` 前端退役/恢复 UI)
- [x] **C6 docker-compose healthcheck** (`77bfdd9`) — 7 服务 healthcheck + `depends_on: service_healthy`
- [x] **C7 首次登录强改密** (`c848ff5` + `dad2b6f`) — 改密页 + 首次跳引导
- [x] 其余（Zabbix / NetBox / GLPI runtime config UI、Zabbix metric fallback worker、Go 1.25 fmt 重排）见 [CHANGELOG.md](CHANGELOG.md) 「未发布」节

### v3 改造待办（2026-09-09 新增，依据 [docs/v3-架构优化需求.md](docs/v3-架构优化需求.md)）

优先级顺序：**R5 → R2 → R1 → R3 → R4**

- [x] **R5 文档与仓库卫生**（2026-09-09 完成）— 版本号对齐 `git describe`；`schema.sql` 拆分（16 张已实现表，39 张设计表移入 `docs/schema-planned.sql`）；ADR-0002 的 gRPC 部分作废（ADR-0003）；`.gitignore:10` `server` → `/server` 修复源码误伤
- [ ] **R2 专线归属 NetBox Circuits**（3-4h）— 废弃自建 `lines` 四表，专线以 NetBox `Circuit` + `CircuitTermination` 建模，ITmanager 只读渲染
- [ ] **R1 AI 模块重定位**（4-6h）— 砍自建 LLM 问答/知识库，改为 ITmanager 作为 HolmesGPT 数据源（toolset 端点，只读 token）
- [ ] **R3 vCenter 纳管**（6-8h）— vCenter → NetBox sync，ITmanager 从 NetBox 读 VM（先 dry-run 一周再开自动清理）
- [ ] **R4 告警与 Keep 划界**（2h 文档 + 后续实施）— 跨源去重/关联归 Keep；ITmanager 保留人工抑制（维护窗口）+ 值班升级
- [x] **R6 工单 SoT 归位**（2026-09-09 完成）— `05-运维工单.md` 重写：ITmanager `tickets` 为唯一真值，GLPI 降级为可选只读参考；新增 `docs/adr/0004-工单SoT决策.md`；明确不做 ITIL 审批 / SLA / 满意度

### 待修复缺陷（2026-09-09 实测，见 [docs/v3-架构优化需求.md](docs/v3-架构优化需求.md) §9）

- [x] **D-1（阻断级）`tickets` 三套 schema 不一致** — 修复轮 `2ec518c`：补齐式迁移 `000013_schema_align`（非破坏、幂等、带类型守卫）+ `db_smoke` CI job（全新安装 + 存量升级两条路径）
- [x] **D-2 工单号碰撞** — `2ec518c`：按当日前缀计数 + 进位字母标签，唯一索引兜底
- [x] **D-3 `alerts.ticket_id` 悬空** — `000013` 补列后本轮补上**写入方**：`POST /api/alerts/{id}/ticket` 一键建单（`TicketService.CreateFromAlert`），认领与插票同事务，插票失败认领一并回滚 → 不再产生悬空指针。前端入口同日落地：告警表格/移动卡片操作列的「建单」按钮（两界面共用 `getAlertActions`，`ticket_id` 非空翻成 disabled 的「已建单」）。**残留**：只覆盖「人点建单」这一条写入路径，自动建单仍未做（ADR-0004 明确不做）；`ticket_id` 指向的工单被外部直接删库仍会悬空，该情形按防御性 404 处理（`TestTicketService_CreateFromAlert_关联悬空返回ErrNotFound`）
- [x] **D-4（阻断级）`users` 表缺 `role` / `deleted_at` 列** — `2ec518c` + `000013`：先加列后回填再设 DEFAULT（避免存量 admin 降级）
- [x] **D-5 `audit_logs` 列名漂移** — `2ec518c`：`000013` RENAME 表/列对齐模型
- [x] **D-6 `RequireRole` 未挂载** — `2ec518c` 挂载 17 条 admin 专属路由；**2026-09-09 进一步升级为能力矩阵**（见下方 AUTHZ 条目）
- [x] **D-7 API Key `permissions` 未校验** — `2ec518c`：校验自身 scope，关联用户 inactive 立即失效

### AUTHZ 角色词表归一 + 权限矩阵（2026-09-09 完成，对应 FIX-PLAN-D1-D7 §7 R-2）

- [x] **权威词表单点化** — `backend/internal/middleware/roles.go`：`admin/ops_admin/ops_user/auditor/readonly/user`，遗留别名 `operator`→`ops_user`、`viewer`→`readonly`（只读入不写出，v4 清理）
- [x] **能力矩阵鉴权** — `read/write/manage/audit/identity` 五档；`RequireRole("admin")` 全部替换为 `RequireCapability(...)`；`read` 是地板（未被更高能力覆盖的端点默认放行）
- [x] **路由清单双向 diff** — `TestRoutes_非GET路由都已分类` 用 `r.Routes()` 反向兜底，新增非 GET 路由忘挂门禁即失败
- [x] **`/auth/me` 下发 `capabilities`** — 前端不再复制矩阵
- [x] **`cmd/set-role`** — 打通 `ops_admin`/`ops_user`/`auditor` 的生产分配路径，含「拒绝降级最后一个 admin」防自锁
- [x] **ADR-0005 + 06 章同步** — `docs/adr/0005-角色词表与权限矩阵.md`、`06-用户权限.md` §6.0.2/§6.2.2
- [x] **前端 6 页 token 失效** — `AlertSuppressions`/`Oncall`/`Runbook`/`Topology`/`MetricSnapshot`/`AssetTimeline` 用 `localStorage.getItem('token') ?? ''` 拼 Bearer，而该键自 C-F5 改 httpOnly cookie 后无人写入 → 恒 401 + 静默回退 mock。**已修复（2026-09-09）**：收敛为 `services/api.ts` 的共享 `apiGet`/`apiSend`（4 份副本已漂移过一次）+ 修 `/api/v1` 前缀 + 拦截器不再把 204/blob 当失败 + Runbook 写操作不再谎报成功 + ESLint `no-restricted-syntax` 回归守卫。见 `docs/FIX-PLAN-FRONTEND-TOKEN.md`

**AUTHZ 审计发现的既有缺陷（本轮记录，未修）**：

- [x] **`POST /api/auth/skip-password-change` 可被空 body 绕过** — 服务端只拒绝 `reason == "first_login"`；body 为空时 `req.Reason` 为空即清除强改密标记 → seed 的 `admin/admin123` 可保留弱口令。**已修复（2026-09-09）**：判据改为 DB 的 `must_change_password`（flag=true 一律 400，不看客户端自报 reason）；前端 `Login.tsx` 跳转参数 `first-login`→`first_login`（原先与 `ChangePassword.tsx` 的判定不一致，跳过按钮在强改密时照样显示）；该路由移入 `protected` 组以复用 AuditLog；不再写假的 `password_set_at`。见 `docs/FIX-PLAN-AUTHZ-LEFTOVER.md`
- [x] **write scope 的 API Key 挂在 admin 账号上可签发新 Key** — `apiKeyAllows` 只按 HTTP 方法判定，`identity` 路由的能力来自关联用户角色 → 可铸造远期 Key 作持久化后门。**已修复（2026-09-09）**：新增 `middleware.RejectAPIKeyAuth`（任何 API Key 一律 403，比原计划「要求 Key 权限含 admin」更严——admin scope 的 Key 同样能自我复制），整组挂在 `/auth/api-keys`，并覆盖同类路径 `PUT /auth/password`。见 `docs/FIX-PLAN-AUTHZ-LEFTOVER.md`
- [ ] **`must_change_password=true` 未在服务端全局强制**（AUTHZ-LEFTOVER G-1）— 登录后拿到的 JWT 仍可调任意端点，「强改密」目前是前端 UX + skip 端点拦截 + **`CreateAPIKey` 定点拒绝**（2026-09-09 审计 F-1 收窄，见 `docs/FIX-PLAN-AUTHZ-LEFTOVER.md` §8.1）。彻底方案需在 `AuthMiddleware` 中查库判定，属性能/架构决策
- [x] **API Key 路径不检查 `LockedUntil`**（AUTHZ-LEFTOVER G-2）— **结案：不修**（2026-09-09 决策，见 `docs/FIX-PLAN-AUTHZ-CLOSURE.md` §2 D-A）。理由：① 因果错位，`LockedUntil` 是「密码被猜」的信号，与「Key 是否被盗」无因果关系；② 耦合会**阻断受害者自救**（锁定判定在密码校验之前，而吊销 Key / 改密必须走登录会话）且任何人可用错密触发 30 分钟停摆；③ 正确补充是「看得见」——已新增登录尝试入审计（D-E）。账号级止血开关是 `status=inactive`（两条路径都立即失效）。连带新增 G-4/G-5
- [x] **前端密钥管理打不到后端**（AUTHZ-LEFTOVER G-3）— **已修复（2026-09-09）**：`api.ts` 四条路径 `/api-keys`→`/auth/api-keys`；`Settings.tsx` 读 `res.data.data.key`→`api_key`（原先一次性明文 Key 永不显示）；过期时间提示 RFC3339→`YYYY-MM-DD`（后端只收 `2006-01-02`）；403 改为区块内 Alert「当前账号无密钥管理权限」而非静默空白。见 `docs/FIX-PLAN-AUTHZ-CLOSURE.md`
- [x] **API Key 可读写通知渠道凭据 / 改写集成出站地址**（F-3 + F-5）— **已修复（2026-09-09）**：`/notification-channels` 整组 + 三条 `PUT /integrations/*` 挂 `RejectAPIKeyAuth`；`/test` 与 `/sync` 保留（堵住 PUT 后只能打管理员配置过的地址）。见 `docs/FIX-PLAN-AUTHZ-CLOSURE.md` §1 S-3/S-4
- [x] **`/auth/me` 不在 `openapi.yaml`** — `capabilities` 进不了 `gen:api` 生成类型，前端接入前需先补 spec。**已结案（2026-09-12，M31 步 3）**：`/auth/me` + `CurrentUser` schema 已补，`capabilities`/`role` 的 enum 与门禁实际用值同源（`middleware/roles.go`）；权限矩阵见 `docs/FIX-PLAN-OPENAPI-CONTRACT.md` §2.3 步 3
- [ ] **`cmd/set-role` 并发窗口** — 防自锁检查与写入已同事务，但两个并发进程仍可能各自通过（TOCTOU）；该命令是人工运维操作，暂不修
- [ ] **`GET /api/integrations/status` 回传集成 URL 与 Zabbix 用户名** — 不含 token，属读地板；若需收紧另立任务
- [ ] **G-4 账号处置无产品化入口**（2026-09-09 新增，AUTHZ-CLOSURE §2 D-A）— `users.status` / `locked_until` 只能改库；`/users` 仅 `GET`，`cmd/` 只有 `admin-bootstrap`/`migrate`/`seed`/`server`/`set-role`。建议下一轮做最小集：`cmd/disable-user` + `PUT /users/:id/status`，并一并覆盖 G-5
- [ ] **G-5 已签发 JWT 不查库**（2026-09-09 新增）— `status=inactive` 对 API Key 立即生效（`middleware/auth.go:187`），但对**已签发会话 JWT 最长 24h 才生效**（`middleware/auth.go:112-124` 只用 claims；`auth.jwt.expire=86400`）。「禁用账号」对会话失窃不是即时止血。属独立架构决策（每请求查库 vs 短 TTL + 刷新）
- [x] **G-7 gin 受信代理未配置 → 限流可绕过、审计 IP 可伪造**（2026-09-09 新增 → 当日结案，安全审计 F1①）— 原状：未调 `SetTrustedProxies`，gin 默认信任全部来源，`ClientIP()` 取 XFF **最左值**（攻击者可控）→ ① 登录限流每请求换 XFF 即无限额度；② 审计 IP 可伪造；③ **API Key IP 白名单可绕过**（安全审查补出的第 4 个消费者，`middleware/auth.go:148-161`）。**已交付**（`docs/FIX-PLAN-TRUSTED-PROXY.md`）：① `server.trusted_proxies []string`，默认空 = 不信任任何来源（直连部署的正确值），`NMP_SERVER_TRUSTED_PROXIES` 可覆盖；② `Config.Validate()` 启动期校验——非法条目与**过宽前缀**（v4 < /8、v6 < /16，`0.0.0.0/1`+`128.0.0.0/1` 可覆盖全网，只拒 `/0` 不够）一律 fail-fast；③ `SetupRouter` 调 `SetTrustedProxies`，出错 fail-closed 降级为不信任并 `slog.Error`；空配置启动 WARN + 运行期首次见 XFF 再 WARN 一次（`middleware/trusted_proxy_warn.go`）；④ compose 固定子网 `172.28.0.0/24` + web 静态 IP `172.28.0.10`，api 只信任该 IP（不信任整段——同网段还有 pg/redis/netbox）；⑤ 文档 `08-部署运维.md` §8.2.3。**验证**：V-1 XFF 被忽略（6 次同对端不同 XFF → 第 6 次 429）、V-2/V-5 审计 IP = XFF 最右不可信跳、V-8 白名单按真实客户端判定；变异反证 2 组（删 `SetTrustedProxies` → V-1/V-2/V-8 红；校验空转 → 非法/过宽用例红）；全量 `go test ./...` 26 包绿。**审计后修订（两名独立审计员，处置表见方案 §8）**：① 校验补三类绕过——IPv4-mapped 等效前缀（`::ffff:0:0/96` 曾绕过 /16 门槛，实测 gin 侧信任全部 IPv4）、主机位非零拒绝（`172.28.0.10/24` 被静默归一为整段）、条目 `TrimSpace` 写回 cfg（原先「校验所见 ≠ gin 所见」，会让 fail-closed 清空整张表）；② 运行期告警改为按「直连对端是否在受信表内」判定并**恒挂载**（原只在空配置时挂 → 「配了但配错」零告警，比不配更隐蔽），启动期非空时 Info 打印生效表；③ `Load()` 加 `viper.SetDefault("server.trusted_proxies", []string{})`——旧 `config.yaml` 无该键时 `NMP_SERVER_TRUSTED_PROXIES` 原会被静默忽略（升级场景）；④ 新增 fail-closed 判别用例（半截信任表 + XFF 换桶，变异反证红）与「只告警一次」的日志条数断言；⑤ `.env.example` 补该项。**审计否决 1 条**：`::fffe:0:0/95` 不拦——基址已非 v4-mapped，gin 的 `IPNet.Contains` 对 IPv4 客户端匹配不上（实测），拦了是误拒。**被否的替代**：release 模式空配置 fail-fast——直连部署下空配置是正确且安全的，fail-fast 会误杀，需新增 `server.behind_proxy` 开关才能区分（理由见方案 R-1）。**残余**：compose 实际跑通仍依赖 G-9/G-10
- [x] **G-8 `RejectAPIKeyAuth` 未挂载时静默失效**（2026-09-09 新增，安全审计 F6；**2026-09-11 M28/B 已交付**）— **【事实更正 2026-09-11】原描述写的判定键 `auth_type` 在中间件里根本不存在**：真实判据是 `c.GetString("api_key_id") != ""`（`auth_type` 只是 SQL 列名，见 `migrations/000001_init.up.sql:22`，代码从不读它）。原状：该中间件靠 `api_key_id` 非空判「当前是 API Key 身份」，若某路由忘了先挂 `AuthMiddleware`，该键恒空 → 走 `c.Next()` 放行，守卫**看起来在、实际不设防**。当前所有用法都在 `protected` 组下（已挂 `AuthMiddleware`），属潜在陷阱而非现存漏洞。待办：改为「无身份即 500」，或让路由注册阶段校验中间件顺序。**已交付（2026-09-11，M28/B，`docs/FIX-PLAN-HARDENING.md` §8.2）**：判据改用「身份是否已建立」而非「某键是否非空」——`AuthMiddleware` 的两条认证路径（JWT `auth.go:120` / API Key `auth.go:194`）都必然 `c.Set("user_id", …)`，它为空即说明上游没跑，此时无法区分 JWT 会话与匿名 → 一律 `apierr.Internal` 500「服务器配置错误」+ `Abort`，日志带 `path`/`method` 便于定位漏挂的路由。选 500 不选 403：这是服务器配置错误、要进错误率告警（403 会被当成正常拒绝而静默）。**未做启动期静态自检**：gin 的 `Routes()` 只返回 `RouteInfo`、拿不到中间件链，注册期断言顺序**不可能**；这道运行时判据本身就是顺序检查，且更强（能发现「组被重建」这类动态情况）。生产影响已核实：全部 6 处用法（`routes.go:251/259/277/279/281/372`）都在 `protected` 组内，无路由命中新的 500 分支。测试：既有 `TestRejectAPIKeyAuth` 的「会话身份放行」子例同批补 `user_id`（否则被新判据打成 500），新增「无身份 fail-closed」用例 + `TestRejectAPIKeyAuth_5xx日志带路径上下文`；变异 M28-M3/M28-M4 全红在断言上（rev1 拟的两条变异因删块后 `fmt` 未使用而红在**编译**上，违反 T-31，已改写为可编译形态）。
- [x] **G-11 API Key 白名单接受 CIDR 但按字符串精确比对 → CIDR 条目永不命中**（2026-09-09 新增，G-7 审计连带发现，非本次引入）— 创建/更新 API Key 时 `handlers/api_key_handler.go:82-90` 只做 `net.ParseIP` 或 CIDR 格式校验（接受 `10.0.0.0/8`），而鉴权侧 `middleware/auth.go:148-153` 用 `entry == clientIP` 精确比较——填 CIDR 的 Key 永远匹配不上，表现为「白名单莫名 403」。IPv6 文本形式差异（`::1` vs `0:0:0:0:0:0:0:1`）同理。待办：① 要么两侧统一为「CIDR/裸 IP 都按网段匹配」（`net.ParseCIDR` + `Contains`，裸 IP 补 /32、/128）；② 要么创建时就拒绝 CIDR 并写清「只接受单个 IP」。**倾向 ①**（与 `server.trusted_proxies` 的语义一致），但需同时决定 IPv6 规范化。关联 G-7（同一个 `ClientIP()` 消费者）。**已交付（2026-09-11，M28/A，`docs/FIX-PLAN-HARDENING.md` §8.1）**：取方案 ①。鉴权侧删掉 6 行字符串精确比较，改走 `ipAllowedByWhitelist(list, clientIP)` → 复用同包 `trusted_proxy_warn.go` 的 `parseTrustedNets`（裸 IP 补 `/32`、`/128`；CIDR 走 `ParseCIDR`；无效条目跳过）+ `isTrustedPeer`（`SplitHostPort` → `ParseIP` → `n.Contains`）。IPv6 文本形式差异一并解决：`net.IPNet.Contains` 内部把 4-in-6 归一化（GOROOT `net/ip.go:480-484`），`::1` 与 `0:0:0:0:0:0:0:1` 同判。**语义差异已显式处理**：空名单 = 不做 IP 检查（放行），与 `trusted_proxies` 空表「谁都不信」**相反**，wrapper 注释里写明，避免后人误当同一语义复用。`validateIPWhitelist` 语义不变（仍接受 CIDR），只补注释说明鉴权侧按网段匹配。测试：`TestIPAllowedByWhitelist` 19 组表驱动（空/nil 名单、裸 IP 命中与落空、CIDR 命中与网段外、`::ffff:10.20.31.7` ⊂ `10.20.0.0/16`、`10.20.31.7` ⊂ `::ffff:10.20.0.0/112`、`::1` 双向、IPv6 CIDR、多条目 OR、垃圾条目、空条目、混合、非法客户端 IP、空白条目）+ 4 个中间件用例 + 路由级 CIDR 命中用例；变异 M28-M1/M28-M2 全红在断言上。**行为突变（必须告知）**：此前永不命中的 CIDR 条目**开始生效**，存量 Key 需复核 —— 见 `CHANGELOG.md`。
- [x] **G-6 Web 界面 TLS 最低版本无强制、无验证**（2026-09-09 新增 → 当日结案，PCI DSS 4.2.1）— 原状：仓库内没有任何 TLS 终止或最低版本约束（`frontend/nginx.conf` 只有 `listen 80`，Go 后端 `cmd/server/main.go:104` 明文 `ListenAndServe()`），「只支持 TLS 1.2+」完全依赖外部反代、无法自证。**已交付**：① `08-部署运维.md` §8.2.2「TLS 终止与最低版本」——终止点（nginx/`nmp-web`）、最低版本、套件/HSTS/票据策略、80→443、后端明文不得直接暴露、自证命令；② `scripts/check-tls.sh`——用 `openssl s_client` **主动发起** TLS 1.0/1.1 握手断言被拒（curl 无法降级尝试），退出码 0/1/2，OpenSSL 3.x 缺 legacy provider 时按「无法判定」退出而非假通过；③ `frontend/nginx-tls.conf.example`——443 + `ssl_protocols TLSv1.2 TLSv1.3` + HSTS + 80→443 模板（opt-in，需自备证书）。**已验证**：真实 nginx:alpine 套模板 → 脚本退出 0；故意放开 `ssl_protocols`+`SECLEVEL=0` 的 nginx → 脚本退出 1 并逐条点名 TLS 1.0/1.1。**残余（属部署方动作，非代码）**：生产必须套模板（或外部反代等效配置）并跑一次 `check-tls.sh`，退出码须为 0
- [x] **G-12 `routes.go` 重复挂载 Logger/Recovery + `healthCheck` 死函数**（2026-09-09 新增，G-7 审计连带发现，非本次引入）— ① `gin.Default()` 已包含 `gin.Logger()` + `gin.Recovery()`，`routes.go:139-140` 又 `r.Use` 了一次，实测每请求访问日志打两遍 **→ ① 已由 G-16 轮修复（2026-09-09）：`gin.New()` + 显式挂载，且 Logger/Recovery 各只挂一次**；② `routes.go:451` 的 `healthCheck` 无任何调用点、0% 覆盖。待办：确认 `healthCheck` 无外部引用后删除。**已交付（2026-09-11，M28/D，`docs/FIX-PLAN-HARDENING.md` §8.4）**：确认全仓无引用后删除该函数（删除时位于 `routes.go:464-469`，硬编码返回 `"database": "connected"` 却**不做任何 ping**，是会撒谎的死代码）。真实探针是 `livenessHandler` / `readinessHandler`（未动）。
- [x] **G-10 compose 的 env 变量名与 config 体系不符 + 反代 upstream 名与服务名不一致**（2026-09-09 新增，G-7 连带发现）— `docker-compose.yml:46-50` 给 api 传的是 `DATABASE_URL` / `REDIS_URL` / `NETBOX_URL` / `ZABBIX_URL` / `GLPI_URL`，而 `config.Load()` 只认 `NMP_` 前缀 + mapstructure 路径（`NMP_DATABASE_PASSWORD`、`NMP_INTEGRATIONS_NETBOX_URL`…），**这五个变量一个都不会生效**；且缺 `NMP_AUTH_JWT_SECRET` / `NMP_DATABASE_PASSWORD` / `NMP_API_KEY_PEPPER`，release 模式下 `Validate()` 必然拒启。另：`frontend/nginx.conf:14` 的 `proxy_pass http://backend:8080/` 指向服务名 `backend`，compose 里该服务叫 `api`（`container_name: nmp-api`）——DNS 解析不到。待办：① 逐项改成 `NMP_` 变量（含 `NMP_SERVER_TRUSTED_PROXIES`，G-7 已加；`backend/.env.example` 同样缺 `NMP_AUTH_API_KEY_PEPPER`——照抄它会因 `Validate()` 拒启）；② 统一 upstream 名（改 nginx.conf 为 `api` 或给 api 加网络别名 `backend`）；③ 与 G-9 的 Dockerfile 一起做一次 `docker compose up` 真跑通并记录 **已交付（2026-09-09）**：① compose 传给 api 的变量全部改为 `NMP_*`（逐项与 mapstructure 键核对：`NMP_DATABASE_{HOST,PORT,USER,PASSWORD,NAME,SSLMODE}` / `NMP_REDIS_HOST` / `NMP_INTEGRATIONS_{NETBOX,ZABBIX,GLPI}_URL` / `NMP_AUTH_JWT_SECRET` / `NMP_AUTH_API_KEY_PEPPER`）；② 三个必需 secret 用 `${VAR:?}` 强制，缺值 compose 直接报错退出（不再有 `nmp123` 硬编码进仓库）；③ upstream 名用**网络别名** `backend` 对齐（实测 compose 网络里 `container_name` 不注册 DNS，只有服务名/别名可以；不改 `nginx.conf`，它同时是非 compose 部署模板）；④ 端口收敛：postgres 只绑环回 `127.0.0.1:5432`（宿主机 `make deploy` 的 db-migrate/db-seed 要连它）、redis 不发布、api 只绑 `127.0.0.1:8080`、web 也只绑 `127.0.0.1:3000`（它提供明文 HTTP，对外必须前置 TLS 终结——安全审计 P1；顺带解决宿主机已占用 6379 导致的 `port is already allocated`）；⑤ aux 服务加 `profiles: ["aux"]`；⑥ `backend/.env.example` 补 `NMP_AUTH_API_KEY_PEPPER`（照抄旧文件会因 Validate 拒启），根目录新增 `.env.example`；⑦ 文档 §8.3 重写——旧示例（image 拉取 + 全量发布端口 + 非 NMP_ 变量名）与仓库文件漂移，已废弃。
- [x] **G-13 `auth.api_key_pepper` 在 shipped config.yaml 里没有键 → env 被静默忽略 + 报错文案写错变量名**（2026-09-09 新增 → 当日结案，commit `954c79f`）— 实测：`NMP_AUTH_JWT_SECRET`/`NMP_AUTH_API_KEY_PEPPER`/`NMP_DATABASE_PASSWORD` 三个 env 齐备，`go run ./cmd/migrate status` 仍报「auth.api_key_pepper 不能为空（通过 **NMP_API_KEY_PEPPER** 注入）」——两个独立缺陷：① viper 的 `Unmarshal` 只遍历 `AllKeys`（yaml 键 + `SetDefault` 键），纯 env 键不进 AllKeys，shipped yaml 缺 `auth.api_key_pepper` 键 → 该 secret 无论怎么注入都读不进来（fail-closed，但文档化部署路径是死的；生产挂载自定义/旧 config.yaml 同样中招）；② 报错文案写的 `NMP_API_KEY_PEPPER` 不是 viper 认的名字（正确是 `NMP_AUTH_API_KEY_PEPPER`），运维照提示改仍然起不来。**已交付**：yaml 补 `api_key_pepper: ""` 占位 + `viper.SetDefault("auth.api_key_pepper", "")`（覆盖「挂载的 yaml 无该键」场景）+ 报错文案改正 + 三条单测（shipped yaml + env 能启动、旧 yaml 无键时 env 仍生效、报错文案变量名）。变异反证：删 `SetDefault` → 红；文案改回错名 → 红。已逐字段核对：**唯一缺口就是 pepper**。
- [ ] **G-14 迁移与运行时解耦（多副本部署前置）**（2026-09-09 新增，compose 轮审查发现）— `database.Init` **无条件**执行 `migrate.Up`（`internal/database/database.go:61-66` + `cmd/server/main.go:34` 注入 `MigrationsFS`），没有开关；迁移锁是非阻塞 `pg_try_advisory_lock`（`migrate/migrate.go:70-78`），**多副本同时冷启动时抢不到锁的副本会启动失败并反复重启**。本轮只把「api 单副本」写进文档，未改代码（改动需新配置键 `database.automigrate`，而新键又要防 G-13 的 viper AllKeys 坑）。待办：① yaml + `SetDefault` 落 `database.automigrate` 占位；② `database.Init` 读该开关；③ compose 加独立 one-shot `migrate` 服务 + `depends_on: service_completed_successfully`（注意 `cmd/migrate` 走全量 `config.Load`→`Validate`，容器必须注入全部必需 secret，否则 gate 永不满足）；④ 多副本部署文档 + `--scale api=N` 验证。
- [ ] **G-15 release 校验与「集成可选」耦合 → compose 默认模式只能留在 debug**（2026-09-09 新增，compose 轮审查发现）— `Config.Validate()` 在 release 下**无条件**要求 `integrations.netbox.token` 与 `glpi.*_token`（`config.go:222-237`，zabbix 已是「URL 非空才校验」的模式），于是 `docker compose` 默认`NMP_SERVER_MODE=debug` 才不会拒启；而 debug 的代价是：弱集成凭据不被启动期拒绝、登录 cookie 的 `Secure` 不开（`auth_handler.go:134-135`）。待办：① netbox/glpi 改成与 zabbix 一致的「URL 配置了才校验」；② 校验解耦后把 compose 默认翻成 `release`；③ 文档同步。
- [x] **G-16 GORM logger 硬编码 `LogLevel: logger.Info` → release 也全量打印每条 SQL**（2026-09-09 新增，compose 轮审查发现）— `internal/database/database.go:38-46` 固定 Info 级别（含参数展开），容器 stdout 会落全量 SQL，生产上是敏感信息与噪声双输。待办：把 GORM 级别接到 `cfg.Log.Level`（warn/release 下静默或只报慢查询），并加单测钉住「release → 不打印 SQL」。**2026-09-09 安全审计补充实证（P4）**：会落日志的敏感语句包括 `middleware/auth.go:134` 的 `WHERE key_hash = '<...>'` 与 `auth_handler.go:280` 改密后的 `UPDATE users SET password_hash='$2a$10$...'`；`/tmp/smoke-compose2.log:184` 可见完整 bcrypt 值被打进容器日志 → `docker compose logs api` 或转发到 Graylog/Splunk 后，任何只读日志账号都能拿到哈希离线爆破。**已交付（2026-09-09，`docs/FIX-PLAN-LOG-HYGIENE.md`，三视角审查处置见其 §7）**：① gorm 级别接 `cfg.Log.Level`（`mapGormLogLevel`：`debug`→Info、`info`/`warn`/空串/非法值→Warn、`error`→Error；包级默认**显式** `Warn` —— gorm 的零值比 Silent 还小，`Trace` 首行就 return，会一条都不打），5 个 main 各加一行 `database.SetGormLogLevel(cfg.Log.Level)`；② `ParameterizedQueries: true` 恒定 + `RecorderParamsFilter` 覆盖 —— **`DB.Scan` 走 `traceRecorder` 的包级钩子，只设 `ParameterizedQueries` 挡不住**（`finisher_api.go:527-533` + `logger.go:220-225`，实测泄漏）；③ `Colorful: false`（ANSI 不再进日志）；④ **gin Recovery 不再 dump 请求头** —— `gin.Recovery()` **和** `gin.CustomRecovery` 都会在 `CustomRecoveryWithWriter` 内部把 `httputil.DumpRequest` 写进 `DefaultErrorWriter`、且只屏蔽 `Authorization`，**Cookie 里的 `auth_token`（JWT，可直接重放）明文落盘**（默认 `server.mode: debug`），改为手写 recover（`middleware/recovery.go`）+ `gin.Default()`→`gin.New()` 顺带去掉重复挂载的 Logger/Recovery（G-12 的 ① 已一并修）；⑤ `pkg/logger` 级别归一化 —— 原先只认大写，而 `config.yaml` 写的是小写 `info` → `order["info"]==0` 等价 DEBUG，**级别过滤完全失效**；同时补 `viper.SetDefault("log.level","info")`；⑥ seed 不再打印明文默认密码。**验证**：10 项变异反证全红且都红在断言上（删 `RecorderParamsFilter` → 实测打出 `password_hash = "$2a$10$…"`；routes 换回 `gin.Recovery()` → 实测 dump 出 `Cookie: auth_token=eyJ…`；改用 `gin.CustomRecovery` → `DefaultErrorWriter` 非空红；级别映射/默认值/参数化/颜色/归一化/`SetDefault` 各一条）；`go test ./...` 26 包绿 + `go vet` 干净。**残余**：错误文本旁路（PG 约束冲突消息、上游响应体、钉钉 webhook token）→ 见 G-28。
- [ ] **G-17 aux 服务生产化（netbox/zabbix/glpi/graylog/elasticsearch/mongoDB）**（2026-09-09 新增，compose 轮审查发现）— 本轮只做了 `profiles: ["aux"]` 隔离（默认不启动）+ DB 密码与主链同源，其余仍是占位/不安全默认：① 与主链共享 postgres `nmp` 角色（应各建独立角色与库，库也未初始化）；② `SECRET_KEY`/`GRAYLOG_*` 占位凭据（graylog 的 SHA2 值非法，根本起不来）；③ `elasticsearch` 关掉 `xpack.security`；④ 版本标签全是可变的（`latest`/`6.0`/`7`）；⑤ 与 api 同处 default 网络（可直连内部 gRPC 50051）；⑥ zabbix 用的是 `zabbix-server-pgsql`（无 Web/API），`NMP_INTEGRATIONS_ZABBIX_URL` 目标不对，需 `zabbix-web-nginx-pgsql`。**2026-09-09 安全审计补充（P2/P5）**：⑦ aux 与主链共用同一个 postgres 超级用户 `nmp` 与同一个 `default` 网络 → 拿下任一 aux 容器（netbox 的 `SECRET_KEY` 是公开占位值，可伪造会话）即从容器内 `env` 读到主库密码，直连读写 `users`/`audit_logs`；同网段还是 L2 共享域，可 ARP 冒充 web 的静态 IP `172.28.0.10` 从而变成「受信代理」伪造 XFF。加固方向：aux 各建独立角色/库 + 独立 network + `cap_drop: [ALL]` + `no-new-privileges`；ES 至少开 `xpack.security`。⑧ 版本标签钉死（`latest-pg16`/`latest` 会静默换版，带持久卷的 postgres 尤其危险）。
- [x] **G-9 `docker-compose.yml` 引用的 Dockerfile 在仓库中不存在 → `docker compose build` 必失败**（2026-09-09 新增，TLS 任务连带发现）— `docker-compose.yml:40-41` 与 `:59-61` 分别声明 `context: ./backend` / `./frontend` + `dockerfile: Dockerfile`，但 `git ls-files | grep -i dockerfile` 与全盘 `find` 均为空——**仓库里没有任何 Dockerfile**。后果：README/文档里的 `docker compose up -d` 开箱即失败；`nmp-api` / `nmp-web` 两个容器名（G-7 的受信代理讨论、§8.2.2 的 TLS 终止点都按它写的）在当前仓库状态下根本起不来。待办：① 补 `backend/Dockerfile`（Go 多阶段构建：`golang:1.22-alpine` 编译 → `alpine`/`distroless` 运行，非 root 用户）；② 补 `frontend/Dockerfile`（`node` 构建 → `nginx:alpine` 托管，默认站点用 `frontend/nginx.conf`，TLS 场景见 §8.2.2）；③ CI 增一步 `docker compose config`（不 build）防止引用漂移；④ 若决定不提供 Dockerfile，则从 compose 中删掉 `build:` 段并改用镜像名 **已交付（2026-09-09）**：① `backend/Dockerfile`（golang:1.25-alpine 编译 → alpine:3.20 运行，`CGO_ENABLED=0` 静态二进制，非 root uid 10001，busybox 自带 wget 做健康检查，build `server`/`migrate`/`admin-bootstrap` 三个命令；用 `CMD` 而非 `ENTRYPOINT`——ENTRYPOINT + compose `command` 会拼接成 `./server ./migrate up`，迁移会静默变成起 server）；② `frontend/Dockerfile`（node:22-alpine → nginx:1.27-alpine）+ 两个 `.dockerignore`；③ **根因之一：`.gitignore:48-49` 的裸 `Dockerfile`/`.dockerignore` 规则匹配任意深度，当年就算写了也进不了仓库**——已删除该规则（`git check-ignore` 复核）；④ CI 新增 `compose-config` job（`test -f` 四文件 + `docker compose config -q`，作用范围按实证收窄：config 不 stat Dockerfile、也不懂 `NMP_` 语义，env 名对齐靠 backend 单测）；⑤ `scripts/smoke-compose.sh` 真跑通并端到端验证 G-7。方案 `docs/FIX-PLAN-COMPOSE-RUNTIME.md`。

- [ ] **G-18 `database.Init` 的 gorm `AutoMigrate` 兜底在真实 postgres 上不可用**（2026-09-09 新增，compose smoke 实测）— `MigrationsFS` 未注入时 `Init` 走 else 分支的 `autoMigrate()`（`internal/database/database.go:67-73`，函数体 `:79-`）。实测：空库上它建表但**不建迁移里的种子数据**（roles 无 admin → `admin-bootstrap` 报「未找到 admin 角色」）；已迁移的库上它与迁移 DDL 漂移，`AutoMigrate` 直接报 `insufficient arguments`（`database.go:106`）。受影响的三个 CLI （`cmd/admin-bootstrap`、`cmd/seed`、`cmd/set-role`）已在 compose 轮注入 `MigrationsFS` 修复（D-I），但兜底本身仍在：待办 ① 删掉兜底改为显式报错（「请注入 MigrationsFS」），或 ② 限定为 sqlite 测试专用（加注释 + 在 postgres 驱动下拒绝进入该分支）。**注意**：现有单测依赖 sqlite 走这条兜底，改动需同步。

- [ ] **G-19 `web` 容器以 root 运行、无最小权限**（2026-09-09 新增，安全审计 P3）— `frontend/Dockerfile` 的 `FROM nginx:1.27-alpine` 之后没有 `USER`，nginx master 以 root 跑（默认 caps 全开），而它是**唯一对外入口**；对比 `backend/Dockerfile` 的 `adduser -u 10001` / `USER nmp`。本轮已补 `HEALTHCHECK`（`--wait` 才真的等到 nginx 可用）。待办：换 `nginxinc/nginx-unprivileged:1.27-alpine`（或自行 `USER nginx` + 改 pid/temp 路径 + `listen 8080`），并加 `read_only: true` + `tmpfs: [/var/cache/nginx, /var/run]` + `cap_drop: [ALL]`。注意 `frontend/nginx.conf` 同时是非 compose 部署模板，改监听端口要同步 `08-部署运维.md` 与 `frontend/nginx-tls.conf.example`。

- [x] **G-20 `models.Asset.CustomFields` 零值 `''` 在真 Postgres 上是非法 JSON → 建资产失败**（2026-09-09 新增 → 当日结案，CLI 真库回归实测）— 原状：`internal/models/asset.go:48-49` 两列 `type:jsonb` + Go `string`，零值 `""` 被写进 INSERT → PG `invalid input syntax for type json (22P02)`；`cmd/seed` 每条资产都建不出来却 `exit 0`（静默半失败），`POST /api/assets` 不带 `custom_fields` 直接 500；sqlite 不校验 JSON 所以单测全绿。**已交付**（`docs/FIX-PLAN-ASSET-JSONB.md`，三视角审查处置见其 §7）：① `models.Asset.BeforeSave` 把空值归一为 `[]`/`{}`（覆盖 Create/Save，sqlite 默认门禁可断言）；② `migrations/000014_asset_jsonb_defaults.{up,down}.sql` —— 回填 NULL + `SET DEFAULT`（**UPDATE 排在 ALTER 之前**并加 `lock_timeout='5s'`：`execInTx` 单事务内 `ALTER` 的 ACCESS EXCLUSIVE 持有到 COMMIT，顺序反了会让回填期间 `assets` 全表不可读写），down 只 DROP DEFAULT 不动数据；③ `cmd/seed` 失败计数 → 非零退出（`make deploy`/CI 不再把半种子当成功）；④ `integration/service.go:127` 的 `Tags: "{}"` → `"[]"`（列语义统一）；⑤ `scripts/db_smoke.sh` 升级库按**版本号**过滤旧迁移（原 `! -name '000013_*'` 会把 000014 也预应用 → 回填恒命中 0 行的假绿）+ 预置 NULL/非 NULL 两行存量资产；⑥ `TestDBSmoke_DownPreservesLegacyColumns` 先回滚 14 再回滚 13（`migrate.Down` 只回滚最新版本，否则用例空转）；⑦ `ci.yml` seed 步断言资产数 > 0 且无 NULL jsonb；⑧ `smoke-compose.sh` 5b 资产断言 + 6b 经 nginx 建资产（201 且 tags=[] custom_fields={}）。⑨ **连带发现（已修）**：`cmd/seed` 演示资产 `asset_tag` 补 `rack.Name` —— `unique_asset` 经 000013 把 `assets.idc_id` 改名为 `site_id` 后语义是「站内唯一」，而 tag 只含 `site.Code`（`AST-DC-BJ-01-001`），同站点 4 个机柜撞键 → 27 服务器 + 9 交换机 `23505`，**48 个演示资产实际只落库 12 个**；G-20 前这些失败只打日志、`exit 0`，CI 的 `assets > 0` 照样通过。配套 `cmd/seed/main_test.go` 新增「站内唯一 + 48 资产」断言，测试 schema 补 `UNIQUE (asset_tag, site_id)` 镜像真库约束。**验证**：`go test ./...` 26 包绿；`scripts/db_smoke.sh` 两条路径绿；变异反证（删钩子 → V-1/V-2/V-6 红；删 `SET DEFAULT` → V-3 红；删回填 → V-4 红；退回旧 `asset_tag` → 单测红 + 真 PG `exit 1`/36 处失败；删 000014 down 的 DROP DEFAULT → V-8 红）；本机真 PG 全新库跑 seed → `exit 0`、48 资产、0 NULL jsonb、幂等重跑 `exit 0`。**否决**：① 不加 `NOT NULL`（全表校验 + 重写 = 长 ACCESS EXCLUSIVE 锁，且 `PUT {"tags": null}` 仍能绕过，DB 约束换不来真不变量）；② 入参规范化不并入本轮（→ G-21，避免扩大爆炸半径）。**实测边界**：钩子只覆盖 Create/Save，`Updates` 一族全部绕过（结构体零值被 gorm 跳过 = 静默 no-op；`Select(...).Updates` / `Updates(map)` 写出 `''` → 22P02；map 传数组被渲染成 `('x')`、传 null 写入 NULL）→ G-21。

- [x] **G-21 资产 jsonb 入参规范化（`UpdateAsset` 的 `Updates(map)` 语义）**（2026-09-09 新增 → 2026-09-13 M42 结案）— `handlers/asset_handler.go:113-119` 把请求体直接当 `map[string]interface{}` 交给 gorm，于是 `{"tags": ""}`、`{"tags": []}`（合法 JSON 数组，客户端最自然的写法，gorm 渲染成 `('x')`）、`{"custom_fields": null}` 分别得到 22P02 500 / 22P02 500 / 写入 NULL（破坏 000014 回填出的「无 NULL」）。另外结构体 `Updates` 的零值被 gorm 静默跳过，`PATCH {"tags": ""}` 看起来成功其实没改。**M42 方案**（docs/M42-completion-report.md）：handler 层加 `normalizeJSONBFields` —— null/`""`/string 标量/非空数组/数值标量 一律返 `ErrInvalidJSONBInput` (400)；空数组 `[]`/空对象 `{}`/非空对象/非 jsonb 列 通过。**与 G-20 提案的区别**：G-20 提案写「建议归一为 [] / {}」，M42 改「统一 400」——理由：handler 层统一拒比归一更明确（client 立即知道错），与 BeforeSave 钩子的「Create 路径零值归一」形成对照（Create 容错 / Update 严格），且 `Updates` 路径无 BeforeSave 钩子兜底，归一会让行为不一致。**已交付**：① `handlers/jsonb_normalize.go` `normalizeJSONBFields` helper（覆盖 tags + custom_fields 两列，10 单测覆盖）；② `handlers/asset_handler.go` UpdateAsset 接线 `normalizeJSONBFields` → `apierr.BadRequest`；③ 真 PG `TestDBSmoke_G21_JSONBUpdateReject` 反证 service 层兜底确实有 42804 类型风险；④ `CHANGELOG.md` M42 section。**验证**：`go test -race ./...` 27 包绿；`scripts/db_smoke.sh` 45 PASS / 0 FAIL；mutation inversion（`checkJSONBValue` 永返 nil → 6 测试 FAIL → revert → PASS）。**残余**：service.Update 仍是裸 `Updates(map)`，handler 是唯一守门。若未来有第二个 handler 绕过 normalizeJSONBFields，service 层会爆 42804；建议下 round 在 service 层加 `Updates(map)` 入参校验（不在 M42 scope，避免扩大爆炸半径）。**结构体 `Updates` 零值被 gorm 跳过**（静默 no-op）问题本轮不动——`db.Model(&a).Updates(Asset{Tags: ""})` 行为是 gorm 设计，不算 ITmanager 缺陷。

- [x] **G-22 NetBox 同步 upsert 用 Go 字段名当列名 → `42703 column "Name" does not exist`**（2026-09-09 新增，G-20 审查连带发现，既存缺陷 → 当日结案）— 原状：`integration/upsert.go:13-22` 把 Go 字段名直接塞进 `ON CONFLICT ("netbox_id") DO UPDATE SET "Name"=EXCLUDED.Name, ...`（`clause.Assignments` 不做字段→列名映射），实测首次插入即失败；与 jsonb 无关。**已交付**（`docs/FIX-PLAN-NETBOX-UPSERT.md`，三视角审查处置见其 §1.1 F-4/F-6/F-7 与 §1.3 F-5、§4 V-0 实测表）：① `buildUpsertClause` 改用 `clause.AssignmentColumns`（参数语义钉死为 **DB 列名**），空列表 → `DoNothing`（gorm 空 `Set` 会渲染 `SET "id"="id"`，PG 上 `42702 ambiguous`，实测）；② 三条路径**全修**：NetBox 走真 upsert（冲突目标 `net_box_id`，更新列 `name, asset_type, brand, model, sn, site_name, updated_at`），Zabbix/GLPI 去掉 `ON CONFLICT`（Zabbix 侧加回必然 42P10 —— `alerts.trigger_id` 无唯一索引，且唯一还会吃掉告警历史；GLPI 侧加回是 42702 —— `SET "id"="id"` 在解析期就歧义，早于仲裁索引检查）；③ `migrations/000015_asset_netbox_unique.{up,down}.sql` 把 `idx_assets_net_box_id` 改成 UNIQUE（PG 唯一索引允许多 NULL → 手工资产不受影响；`DROP INDEX` 的 ACCESS EXCLUSIVE 持有到 COMMIT，读写全阻塞，注释写明大表走维护窗口）；`Asset.NetBoxID` 标签 `index` → `uniqueIndex` 对齐；④ 删掉 `SyncFromNetBox` 的「预查询 + 回填 ID」（其预查询写 `netbox_id IN ?` 也是错列名 → F-6）。**连带修复**：更新列原含 `status`，而 `ConvertToAsset` 硬编码 `"active"` —— 会把本地已退役资产静默改回 active 且留下 `retired_at`（F-7）；漏了 `site_name`（换机房后本地永陈旧）。**验证**：`go test ./...` 26 包绿 + `go vet` 干净；`scripts/db_smoke.sh` 两条路径绿（新增 `TestDBSmoke_NetBoxUpsert`：索引唯一性 / 重复拒绝 / 多 NULL / 真同步端到端 / 脏数据挡住迁移的负循环）；**变异反证 10/10 变红**（M1 手拼 EXCLUDED.、M2 SET 退回 Go 字段名、M3 冲突目标退回 `netbox_id`、M4 测试库去唯一索引、M5/M6 Zabbix/GLPI 加回 ON CONFLICT、M7 000015 建普通索引、M8 更新列加回 `status`、M9 去掉 `site_name`、M10 退回「两次 Down」的旧版本）。**M10 的注意点**：反向变异（只删最后一次 Down）**不会变红、会静默空转** —— 末尾断言恒真，这正是必须显式补第三次 Down 的理由（V-6）。**实测教训**：sqlite 列名解析大小写不敏感 → `SET "Name"=EXCLUDED.Name` 在 sqlite 上**静默成功**，故单测必须断言渲染出的 SQL 字符串（见 T-30）；迁移失败日志里**没有 PG 的 DETAIL**（驱动 `PgError.Error()` 只拼 Severity/Message/SQLSTATE，本仓库未开 gorm `TranslateError`），只剩 `ERROR: could not create unique index … (SQLSTATE 23505)` → 定位重复值只能靠升级前自检 SQL。**第二轮（测试有效性 + 正确性审计，2026-09-09）**：F-A（最严重）修复 —— 删掉 000015 时两套用例原本**全 SKIP + 脚本 EXIT=0**，现改为「前置缺失 → `Fatalf`」+ `MigrateRunner` 守恒断言（`applied == embed 内 *.up.sql 数` 且 15 已记录），实测变异 N1/N1b 均红（`docs/FIX-PLAN-NETBOX-UPSERT.md` §4 V-11/V-12）；F-D/F-2 修复 —— 更新列提为 `netboxUpdateCols` 并由单测**引用该变量**断言（不再另抄），加 `tags`/`custom_fields`/`rack_name` 不被覆盖的断言（N2 实测红）；F-C/F-5/F-3 修复 —— dbsmoke 预置人工标签断言不被覆盖、批次内按 `net_box_id` 去重（PG 同批重复 id 会 21000 整批回滚，N5 实测红）、`now` 改 `time.Now().UTC()` 对齐 gorm `NowFunc`；F-8 顺手修 —— `PATCH /assets/:id` 撞唯一索引从 500 改 409；F-1 划界 —— Zabbix 预过滤只认 `status='problem'`，本地已 ack 的告警会被重复插入，属语义决策 → **G-27**，并用断言钉住当前行为；F-7 补 `t.Cleanup` 恢复共享库状态。新增用例：混合批次、同批重复 id、空列表/错误透传、ack 边界、`Update` 409；`SyncFromNetBox` 语句覆盖 78.6% → **94.4%**（`SyncFromZabbix` 82.8%、`SyncFromGLPI` 85.2%）。变异反证累计 **15 项全红**（M1–M11 + N1/N1b/N2/N5），并新增 T-31（两类假绿：前置 Skip / 测试抄一份生产清单；变异要红在断言上而非编译上）。

- [x] **G-24 seed 演示网络接口的 IP 非法（`192.168.A.10`）**（2026-09-09 新增，G-20 退出码连带暴露）— **已修（2026-09-10）**。原状：`cmd/seed/main.go` 用 `fmt.Sprintf("192.168.%s.10", rack.Row)` 拼 IP，`rack.Row` 是机柜排字母（`A`/`B`）→ 生成 `192.168.A.10` 这类**非法 IPv4**。000013 把 `asset_networks.ipv4_address` 从 inet 改成 varchar(45) 之前是**硬失败**（被 `fail()` 记成日志、进程仍 exit 0）；改名后静默入库。**同一个 bug 还有第二面**（原 TODO 未记）：同机房下 A01/A02/A03 三排机柜的 `Row` 都是 `"A"`，9 台服务器 × 3 张网卡共用同一批地址 —— 108 张网卡只有 6 个不同值。
  - **修复**：改用 **RFC 5737 文档网段**（TEST-NET-1/2/3：`192.0.2.0/24`、`198.51.100.0/24`、`203.0.113.0/24`）—— 标准保留给示例/文档，永远不会被路由，演示数据不可能撞上真实设备。新增 `demoSiteNets` + `demoIPv4(siteIdx, rackIdx, serverIdx, nicIdx)`：第三段按机房取网段（**三个机房 ↔ 三个网段一一对应**），第四段按 `rackIdx*9 + serverIdx*3 + nicIdx + 1` 编码 → 机房内天然唯一。**刻意不用递增计数器**：那会在 seed 长大后静默溢出成 `192.0.2.300`，重蹈「非法值静默入库」的覆辙。三个 range 循环改为带下标；新增 `len(sites) != len(demoSiteNets)` 前置校验（加机房不扩网段会静默复用 IP 段）。`alerts` 里 5 条同源宿主 IP 字面量一并改（`switch-Rack-A03-01` 留空是**预期**：seed 只给交换机建无 IP 的接入端口）。
  - **测试**：`cmd/seed/main_test.go` 两条 —— `TestSeed_演示IP合法且不重复`（每个非空 `ipv4_address` 必须 `net.ParseIP` 可解析 + 108 张网卡产出 108 个互不相同的地址）与 `TestSeed_演示IP限定在RFC5737文档网段`（**只断言「合法」不够**：改成 `192.168.1.10` 一样合法却可能指向真实资产）。`demoIPv4` 语句覆盖 100%。
  - **变异反证 2/2 红在断言上**：M1 `demoIPv4` 退回旧拼法 → 两条用例都红（`ipv4_address="192.168.A.10" 不是合法 IPv4（G-24）` / `不在 RFC 5737 文档网段内`）；M2 网段换成 `192.168.1/2/3`（**仍合法、仍唯一**）→ **只有** RFC 5737 那条红，证明两条用例守的是不同性质、不互为冗余。
  - **顺带发现（未修，登记观察）**：sqlite 测试台里 `assets.id` 恒为零值 —— GORM 对带 `default` 标签的零值主键不发列，而 sqlite 的 `TEXT PRIMARY KEY` 允许多个 NULL（真 PG 由 `gen_random_uuid()` 生成，无此问题）。既有测试台特性，与 G-24 无关，测试里的归属描述因此改用 `interface_name`。
- [x] **G-23 `ticket.Tags` 表示一致性**（2026-09-09 新增，G-20 审查划界）— M43 ship：handler + service 双层入参规范化. `models.Ticket.Tags` 类型仍 string (跨多文件, 留 backlog).
- [x] **G-25 GLPI 同步一次新增 ≥2 张工单必失败（`ticket_number` 撞号）**（2026-09-09 新增，G-22 修复实测暴露）— **已修（2026-09-10）**。原状：`models/ticket.go` 的 `Ticket.BeforeCreate → generateTicketNumber` 按「**当天已建条数**」算号（`TICKET-YYYYMMDD-<序号>`），而 gorm 的 `before_create` 回调对整片 slice 的**每一行都跑完才进 INSERT** → 每行查到的条数相同 → 整批拿到同一个号 → `tickets.ticket_number` 唯一索引拒绝整批，`SyncFromGLPI` 返回 error、`SyncAll` 记 glpi 失败。**实测**：一次 `CreateInBatches` 2 张票 → 同一个 INSERT 里两行都是 `TICKET-20260910-A` → `UNIQUE constraint failed`；逐条 `Create` 正常（每行插完再算下一行）。影响面：GLPI 首次同步（或任何新增 ≥2 张票的同步）全部失败。
  - **选型**：三个候选里取「**批量插入前显式预分配**」——① 按 `external_id` 派生会改掉用户可见的工单号语义（`GLPI-42` vs `TICKET-…`），且 `external_id` 上无唯一索引、跨集成源不保证唯一；② 改逐条插入 + 冲突重试要往 `IntegrationService` 里注入 `TicketService`（新耦合）且 N 次往返；③ 预分配是三者中唯一「不改语义、不加依赖、不改调用方结构」的。
  - **修复**：`models/ticket.go` 抽出 `ticketNumberPrefix()` / `nextTicketSeq()`（`generateTicketNumber` 同步改用，行为不变），新增导出 `AssignTicketNumbers(db, tickets)` —— 查一次当天条数，从该序号起给每行连续分配；已填号的行走 `BeforeCreate` 的逃生门（`TicketNumber != ""` 不覆盖）原样保留；`integration/service.go:263` 在 `CreateInBatches` 前调用。
  - **残余风险（已修，M34 D-2 工单号 used-set+max+1）**：删过工单后 count 回退 → 算出已占用的号。**2026-09-12 M34 修复** —— `models/ticket.go` 把 `generateTicketNumber` 与 `AssignTicketNumbers` 的「当天条数法」统一改为「当天 used-set 求 max + 1」算法（`7fb2ca9`），空洞免疫。`migration/000028_tickets_schema_align.up.sql` 同步给 tickets 加 `DROP NOT NULL ticket_type` 让模型字段语义可落库（`4496087`）。测试：`db_smoke_test.go` 新增 `TestDBSmoke_TicketsSchemaRoundTrip` + `TestDBSmoke_GenerateTicketNumberDayScoped`（25 张同号全 distinct A..Z + AA） + `TestDBSmoke_TicketNumberRetry`（23505 唯一索引兜底），`80be839` 已绿在真 PG。文档 `docs/FIX-PLAN-D1-D2-TICKETS.md` + `docs/IMPL-D1-D2-TICKETS.md`（`bc63311`/`135e6ac`）。**残余（G-25 家族非阻断）**：① 并发同步之间不互斥（需求 §1.5 已登记超范围），由 ticket_number 唯一索引 + TicketService.Create 重试兜底；② `migration/000028` 是 000013 的零增量补丁（重复的 ADD COLUMN IF NOT EXISTS = noop）—— 后续读 `schema_drift_test` 时会见到「`tickets` 重复声明」警告，非新缺陷，是为幂等保留。
  - **测试**：`internal/models` 三条 —— `TestTicket_CreateInBatches_不预分配则整批同号`（**根因特征化**：断言两行号相同 + 错误含 `UNIQUE`）、`TestTicket_AssignTicketNumbers_批量预分配互不相同`（A/B/C 连续 + 预分配后整批可插 + 第二批续 `-D`）、`..._已有号不覆盖`；`internal/integration` 一条端到端 —— `TestSyncFromGLPI_一次同步多张工单不撞号`（喂 3 张票全入库且号互异 + 第二次同步 0 新增）。`TestSyncFromGLPI_两次同步不重复` 的「G-25 未修故只喂 1 张」注释同步更新（喂 1 张的理由改为「与多张用例分工」）。变异（删掉 `AssignTicketNumbers` 调用）**红在业务断言上**（`Messages: 一次同步 3 张新工单不得撞号（G-25）`），非编译/SQL 匹配错误。`ticket.go` 语句覆盖 **100%**（`AssignTicketNumbers`/`nextTicketSeq` 均 100%）；`go test -short ./...` 27 包绿、`go vet ./...` 干净。新增 trap 见 `docs/TRAPS.md` **T-39**。
- [x] **G-26 `migrations/000014` 的 `SET lock_timeout` 会泄漏到连接池的连接上**（2026-09-09 新增，G-22 审查发现）— **已修（2026-09-10）**。`000014_asset_jsonb_defaults.up.sql` 原用会话级 `SET lock_timeout='5s'`（不是 `SET LOCAL`），`execInTx` 事务 COMMIT 后该设置**仍留在会话上**，连接归还连接池后同一个连接后续所有语句都带 5s 锁超时 → 高并发/长事务下业务写入莫名 `55P03 lock_timeout`，报错点与迁移毫无关联。
  - **修复**：改 `SET LOCAL lock_timeout = '5s'`（作用域止于本事务，对同步执行的 ALTER 保护不变）。全仓仅此一处会话级 `SET`（000013:295 是 `SET ROLE`，不同物）；唯一执行路径 `execInTx` 本就是事务（`internal/migrate/migrate.go:298-325`），`SET LOCAL` 不会因 autocommit 静默失效。
  - **测试**：新增 `TestDBSmoke_MigrationNoSessionGUCLeak`（真 PG）。**关键设计**：不复用 `migrate.Up` 复现——GUC 落在执行迁移的那条物理连接上，而 `migrate.Up` 期间至少占两条连接（`acquireLock` 的会话级 advisory lock 单独持一条，`migrate.go:54-79`），归还顺序 LIFO，裸查 `SHOW lock_timeout` 大概率拿到干净那条 → **假绿**。改为显式 `sqlDB.Conn()` 独占一条连接，同一事务跑完 000014 文件，再在同一条连接上断言 `SHOW lock_timeout != '5s'`；并加前置断言（起点必须是 PG 默认 `"0"`）。测试名已加入 `scripts/db_smoke.sh` 的 `-run` 列表（该脚本用显式正则列举用例，漏加即永不执行）。
  - **验证**：`scripts/db_smoke.sh` 全绿（全新 11 用例含新用例 + 升级路径 3 用例）；变异（`SET LOCAL` → `SET`）**红在断言上**（`assert.NotEqual(t, "5s", after)` 的业务文案，非 SQL 匹配错误）；`go test -short ./...` 26 包全 ok。

- [x] **G-27 Zabbix 同步会把「本地已确认」的告警重复插入（同一 trigger 两行）**（2026-09-09 新增，G-22 正确性审查发现；**2026-09-11 已修，M27/A**）— `integration/service.go:168` 的预过滤只认 `status = 'problem'`，而运维在 ITmanager 里点「确认」后本地行变成 `status='acknowledged'`（`service/alert_service.go:212`）。Zabbix 侧 `trigger.get` 带 `only_true:true` + `filter:{value:1}`（`integration/zabbix.go:166-168`），trigger 未恢复就一直在结果里 → 下次同步预过滤查不到该行 → **再插一行 `status='problem'`**，同一 trigger 在告警列表出现两行、ack 状态丢失。修复前 Zabbix 同步在真 PG 上必 42703/42P10（从未写过数据），属「修复解锁」的既存缺陷。**两条路**：① 预过滤改 `status IN ('problem','acknowledged')`（承认「本地已 ack 的进行中告警不再重复插」）；② 维持现状 + 明写边界。取舍点：ITmanager **没有恢复同步**（只插 problem、从不把行改成 resolved），所以本地 ack 行对应的仍是 Zabbix 侧同一个进行中告警，①更贴近语义；但 Zabbix「恢复后再触发」的场景下本地 ack 行会挡住新告警（②反而能出第二行，代价是丢 ack）。属**语义决策**，G-22 轮不做，`TestSyncFromZabbix_本地已确认的告警会重复插入` 已把当前行为钉住（改语义时该断言要一起改）。  **已交付**（2026-09-11，`docs/FIX-PLAN-ZABBIX-SYNC.md` + `docs/IMPL-ZABBIX-SYNC.md`，四个独立 commit：文档定稿 / 迁移 000027 / A / B，本行随第 5 个 commit 收尾）：走**路线①**并推广到底 —— 判据不是「`status IN (problem, acknowledged)`」，而是「**同一 trigger 的同一次故障发生**」：`lastchange` 可用时按 `(trigger_id, problem_start.Unix())` 精确去重（`alertIdentityKey`），只有在 `lastchange` 缺失/不可解析时才退回「同 trigger 且未解决」（Go 形态 `status != "resolved"`，**不得**写成 SQL `<>` —— 三值逻辑下 NULL 行的归属相反，方向从「多一行可见重复」变成「静默吞告警」）。**配套三件**：① 迁移 `000027_alerts_zabbix_identity_unique` 建部分唯一索引 `(trigger_id, problem_start) WHERE source='zabbix' AND trigger_id IS NOT NULL AND trigger_id <> ''`，插入走同事务 `ON CONFLICT (...) WHERE <与索引逐字相同的谓词> DO NOTHING` —— 预过滤是 TOCTOU，无仲裁者时并发撞车会让整批新告警一起回滚（42P10 / 23505）；② 预过滤与 `synced` 计数都加 `source='zabbix'` 收窄，与索引谓词保持同一判据；③ 迁移带 DO 自检：库里已有重复身份时**fail-closed** 并把样本 `trigger_id` 写进异常（运维否则只拿到无 DETAIL 的 23505）。**验证**：sqlite 六行行为表（含第 4 行反向用例「同 trigger 不同故障时刻必须入库」）+ 真 PG 冒烟 4 条（`AlertsZabbixIdentityUnique` / `ZabbixSyncOnConflict` 注入式混合批 / `Migration027BlockedByDuplicates` / `Migration027AllowsNullProblemStart`）；变异 M1–M9 与 S-1…S-4 **全红**，其中删 `TargetWhere` 在真 PG 上逐字红在 `SQLSTATE 42P10`。**已知假绿**（已写进测试注释，不假装守住）：删掉 `exact` 的**查询**那两行，六行表**全绿** —— 真正的兜底是 000027 的索引，由真 PG 注入式用例守；`exact` 的价值是「少一次注定失败的往返」。**新 trap**：T-50（部分索引 + ON CONFLICT 的谓词必须逐字一致）、T-51（`migrate.Down` 只滚最新一层）。

- [x] **G-28 错误文本旁路：凭据随 `*url.Error` 落进应用日志与 `notification_logs.error_msg`**（2026-09-09 新增，G-16 三视角审查发现，未修）— G-16 只堵住了 **SQL 参数**，**错误文本**是另一条路。实测链路：`notification/sender.go:135` 把钉钉 webhook（`https://oapi.dingtalk.com/robot/send?access_token=…`，token 在 query 里）交给 `http.NewRequestWithContext`、`:140` 的 `http.Client.Do`；失败时 Go 返回 `*url.Error`，`Error()` 就是 `Post "https://…?access_token=SECRET": …` —— 完整 URL 连凭据一起。它同时走两个出口：① `worker.go:153` 的 `log.Printf("… send err for channel %s: %v")` → 应用日志（stdout，`log.level` 默认 info 也照打）；② `worker.go:246/253` 的 `markFailed(ctx, id, err.Error())` → `notification_logs.error_msg`（`models/alert.go:130`，varchar(500)，进备份、进只读 DB 账号视野）。钉钉机器人 token 可发任意消息到运维群（告警抑制 / 钓鱼）。同类：`apierr.go:38-41` 把 `internalErr.Error()` 原文写进 `gin.DefaultErrorWriter`（stderr）——包装过的驱动错误、上游错误原文都会落盘（**链路 A**：`handlers/channel_handler.go:85` 的 `apierr.Internal(c, "测试发送失败", err)` ← `service/channel_service.go:96` ← 同一个 `*url.Error`，即人工点「测试发送」也能触发，不必等告警）。第三条面：`middleware/recovery.go:27` 的 `slog.Any("panic", err)` 会把 panic 的 error 值整块打出（当前无带凭据的 panic 源，属潜在面）。**已核实的非向量**：PG 的 `DETAIL`（`Key (col)=(value)`）**不会**出现，pgx v5.5.1 的 `PgError.Error()` 只拼 `Severity: Message (SQLSTATE)`（`pgconn/errors.go:51-53`）。待办：① 通知发送错误按「渠道类型 + HTTP 状态」重写，**不带 URL**（`url.Error` 用 `errors.As` 拆出 `Op`/`Err`，`URL` 只留 scheme+host+path，query 一律丢弃或脱敏）；② `error_msg` 入库前过一遍脱敏（query 里的 `access_token`/`token`/`key`/`secret` 值替换为 `***`）；③ 给 `apierr.Respond` 的 5xx 分支加同样的脱敏，或改为只记 `code` + 类型化摘要；④ 回归用例钉住「含 token 的 URL 不出现在 error / 日志 / error_msg」。**已交付（2026-09-09，`docs/FIX-PLAN-ERROR-REDACT.md` rev2，三视角审查处置见其 §7、实现记录见 §8）**：① 新增纯函数包 `internal/redact`——`URL()` 把任何 URL 塌缩成 `scheme://host`（丢 userinfo/path/query，解析失败 → `<invalid-url>`），`Text()` 三条**结构性**规则（URL 形状 / `Authorization: Bearer|Basic` / 键值形态保留键名换 `***`），不按参数名做黑名单（黑名单必漏 path、header、userinfo 三种形态）；② `notification/sender.go` 新增 `urlErrCause`（`errors.As` 递归剥 `*url.Error`，上限 4 层，内层 nil/超限 → 固定文案 `"未知错误"`，**绝不回传原串**），钉钉/自定义 webhook 各 2 处 return 改为「`scheme://host` + 底层 cause」；非 2xx 与 email 文案**未动**；③ 四个出口全部接上：`worker.go:149/156` 两行日志、`markFailed` 入库前（先脱敏**再按 rune 截断**到 500——修 S-1 的字节截断切断 UTF-8 导致 PG 22021 拒收、行永远 pending）、`apierr.go` 5xx 内部日志、`integration_handler.go:121/174/213` 三处连通测试 400 文案；`markFailed` 的 UPDATE 错误不再静默丢弃（修 S-2）；④ 验证：`go test ./...` 26 包绿 + `-race` 绿 + `go vet`/`gofmt` 干净；`redact`/`urlErrCause`/`markFailed` 语句覆盖 100%；**变异反证 9/9 全红在断言上**（含 M3 `Text` 恒等、M8 日志去掉脱敏、M10 退回字节截断）；审查建议的 M5「先截断后脱敏」经四组构造实测**不可观测**，已从变异表移除并写明理由（顺序不是安全边界）。**残余**：`recovery.go` 的 `slog.Any("panic", err)`（无已知带凭据源）、`httpx.go:167` 拼上游 4xx 响应体 → 见 **G-31**；gorm/pgx 编码错误文本（`%#v`）→ **G-32**；seed 企微渠道键写错 + 钉钉 `SignSecret` 未参与签名 → **G-33**。 **第二轮（2026-09-09 晚，安全审计 + 正确性审计回执，处置见 `docs/FIX-PLAN-ERROR-REDACT.md` §9）**：① **H-1（高，已修）**——`integration/service.go:282/289/296` 与 `metric_sync.go:111` 四处 `log.Printf("%v", err)` 没接脱敏，集成 URL 的 query/path 凭据原文落 stderr；定案**在 httpx 出口收口**（`redactedErr`：`Error()` 过 `redact.Text`、`Unwrap()` 保错误链），一处覆盖全部消费者，避免下次新增日志点又漏；② **M-2/M-3（已修）**——规则 1 加 scheme-relative 分支（`//host/…`，authority 限「带点域名 / localhost / [IPv6]」防误伤路径）、值类不再排除 `'`/`\`、`URL()` 对 `Host` 尾冒号（`https://user:`）返回 `<invalid-url>`；③ **正确性 P1（已修）**——规则 3 值类放宽（不再排除 `}` `]` `<` `>`）、规则 2 放宽（不再排除 `,`），修 `{"password":"ab}c"}` 漏尾与 `password=}SECRET` 整条不匹配；④ **P4（已修）**——`markFailed` 加 `strings.ToValidUTF8`，修「≤500 rune 的非法 UTF-8 被 PG 22021 拒收 → 行永远 pending 无限重发」；⑤ 验证：**27 包全绿**、`-race` 绿、`redact`/`URL`/`markFailed` 100%，第二轮变异 **M11–M16 六项全红在断言上**。新增残余 → **G-34**（值边界）与 **G-35**（过度脱敏）。 **第三轮（2026-09-09 深夜，测试有效性审计回执，处置见同文档 §9.5）**：① **HIGH-1** webhook 的 parse 失败 return 零覆盖 → 补 `TestWebhookSender_非法URL不泄漏原串`（U1 变异红在断言，泄漏原文含 `http://[::1/services/T000/B000/SECRETPATH`）；② **HIGH-2** NetBox/GLPI 的 400 回显脱敏零覆盖 → 补两条用例（**U3′ 组合变异**——同时去掉 handler 层与 httpx 层脱敏——红在断言；**单层**去 handler 脱敏仍绿，因 httpx 已源头收口，登记 **T-35**）；③ **MED-1** `worker.go` resolver 日志零覆盖 → 补 `TestHandleAlertEvent_resolver错误日志不泄漏URL凭据`（`WorkerConfig.Resolver` 注入带凭据错误，U2 红）；④ **MED-2** `urlErrCause` 深度 4 丢 cause（文档称「上限 4 层」实际只处理 3 层）→ 把有界判定移到解包前，V-11 补 depth=4（U4 红）；⑤ LOW-1 恒真断言保留为哨兵、LOW-2 值类耦合保留（注释已说明）；验证：**27 包全绿**、测试函数 **959**、`notification` 覆盖 **73.0%**。

- [x] **G-29 容器日志无轮转（`json-file` 默认无上限）**（2026-09-09 新增，G-16 审查发现；**2026-09-11 M28/C 已交付**）— `docker-compose.yml` 全仓没有 `logging:` 段，Docker 默认 `json-file` 驱动**不轮转**：`api`（gin 访问日志 + gorm 慢查询/错误 + 应用日志）与 `web`（nginx access log）长期运行会把宿主盘写满，写满后 postgres 写入失败、容器被 OOM/异常重启，且 `docker compose logs` 本身也会变得不可用。G-16 把 SQL 参数化后单条日志变短，但不改变「无上限」这件事。待办：给所有服务加 `logging: {driver: json-file, options: {max-size: "10m", max-file: "5"}}`（或统一走 journald/远端采集），并把上限写进 `08-部署运维.md` §8.4.3。**已交付（2026-09-11，M28/C，`docs/FIX-PLAN-HARDENING.md` §8.3）**：`docker-compose.yml` 顶部加 `x-logging: &default-logging`（`driver: json-file` + `max-size: 10m` + `max-file: 5`），10 个服务（postgres/redis/api/web/netbox/zabbix/glpi/graylog/elasticsearch/**mongoDB**）各加一行 `<<: *default-logging`。**行号更正**：文档写在仓库根 `08-部署运维.md` **§8.4.4**（§8.4.3 是 log.level / SQL 日志，不是本节），新增章节含增长特征表、锚点坑、`--format json` 验证命令。**锚点坑（rev1 真踩过）**：YAML 的 `<<:` 是把锚点的**条目摊平**进目标映射、不是把目标嵌到锚点下，所以 `logging:` 这一层必须在锚点里；rev1 把 `driver`/`options` 裸挂在锚点下，`docker compose config` 报 `Additional property driver is not allowed`。**验证**：`docker compose --profile aux config --services | wc -l` = 10（rev1 曾误写成 9，漏了 `mongoDB`）。

- [x] **G-30 冒烟脚本把数据库口令放进进程参数与环境**（2026-09-09 新增，G-16 审查发现，未修，低危）— M44 ship：docker run argv 改 --env-file + .pgpass + PGPASSFILE + openSmokeDB 拆 PG* env vars. 三类泄漏 (argv/env/DSN) 全收紧.

- [x] **G-31 同类错误回显点未逐一收口（HTTP 400 文案 / 上游响应体 / panic 值）**（2026-09-09 新增，G-28 审查发现，未修）— M45 ship：apierr 4xx helper 入口全过 sanitizeMessage (redact+StripControl). 38 个 caller API 不变, 自动受益.
- [ ] **G-32 gorm/pgx 的编码错误文本可能带值（`%#v`）**（2026-09-09 新增，G-28 审查发现，未修）— pgx v5 在类型编码失败时会把**值本身**用 `%#v` 拼进错误（`pgtype/pgtype.go:1905` 附近），而 gorm 的 logger 只参数化了 SQL 文本、不处理错误文本。当前无「凭据 → 非字符串列」的调用点，故非活缺陷；若将来把 token/口令写进非 text 列（如 `jsonb`、`inet`、`uuid`）并触发编码错误，凭据会随错误文本落进 G-16 已接级别的 gorm 日志。待办：评估自定义 gorm logger 只打 SQLSTATE + 表名，或对 `Error()` 出口统一过 `redact.Text`。
- [x] **G-33 seed 企微渠道配置键写错 + 钉钉 `SignSecret` 解析后从未使用**（2026-09-09 新增，G-28 审查发现，功能缺陷非安全）— `cmd/seed/main.go` 给企业微信渠道写的键是 `webhook_url`，而 `WebhookSender` 只认 `url`（`sender.go` 的 `channelConfig.URL`）→ 该渠道恒报 `webhook: url is required`，通知永远发不出去；另 `channelConfig.SignSecret`（钉钉加签）解析后**没有任何代码使用**，配了也不生效。待办：seed 改键名 + 钉钉实现加签（或从 schema/UI 里去掉该字段以免误导）。**M1 已交付（2026-09-09，`docs/FIX-PLAN-NOTIFY-CHANNEL.md` rev2，三视角审查处置见其 §7）**：把问题扩到「配置契约错位」全域并修 M1——① 后端写入端 fail-fast：新增 `validateChannelConfig`（`notification.NewSender` 试构造，**不发起网络 I/O**，失败原因在 service 侧过 `redact.Text` 后随 `ErrInvalidInput` 返回——400 出口没有兜底，`apierr.BadRequest` 的 internalErr 恒 nil）；`Create` 校验名称+配置；`Update` 用**合并后的 (type, config)** 校验（只改 name/is_enabled 才跳过），`config`/`type` 非字符串一律 fail-closed（否则 gorm `Updates(map)` 会把 `float64`/`bool` 静默写成 `"12345.0"`/`"1"`，见 T-36）；② handler：`Create` 400 回显原因（原硬编码「渠道名称不能为空」会吞掉构造器原因）、`Update` 补 `ErrInvalidInput` → 400（原来会落成 500）；③ 前端 `Settings.tsx` 表单键名对齐后端 tag（原用 `smtp`/`port`/`username`/`password`/`webhook`，全是坏键）、端口改 `InputNumber`（`<Input type="number">` 产出字符串 → `SMTPPort int` 反序列化失败，M1 上线会把 UI 自己保存的邮件渠道全挡）、email 补 `from`/`to`（`Select mode="tags"`）、下拉去掉后端未实现的 `wechat`、编辑不再硬编码 `is_enabled: true`（原会把已停用渠道静默启用）；④ seed：邮件行补 `to`（**原本是启用中的坏行**）、钉钉行 `secret` → `sign_secret`、企微行 `webhook_url` → `url`；⑤ OpenAPI `config` 由 `object` 改 `string`（对象形态从未工作过：Create 400 / Update 500）；⑥ **跨语言契约单一来源** `frontend/src/pages/__fixtures__/channelConfigSamples.json`（前端 deep-equal、后端读同一文件喂 `NewSender`）。验证：968 backend 测试函数（27 包全绿）+ 170 frontend 测试（27 文件全绿）+ `tsc`/`lint` 干净；**变异 V-9/V-10/V-11 全红在断言上**。**M2（钉钉加签 + 响应 `errcode` 校验）见 rev5（已交付）；M3（企微 sender → G-36）未做**。 **rev3（2026-09-09 深夜，实现后三路只读审计回执，处置见同文档 §7.2）**：安全与正确性两路**独立命中同一个 HIGH**——`Update` 的 fail-closed 建在「精确匹配小写键」上，而 gorm `Updates(map)` 的 `LookUpField` 会把 Go 字段名解析到同一列，`{"Config":12345}` / `{"Type":"dingtalk"}` / `{"id":…}` 全部绕过校验落库（真 PG 18 实测修复前 HTTP 200；新增 T-37）→ 改「键归一化到小写 + 白名单（未知键 400）」并让写入值 == 校验值；② **前端 Modal 串记录（H-2）**：`initialValues` 只在挂载时应用，连续编辑两条渠道会把上一条写进当前记录（后端拦不住，写进去的组合自洽）→ 打开弹窗时 `form.resetFields()`；③ **400 body 回显 Type 的非 URL 形态（安全 M-1）**：裸 token / JWT / percent 编码 / 无 scheme / 多行都能穿过 `redact.Text` → `sender.go` 的未知类型错误改静态文案，400 出口从此无调用方可控内容；④ **契约只钉必填键（M-2）**：表单与样本「一起改名」两侧双绿 → 新增第三条腿「样本键集合 ↔ `channelConfig` json tag」（`sender_contract_test.go`）；⑤ 存量 `wechat` 行改为显示可读标签 + 下线提示（原会按 Webhook 渲染、必填永远过不了）；⑥ OpenAPI 响应 enum 加回 `wechat`、输入 enum 收窄、补 `is_enabled`；手写类型 `config` 改 `string`。验证：**971 backend 测试函数 / 172 frontend 测试全绿**，变异 **V-9..V-16 全红在断言上**（V-11「删 redact.Text」因 400 出口已无可控内容而**失效**，由 V-13「恢复 Type 回显」取代，见文档说明）。 **rev4（2026-09-09 深夜，第三路「测试有效性」审计回执，处置见同文档 §7.3）**：审计自设计 20 条后端 + 9 条前端变异逐条实测，结论是**代码无新缺陷、缺的是测试承重力**，4 处「变异存活」全部闭合——① **H-1** seed 钉钉行 `sign_secret` 无测试钉住（`NewDingTalkSender` 只要求 `webhook_url` 非空，键名改成 `secret` 照样构造成功）→ `cmd/seed` 用例加逐类型**键集合断言**（V-18 红）；② **M-1** 前端 `is_enabled` 保留逻辑无断言（改回硬编码 `true` 全绿 → 编辑已停用渠道会静默启用、真发告警）→ 用例 fixture 改 `is_enabled: false` 并断言 `updateChannel` payload（V-19 红）；③ **L-1/L-2** 兜底样本键名与 wechat 下拉 `disabled` 无测试 → 各补一条（V-21/V-20 红）；④ **L-3** 「只改name不触发校验」子用例此前不承重（种子行合法，恒校验也过）→ 种子改为存量坏行（V-22 红）。**两条存活判为结构性、明确接受**：删 `redact.Text`（400 出口已无任何可造向量）、`Update` 写回 `effType/effConfig`（顺序执行下行为等价，需 `-race` + 可控交错）。另修 L-6（describe 未重置 `apiKeyApi.list` → 测试顺序隐式耦合）。验证：**971 backend 测试函数 / 174 frontend 测试全绿**（+2），5 条新变异**逐条确认红在断言上**。 **rev5（2026-09-09，M2 交付：钉钉加签 + 回执校验，`docs/FIX-PLAN-NOTIFY-CHANNEL.md` §7.4）**：① **加签**——新增 `dingTalkSignedURL`（消歧义公式 `key=sign_secret`、`msg=timestamp+"\n"+sign_secret`、`sign=base64(HMAC-SHA256(key,msg))`；`url.Values.Encode()` 追加 query，不手拼 `&timestamp=`；ts 作参数传入便于测试）；`Send` 仅在 `sign_secret` 非空时加签，签名 URL 只放局部变量、不回写 cfg / 不进错误文本（限时凭据）；② **回执校验**——钉钉 2xx 后解析 `errcode`，非 0 返错（含 errcode + 脱敏截断后的 errmsg），**回执非 JSON 走 fail-closed**；通用 webhook best-effort（合法 JSON + 数值型 `errcode`/`code` 非 0 才判失败）；③ **文本卫生**——`sanitizeSnippet`（限读 4KiB → `redact.Text` → `ToValidUTF8` → 按 rune 截到 200）。验证：**981 backend 测试函数**（+10）/ 27 包全绿 / `gofmt`+`vet` 干净；签名向量由**独立实现**算得并断言 ≠ key/msg 写反的值；**变异 M2-1..M2-8 八条全红在断言上**（M2-7 首次写成字节截断会红在编译上，按 T-31 改成可编译变异后红在断言：`expected 200 / actual 68`）；新增残余 **R-12**（webhook best-effort 会把 `{"code":200}` 判失败）、**R-13**（钉钉回执非 JSON fail-closed 在网关改写响应体时误判）。**M3（企微 sender）仍待做 → G-36**。 **rev6（2026-09-09，M2 两路只读审计处置，`docs/FIX-PLAN-NOTIFY-CHANNEL.md` §7.5）**：安全与正确性两路**独立命中同一个 MEDIUM**——`dingRespErr` 对「合法 JSON 但无 `errcode`」（`{}`/`null`/`{"errmsg":"ok"}`/`{"errcode":null}`）判成功（零值 0），与「无法确认送达即失败」自相矛盾 → `ErrCode` 改 `*int`、缺键/null 一律返错；② 新增 `stripControlChars`（口径同 `handlers.sanitizeAuditUsername`），`sanitizeSnippet` 与 `worker.markFailed`（入库出口，覆盖 SMTP/驱动等非 sender 来源）都接上——NUL 会让 PG 22021 拒收 → 行永远停在 pending 被无限重发，CR/LF 可伪造行式消费的日志/error_msg；③ `webhookRespErr` 两个键都看完再决定（`{"errcode":0,"code":1}` 此前判成功）；④ 回执限读 4KiB → 64KiB（旧上界会切断合法 JSON → 真送达判失败）；⑤ 字符串/浮点 `errcode` 报专用文案（旧文案「不是合法 JSON」把排障带偏）；⑥ `dingTalkSignedURL` 显式 `ParseQuery` 检查 error（`u.Query()` 会静默丢掉 `?access_token=%zz`）；⑦ `InDelta` 60s → 10s。验证：**27 包全绿 / 986 测试函数**（+5）/ `-race` 绿 / `gofmt`+`vet` 干净；`stripControlChars`/`sanitizeSnippet`/`respBody`/`dingTalkSignedURL`/`dingRespErr`/`webhookRespErr`/`markFailed`/`DingTalkSender.Send` 覆盖 **100%**；**变异 M7-1..M7-9 九条全红在断言上**；新增残余 **R-14**（>64KiB 回执仍截断）、**R-15**（`Send` 需等 body 读完）；`customSenders` 无同步（既有）→ **G-38**。**结案（2026-09-12，M29 台账轮复核）**：本条已陈旧，两个原件均已交付 —— ① 钉钉加签：`dingTalkSignedURL` 已实现并接在 `DingTalkSender.Send`（`sender.go:377`），签名向量由**独立实现**算得并断言 ≠ key/msg 写反的值（`sender_sign_resp_test.go`，M2 rev5）；② 企微 seed 键：`cmd/seed/main.go:268` 现为 `{"url": …}` + `type: "wechat"`，`WeChatSender` 端到端已交付（G-36/M3），并有 `cmd/seed` 逐类型键集合断言钉住。本条无未做项。


### v1.0.2 已发布（6/17）
- [x] **README.md**：6 GitHub badges (Release/CI/License/Go/React/Docker) + 状态推进
- [x] **CHANGELOG.md**：v1.0.0 / v1.0.1 / v1.0.2 sections
- [x] **Makefile**：`make deploy` / `deploy-min` / `deploy-status` 3 target
- [x] **GitHub releases**：3 个 (v1.0.0 稳定 / v1.0.1 部署补丁 / v1.0.2 文档)

### v1.0.0 / v1.0.1 累计成果
- [x] **P0-P2 全链路**：P0-1 诊断时间线 + P0-2 抑制规则 + P1-1 拓扑 + P1-2 值班升级 + P2-1 Runbook + P2-2 Zabbix 兜底
- [x] **S 级 3 项**：暗色模式 (ab05d3d) + 误报 ML 训练集 (e502701) + Cmd+K 全局搜索 (f7e98eb)
- [x] **A 级 3 项**：ping/traceroute (6edd9c9) + 复盘 PDF (ea70644) + KPI 仪表盘 (2b893bc)
- [x] **A 级 review 全套修复** (3a667e8)：traceroute dead code + binary 启动验证 + 嵌入中文字体 + io.Writer 流式 + KPI 阈值常量 + KPIs sqlmock 测试
- [x] **测试指标**：backend 22 packages / 603 tests / frontend 19 files / 117 tests / tsc 24=baseline
- [x] **CI**：pre-commit hook (gofmt + swagger validate + .bak 拦截) + `.github/workflows/ci.yml`

### 性能总计
- 估时 14.5h → 实测 6.75h (**2.1x 加速**)

### 已完成 (2026-06-16 更新增量)

- [x] **测试覆盖**：backend 230 → 401 passed (20 packages)，frontend 14 → 53 passed (8 files)，总测试 338 → 454
- [x] **覆盖率**：61.2% (api 85.5% / apierr 88.5% / apikey 100% / config 94.3% / httpx 87.8%)
- [x] **代码审查 Bug 修复 (17)**：
  - handlers 8 bug：FailedLogin race / 弱密码 / 改回旧密码 / rate_limit 越界 / IP 越界 / uuid 静默 / type=garbage / Name UNIQUE
  - httpx 2 bug：half-open race / ctx 取消误触熔断
  - service 17 bug：Severity int / Limit=0 / 重复 First / Bulk 1000 上限 / 死代码 / usedMap 错 / User 分页 / Dashboard 单条 SQL / 假数据
  - cmd + 前端 + 中间件 4 bug：admin role 检测 / testdata schema 漂移 / asset_networks ipv6_address / localStorage 缺失
- [x] **Swagger UI 集成**：`/swagger/index.html` + `swagger-cli validate` CI
- [x] **type-safe API client**：3/13 服务方法已 typed，`npm run gen:api` 自动生成
- [x] **pre-commit hook**：gofmt + swagger-cli validate + .bak 拦截
- [x] **TESTING.md**：4.7K 测试现状报告

### 待完成 (2026-06-17 之后)

## 1. 数据库初始化 (优先级: 高)
- [x] 运行 `go run ./cmd/seed/main.go` 创建初始数据
- [x] 重启后端服务 `go run ./cmd/server`

## 2. 完善认证功能 (优先级: 高)
- [x] 实现 JWT Token 生成和验证中间件
- [x] 完善 Login/Logout 接口
- [x] 添加默认管理员用户 (admin/admin123)
- [x] 添加 API Key 认证支持
- [x] **登录失败锁定** (FailedLogin atomic CAS 修复 race)
- [x] **密码强度校验** (handler 拒绝弱密码)

## 3. 完善后端 API (优先级: 高)
- [x] 资产 CRUD - 完整实现
- [x] 告警确认/解决 - 完善逻辑
  - [x] **Bulk 操作 1000 上限** (ErrTooManyItems)
  - [x] **Severity 数值比较** (string → int)
- [x] 工单管理 - 本地 CRUD 完成 (GLPI 集成待完成)
- [x] **熔断器** (httpx: half-open race + ctx 取消修复)
- [x] **User List 分页** (避免全表扫描)
- [x] **Dashboard 单条 SQL 聚合** (5 次 count → 1 条)

## 4. 第三方集成 (优先级: 中)
- [x] NetBox 集成 - 资产同步
- [x] Zabbix 集成 - 告警获取
- [x] GLPI 集成 - 工单同步
- [x] 集成 API 端点

## 5. 前端完善 (优先级: 中)
- [x] 登录页面
- [x] 实时数据对接
- [x] **vitest 测试** (5 page + 8 hook + 31 store = 53 tests)
- [x] **queryKeys factory** (`useApiQuery.test.ts`)
- [x] **zustand store 单元测试** (`stores/index.test.ts`)
- [x] **type-safe API client** (3/13 服务方法)
- [x] **暗色模式** (zustand persist 'theme-storage' + ConfigProvider `theme.darkAlgorithm` + ThemeSwitcher 按钮 + 13 tests) — commit ab05d3d
- [x] **误报标记 + ML 训练集导出** (alerts 表加 4 字段 + `POST /alerts/:id/mark-fp` + `GET /alerts/false-positives/export` CSV + 8 backend tests + 2 frontend tests)
- [x] **全局搜索 Cmd+K** (Antd Modal + 自写 fuzzy 子序列匹配 + useGlobalHotkey hook + 3 资源跨搜资产/告警/工单 + 18 frontend tests)
- [ ] 机柜可视化增强
- [ ] 前端 coverage 工具 (无 `--coverage` 配置)

## 6. 文档与 CI (优先级: 中) — 2026-06-16 新增
- [x] **TESTING.md** (4.7K)：测试现状 + 21 bug 清单 + 覆盖率表
- [x] **Swagger UI** + CI validate
- [x] **TODO.md / tasks.md / 12-优化建议.md / 开发计划.md** 状态同步 (2026-06-16)
- [ ] **CI 升级**：加 `go test -race` + frontend vitest 步骤
- [ ] **CI 升级**：加 coverage 阈值门禁 (目前仅 generate)
- [ ] **覆盖盲区**：`internal/database` 0% (依赖 PG) / `internal/middleware` 36.5% (跟 api 共享) / `internal/integration` 39.5%
- [ ] **type-safe 推进**：10/13 服务方法仍 `data: any`

## 7. 部署与运维 (优先级: 低)
- [ ] 生产环境 docker-compose
- [ ] Nginx 反向代理
- [ ] HTTPS (Let's Encrypt)
- [ ] 日志收集 (Filebeat)
- [ ] 生产部署 + 冒烟测试

## 6. 优化路线图（详见 [docs/优化路线图.md](docs/优化路线图.md)）

### P0 - 立即做
- [x] **P0-1 诊断时间线** (2-3h) ✅ — `GET /api/v1/diagnostics/assets/:id/timeline` 聚合 alerts/tickets/asset_networks 4 张表 + MTTR 摘要 + 前端 Antd Timeline 渲染。Service 10 test + Handler 7 test + Routes 3 test + 前端 3 test = **23 new tests**。文档：`docs/优化路线图.md`。
- [x] **P0-2 抑制规则引擎** (4-6h) ✅ — `alert_suppressions` 表 + CRUD API + /preview 模拟评估 + in-memory 窗口缓存（sync.RWMutex）。Service 24 test + Handler 15 test = **39 new tests**。前端 `/alert-suppressions` 管理页 + menu。
- [x] **P1-1 网络拓扑** (6-8h) ✅ — `GET /api/v1/topology` 聚合 assets + asset_networks + alerts + 环形自动布局 + 虚拟节点 (connected_to 找不到时)。Service 11 test + Handler 6 test + 前端 5 test = **22 new tests**。前端 `/topology` SVG 渲染（0 依赖）+ 告警 badge + down 边高亮。

### P1 - 本月内
- [x] **P1-2 值班 + 升级** (8-10h) ✅ `720b307` — 4 表 (Schedule/Shift/EscalationPolicy/EscalationLevel) + 接 `now time.Time` 参数 + 排班冲突检测 + 升级链 + CRUD/GetCurrentOncall. Service 19 test + Handler 14 test + 前端 2 test = **35 new tests**. 12 route paths + swagger 10 paths + 5 schemas + type client 18 type.
- [ ] **P1-3 MIB 浏览器** (5-6h) — 网络设备 SNMP MIB 树浏览 → OID → 指标字典
- [ ] **P1-4 历史快照对比** (4-5h) — 2 个时间点资产配置 diff (CDP/LLDP/VLAN 变化)

### P2 - 下个月
- [x] **P2-1 故障 Runbook** (3-4h) ✅ `f79873f` — `runbooks` 表 + CRUD + ListForAssetTypeAndSeverity 推荐 + /runbooks/recommend. Service 16 test + Handler 15 test + 前端 3 test = **34 new tests**. swagger 6 paths + 1 schema + 25 Runbook types.
- [ ] **P2-2 Zabbix 兜底** (4h) — metric_snapshots 落 TimescaleDB

### 覆盖率目标 75%
- [ ] `internal/database` 0% → 60%
- [ ] `internal/middleware` 36.5% → 70%
- [ ] `internal/integration` 39.5% → 60%

### 用户/诊断小改进（穿插做）— 全部完成
- [x] 全局搜索 Cmd+K (3h) ✅ `f7e98eb` — Antd Modal + 自写 fuzzy + 18 tests
- [x] 一键 ping/traceroute (2h) ✅ `6edd9c9` — ICMP 探活 + 14 handler tests
- [x] 暗色模式 (0.5h) ✅ `ab05d3d` — Antd darkAlgorithm + zustand persist + 13 tests
- [x] 标记误报 → ML 训练集 (1h) ✅ `e502701` — 4 字段 + CSV 导出 + 10 tests
- [x] 故障复盘 PDF (4h) ✅ `ea70644` — go-pdf/fpdf + 霞鹜文楷嵌入 (OFL 1.1, 15MB) + io.Writer 流式 + 7 tests
- [x] 工时统计 KPI (3h) ✅ `2b893bc` — MTTR/MTTD/告警密度/SLA + KPI_THRESHOLDS 常量 + 4 sqlmock tests
- [ ] 统一 tags 中间表 (6h) — 搁置
- [ ] 移动端适配 (4h) — 搁置
- [ ] MIB 浏览器 (8h) — 搁置
- [ ] 历史快照对比 (6h) — 搁置

每条任务完成后跑：编写代码 → 代码审查 → 单元测试 → pre-commit → commit。

## 一键部署 (v1.0.1 新增，2026-09-09 按主链/aux 重构)

前置：`cp .env.example .env && chmod 600 .env`，三个 secret 用 `openssl rand -hex 32` 各生成一份
（缺值 compose 直接报错退出）。

```bash
# 生产（推荐）：装依赖 + 起主链 4 服务(postgres/redis/api/web)；迁移由 api 启动时执行
make deploy-min
# 首次 5-10min (拉镜像 + build)，后续 1-2min (缓存)
# 首个管理员：docker compose exec api env FIRST_ADMIN_USERNAME=admin \
#   FIRST_ADMIN_PASSWORD='<强密码>' ./admin-bootstrap

# 演示环境：同上 + seed（⚠️ 写入 admin/admin123 等已知密码账号，勿用于生产）
make deploy

# 健康检查：主链 4 服务 + aux（未启动显示 ⏸️）
make deploy-status

# 辅助系统（netbox/zabbix/glpi/graylog + es/mongo）：默认不启动
make docker-up-aux

# 详细命令
make help
```

详见 [08-部署运维.md](08-部署运维.md) §8.3 与 [README.md](README.md) 快速开始。

## 启动命令（手动模式，无 Docker）

```bash
# 1. 启动 PostgreSQL (如果未运行)
docker run -d --name nmp-postgres \
  -e POSTGRES_USER=nmp \
  -e POSTGRES_PASSWORD=*** \
  -e POSTGRES_DB=network_monitor \
  -p 5432:5432 timescale/timescaledb:latest-pg16

# 2. 初始化数据库数据
cd backend && go run ./cmd/seed/main.go

# 3. 启动后端
cd backend && go run ./cmd/server

# 4. 启动前端 (另一个终端)
cd frontend && npm run dev
```

## 访问地址
- 前端: http://localhost:5173 (vite dev) / http://localhost:3000 (docker)
- 后端 API: http://localhost:8080
- API 文档: http://localhost:8080/swagger/index.html
- 测试报告: [TESTING.md](TESTING.md)
- 一键部署指南: [Makefile](Makefile) + [docker-compose.yml](docker-compose.yml)

- [x] **G-34 脱敏规则 2/3 的值边界残余：值以分隔符或引号开头时不匹配**（2026-09-09 晚新增，G-28 第二轮正确性审计 P1 残余，未修；**2026-09-12 M30 已交付**，见 `docs/FIX-PLAN-REDACT-BOUNDARY.md` §8，方案 `8b03232` + 审计迭代 `7253331`）—
  **【交付摘要】** 三条「漏」全部转绿：`password=&SECRET` → `password=***`、`password=;SECRET` → `password=***`、`Authorization: Bearer "SECRET` → `Authorization: Bearer ***`（另有 `,`、`""`、`'''`、Tab/多空格等 L1–L11 全谱）。修法：F1 规则 2 值类收 `["']*`（起始引号并入值类）、F2 规则 3 值类收 `(["']*)` **捕获回写** + 起分隔符 `[&,;][^\s]*`、F4 分隔符补全角 `：`/`＝`、F4b 分隔符量词 `+`（连续分隔符）。变异 M30-M1/M2/M3/M3b/M7/M7b 全红在断言上。
  **【行为突变（已告知，CHANGELOG 有独立段落）】** ① 值首字符是 `&`/`,`/`;` 时遮盖到下一个空白（`?a=1&password=&b=2` → `?a=1&password=***`）；② 全角 `：`/`＝` 也成为键值分隔符，中文无词间空格 → `重置 password：请联系管理员` → `重置 password：***`；③ 连续分隔符整体保留（`password==SECRET` → `password==***`，此前 `password=***`），遮盖性不变。
  **【原登记内容（保留）】** 规则 3 的值类以「空白 / 引号 / `&,;`」为界，值**以**这些字符开头时整条不匹配（`password=&SECRET`、`token="SECRET`），规则 2 同理（`Authorization: Bearer "SECRET`）。RE2 无反向引用 → 做不到「同名引号配对」，当前用「引号可选 + 值类到定界符」近似。修法：拆成「引号形态」与「裸值形态」两条规则（引号形态用 `"([^"\n]*)"` / `'([^'\n]*)'` 两个分支），或改用小型状态机替代正则；改前先补 P1 的四组边界用例。可达性低（需凭据以 `name=value` 形态出现且值首字符是分隔符），但属该规则声称覆盖的形态。
  **【2026-09-11 M28/F 决策：降级为「已登记、待独立模块」，本轮不修】** 理由：① 可达性要求「凭据以 `name=value` 形态进日志」**且**「值首字符恰为定界符」，真实日志里罕见；② 修法（拆「引号形态 / 裸值形态」两条规则，或换小型状态机）会重写 G-28 刚收敛的三条规则，**必须独立一轮需求文档 + 变异验证**，塞进 M28 会污染一个已经很宽的改动面。
  **实测边界表（`redact.Text` 真实输出，probe 跑出来的、非推演）**：
  - `password=&SECRET` → 原样 `password=&SECRET` —— **漏**（本项，规则 3）
  - `password=;SECRET` → 原样 —— **漏**（本项，规则 3）
  - `Authorization: Bearer "SECRET` → 原样 —— **漏**（本项，规则 2）
  - `token="S` → `token="***` —— **不漏**（rev1 曾误判为漏：规则 3 的值类已含「可选引号组」，短值会被吃掉）
  - `http://token:8080/x` → `http://token:***` —— **过度**，不属本项，见 G-35
  **前置**：修之前先把上表 5 组固化成 P1 用例（现在只有 3 条是「漏」的期望行为，另 2 条是**边界锁定**，防止修 G-35 时互相踩）。
- [x] **G-35 规则 1 输出被规则 3 二次误伤：主机名命中敏感词时端口被抹掉**（2026-09-09 晚新增，G-28 第二轮正确性审计 P3，未修；**2026-09-12 M30 与 G-34 同轮交付**，见 `docs/FIX-PLAN-REDACT-BOUNDARY.md` §8）—
  **【交付摘要】** `http://token:8080/x` → `http://token:8080`（端口保留），全谱：`secret:8080`、`my-token:8080`、`pwd:6379`、`access_token:8080`、`https://pwd:443/a?token=abc` → 端口/主机保留、path/query 照旧丢。修法：`Text` 按 URL 匹配**分段**，URL 段只过 `URL()`，规则 2/3 只作用于非 URL 段（不再看规则 1 的输出）。**分段依赖的不变式**：`URL()` 的输出不含凭据形状 —— path/query/userinfo 由塌缩丢弃，host 里的键值分隔符（`=`/`＝`/`：`）由 F3b 挡成 `<invalid-url>`；放宽任一条，分段就退化成泄漏（这正是本轮两族净回归的成因，见下）。
  **【同轮新增的净回归防护（审计两路独立报出 + 11520 组差分复核）】** 分段把「规则 3 看得见规则 1 输出」这个副作用拆掉后，`<敏感键>=<URL>` 的值会退回明文（`password=https://example.com` → 旧版 `password=***`）。修法：`eatsFollowing()` **问规则本身**（哨兵试探）判断「这段文本之后的内容会不会被规则 2/3 吃成值」，命中则不切分。**弃用「另写一份凭据前缀模式」**的理由：该模式与规则 2/3 必须逐字同集，差分实测两族形态（引号形态、重复分隔符）它都漏过一次——两处模式不同集就是泄漏面。
  **【原登记内容（保留）】** `Text("http://token:8080/x")` → `http://token:***`：规则 1 先把 URL 塌缩成 `http://token:8080`，规则 3 又把 `token:8080` 当键值形态、把端口 `8080` 当值抹掉。**过度脱敏、不泄漏**，但排障时丢端口。 **【2026-09-11 M28/F 决策：降级为「已登记、待独立模块」，本轮不修】** 理由：方向是**过度**脱敏（不泄漏），代价是排障时丢端口；而两种修法都要求「规则 3 知道自己在 URL 里」——这正是 G-34 要引入的状态，**两项必须同一轮做**（否则改规则 3 的边界会同时移动 G-34 的三条「漏」用例）。
- [x] **G-36 企业微信（`wechat`）渠道端到端**（2026-09-09 新增，G-33 审查发现；**2026-09-09 已交付，见 `docs/FIX-PLAN-NOTIFY-CHANNEL.md` §7.6（交付）/§7.7（两路只读审计处置）**）— 前端下拉曾提供 `wechat` 但后端 `NewSender` 没有该类型（`unsupported channel type: wechat`）；M1 已把前端选项与 OpenAPI 输入 enum 去掉（存量 `type=wechat` 行不会走到后端：前端下拉保留一条禁用项显示「企业微信（暂不支持）」，编辑时渲染下线提示而**不**按 Webhook 渲染——正确性审计 M-1 更正了此处原先「会被 400 挡住」的说法）。要真正支持需新增 `WeChatSender`：body 为 `{"msgtype":"text","text":{"content":…}}`（当前 `WebhookSender` 发 `{"content":…}`，键名修好也发不出去）、解析 `{"errcode":N,"errmsg":…}` 判失败（钉钉/通用 webhook 的回执校验已随 M2 落地，可直接复用 `dingRespErr` 口径）、`NewSender` 注册、seed 企微行改 `type: "wechat"`、前端下拉与 OpenAPI enum 加回。属 G-33 的 M3。 **交付**：`WeChatSender`（body `{"msgtype":"text","text":{"content":…}}`、回执 `errcode` 复用抽出的 `errcodeRespErr(raw,label)`、错误出口过 `redact.URL`/`urlErrCause`）+ `NewSender` 注册 + seed 企微行 `type: wechat` + 前端下拉可选/表单只渲染 `url`（不给 secret 死键）+ 跨语言样本与 OpenAPI enum 加回；新增 9 个后端测试函数、替换 2 条前端用例，**变异 M8-1..M8-10 十条全红在断言上**；backend 995 测试函数 / frontend 174 全绿。残余：M1 期 seed 的存量 `type=webhook` 企微行需用户改选类型重存（不静默，行落 failed 可查；不加启发式迁移）。
- [ ] **G-39 告警规则的「通知渠道」选择实际不生效（`AlertRule.NotifyChannels` 只写不读）**（2026-09-09 新增，M3 正确性审计观察项，未修）— `models/alert.go:85` 的 `notify_channels` 只被 `cmd/seed` 写入，全仓**无任何读取点**：`worker.handleAlertEvent` 取的是「所有 `is_enabled=true` 的渠道」（`worker.go:121-122`），与规则上的选择无关 → 管理员在规则里勾掉的渠道照样收告警。同形：`config.DingtalkConfig`（`internal/config/config.go:120`）无人使用。修法：让 `handleAlertEvent` 按规则的 `notify_channels` 过滤（注意规则与告警的关联路径当前也不存在——需先定「告警命中哪条规则」的语义），或把字段从 schema/UI 摘掉以免误导。
- [x] **G-37 OpenAPI 的通知渠道路径与实际路由不一致**（2026-09-09 新增，G-33 M1 rev3 发现；**2026-09-12 M31 全部结案**，见 `docs/FIX-PLAN-OPENAPI-CONTRACT.md`）— `backend/internal/api/openapi.yaml` 写的是 `/notification/channels`、`/notification/channels/{id}`、`/notification/channels/{id}/test`（且 test 是 `post`），而真实路由是 `/notification-channels` 整组（`routes.go:363`）、test 是 `put`（`api.ts:214`）。文档-only 漂移，但会让「OpenAPI 是契约单一事实来源」的说法打折。修法：改 3 处 path + 1 处动词，`npm run gen:api` 重新生成 `api.types.ts`，并加一条「OpenAPI path 集合 == `routes.go` 注册集合」的测试（或反向由路由生成 OpenAPI）。**rev2（2026-09-09，commit 3988375，幻影端点已闭合）**：① 修 6 处幻影端点——`/notification/channels[/{id}[/test]]` → `/notification-channels`（3 条 path）、渠道测试 `post` → `put`、`/alerts/{id}/acknowledge` → `/alerts/{id}/ack`；② 新增 `TestRoutes_OpenAPI无幻影路径`（`routes_integration_test.go`）：从 `SetupRouter` 的 `Routes()` 取真实路由（gin `:id` 归一成 `{id}`），断言 **spec ⊆ 路由**——方向是「只挡幻影端点」，不要求路由全部已文档化；用例读 `/openapi.yaml` HTTP 端点（与 `/swagger` 同一份 embed 产物），变异 M6-1..M6-5 五条全红在断言上（含改 `routes.go` 动词、去掉 `:id` 归一化两条，证明用例读的是真实路由而非自证）；③ `npm run gen:api` 重生成 `api.types.ts`。**残余（文档完整度，非契约正确性）**：29 条真实路由仍未文档化——`/alerts/bulk-{ack,resolve,delete}`、`/assets/export`、`/assets/{id}/{retire,restore}`、`/audit-logs`、`/auth/{me,password}`、`/auth/api-keys`（4 条）、`/health`、`/integrations/*`（8 条）、`/notification-channels`（5 条）。修法：按组补 YAML（`notification-channels` 组可直接复用既有 schema，只差 path 名）。**M31 结案（2026-09-12）**：残余 22 条按组分两步补完（步 2 `/health`+`/integrations` 9 op / 8 schema；步 3 `/auth`+`/audit-logs`+`/alerts` 批量+`/assets` 13 op / 13 schema，全部字段来自 handler 实读），**实测路由集合 93/93 全文档化、spec 零幻影**。断言从单向升级为**双向集合相等**（`TestRoutes_OpenAPI契约集合相等`，D-1：**不设 allowlist**——allowlist 会把「未文档化」洗成合法态、理由字符串是软控制、交付态恒绿 vacuous）；另补 `TestRoutes_OpenAPI公开端点集合相等` 钉住鉴权语义（顶层 `security` 生效后，spec 标 `security: []` 的 3 条 == 运行时无凭据可达的 3 条）。**变异 M31-M1..M7**：M1（删 spec path）、M4（spec 方法名改大写）、M5（删公开端点的 `security: []`）、M6（给受保护端点加 `security: []`）、M7（删顶层 `security`）**只有新断言能抓**，旧断言全绿——这是「新方向确有守卫」的证据；M2（改动词）、M3（删路由注册）旧断言也红，只作交叉验证。**计划态归因更正**：§5.3 原称「删路由是旧断言覆盖不到的方向」**不成立**（旧断言遍历 spec 逐条核真实路由，删注册正好命中），已在文档 §8.2 更正。工具链：接线 `npm run validate:api`（swagger-cli，依赖早已存在但从未接线——README 的「validate 通过」此前是陈旧声明）+ `gen:api` 漂移门禁纳入每步验收。
- [x] **G-38 `customSenders` 是无同步的包级 map**（2026-09-09 新增，M2 安全审计 INFO，低危；**2026-09-11 M28/E 已交付**）— `notification/sender.go:93-96` 的 `var customSenders = map[string]Sender{}` + `RegisterSender` 无锁，`Resolver` 并发读。当前**生产无写入者**（`RegisterSender` 只在测试里调用），故非活缺陷；但若将来把它当运行时插件点用，或测试与 worker goroutine 并发，就是 data race（`go test -race` 会抓）。修法：`sync.Map` 或 `sync.RWMutex`（3 行），或改成显式依赖注入（`WorkerConfig.Resolver` 已有注入点，可让 `RegisterSender` 退役）。**已交付（2026-09-11，M28/E，`docs/FIX-PLAN-HARDENING.md` §8.5）**：取最短路 —— 加 `customSendersMu sync.RWMutex` + `lookupCustomSender`（读锁），`RegisterSender` 加写锁，3 行改动量级；**未**退役 `RegisterSender`（注入点在 worker 侧、与 `Resolver` 调用点不同层，改造面大于收益，且本轮目标是消除竞态而非重构扩展点）。测试：`TestResolver_并发注册与解析无竞态`（1 个写 goroutine 做 2000 次 `RegisterSender` + 4 个读 goroutine 循环 `Resolver`，`close(start)` 做发令枪，`stop` 收尾，cleanup 持锁清理 map）。**变异 M28-M7**：去掉锁后 `-race` 必红。**关键细节（否则是假绿）**：读者**必须走 `Resolver`** —— `NewSender` 根本不读这张 map，用它当读者时无锁也照样绿。
- [ ] **G-40 通知渠道凭据静态明文落库 + 允许 `http://`（无 scheme 校验）**（2026-09-09 新增，G-33 M3 安全审计 INFO-4，未修，既有设计）— `notification_channels.config` 是明文 JSON（`smtp_password` / `sign_secret` / 企微 URL 里的 `key`），DB 泄露即凭据泄露（进备份、进只读 DB 账号视野）；`NewWeChatSender`/`NewWebhookSender` 不校验 scheme，管理员配 `http://` 时凭据明文过网（钉钉/webhook 同形，非 M3 引入）。修法方向：① 配置列改密文（需先定密钥管理，KMS/环境变量派生）；② 保存时拒绝 `http://`（`https` 白名单）或仅对环回地址放行；两者都要过一轮需求文档 + 审计，不在 M3 轮动。
- [ ] **G-41 `zabbix_truncated` 是跨语言裸字符串约定，改名会静默失效**（2026-09-11 新增，M27/B 收尾自查发现，低危）— 后端 `integration_handler.go` 的 `results = map[string]int{"zabbix": count, "zabbix_truncated": truncated}` 与前端 `Settings.tsx` 的 `res?.data?.data?.synced?.zabbix_truncated ?? 0` 各写一遍字面量，中间**没有任何契约**（既不在 openapi 里，也没有 TS 类型）。改名/拼错 → 前端读不到 → `?? 0` 兜底 → **永远显示「未截断」**，正好是 B 要消除的那类静默（且比 B 修之前更隐蔽：日志里仍有「超过上限」，只有 UI 静默）。同位置的 `glpi_skipped` 有同样性质。**可选路**：① 把两个 key 提成双侧引用的常量（Go 侧 const + 前端 const，仍然各一份）；② 走 openapi/`gen:api` 让类型系统兜住（M27 刻意没动 openapi —— 该响应是 `map[string]int` 自由形态，要收口得先改响应结构，属独立改动）；③ 只登记不改（当前）。**本轮取 ③**：改名是低频事件、且 B 的收益不依赖它。
- [ ] **G-42 API Key 白名单接受「过宽 CIDR」（含 `0.0.0.0/0` / `::/0`）且写入侧无任何守卫**（2026-09-11 新增，M28/A 审计连带发现，**本轮刻意不做**）— A 项把鉴权侧改成按网段匹配后，`0.0.0.0/0` 这类条目**才真正开始生效**（此前字符串精确比较下它们是永不命中的死条目）——等于把一把万能钥匙从「摆设」变成「真能开门」，而写入侧 `handlers/api_key_handler.go` 的 `validateIPWhitelist` 只校验格式、不校验前缀宽度，也没有任何告警。**对照（同一个概念、两处严格度不一致）**：`config.validateTrustedProxies`（`internal/config/config.go:260-330`）对 `server.trusted_proxies` 明确拒绝过宽前缀（v4 < `/8`、v6 < `/16`）、拒绝 `::ffff:0:0/96`（等价 v4 `/0`）、拒绝非零主机位。**为何本轮不做**：在写入侧拒绝 `/0` 会把运维锁死——存量条目一旦存在，连「改成合法值再保存」都会被自己的校验挡住（得先删整条白名单），是可用性回退且超出 G-11 范围；正确做法是**保存时告警（前端二次确认 / 后端返回 warning）而非拒绝**，需要产品口径 + 独立一轮。已在 `CHANGELOG.md` 的行为突变告知里提示运维复核存量 `ip_whitelist`。
- [x] **G-43 `apierr.Respond` 的 5xx 日志行裸拼 `URL.Path` → 可用 `%0d%0a` 伪造日志行**（2026-09-11 新增，M28/F 实测发现，未修，CWE-117 日志注入）— `internal/apierr/apierr.go:41` 用字符串拼接写 `"[ERR] " + method + " " + URL.Path + " code=…"`。Go 的 `net/http` 读完请求行后按 `url.ParseRequestURI` **解码**转义，`%0d%0a` 会变成真实 CR/LF 落进 `URL.Path`。**实测（真 socket 打 raw 请求行，不是推演）**：`GET /api/assets/abc%0d%0a[ERR]%20FORGED HTTP/1.1` 命中 `/api/assets/:id` → handler 触发 5xx 后，日志实际输出**两行**：
  ```
  [ERR] GET /api/assets/abc
  [ERR] FORGED code=internal_error internal=boom
  ```
  第二行与一条**真实**错误行无从区分（可伪造来源、或把真错误埋进噪声里）。**触发前提**：已认证身份 + 一条会走 5xx 的路由 + `%0d%0a` 落在**路径段内**——gin 按**解码后**的 path 匹配，所以 `/api/assets%0d%0aX` 会直接 404、**到不了** handler，必须塞进 `:param` 段。**三个同形不同命的对照（别一起改错）**：① `middleware/audit.go:97` 的 `Path: c.Request.URL.Path` 把同样原文**落库**（`audit_logs.path`），是存储型同源问题（导出/渲染时伪造行）；② `middleware/audit.go:54` 的同名取值只用于 `SkipPaths` 查表，**无影响**；③ **`gin.Logger()` 反而安全** —— 它用 `%#v` 输出，CR/LF 被转义成字面量（实测日志为 `GET "/api/assets\r\nFORGED LINE"`）。**修法**：`apierr` 加 `logSafe()`（剥 `
`/`
` 及控制字符）或该处改 `%q`；audit 落库侧同理。**注意脱敏 ≠ 转义**：现有 `redact.Text` 完全不处理 CR/LF。 **已交付（2026-09-12，M29-B，`docs/FIX-PLAN-LOG-INJECTION.md` §2.2 + §8）**：`apierr.Respond` 的 5xx 行改为整行 `redact.StripControl(...)`（method/path/code 全不可信）后再补回换行；内层先 `StripControl` 再 `Text`（顺序是安全边界，见 T-55）。守门用例走**真 socket 打 raw 请求行**（`TestRespond_5xx_路径含CRLF不伪造日志行`），变异 M29-MB 红在断言上。同批把 §5.4 里同形态的第二处（`middleware/audit.go` 的 `Path` 入列）也一并收口 → G-44。
- [x] **G-44 审计字段无截断 / 错尺子 → 审计行静默丢失，以及 `markFailed` 脱敏顺序反了（真泄漏）**（2026-09-11 新增，M29 普查发现，**2026-09-12 M29-C/F 已交付**，见 `docs/FIX-PLAN-LOG-INJECTION.md` §2.3/§2.6 + §8）— 两个缺陷同源：出口缺净化。① `middleware/audit.go` 的 `Path`/`UserAgent`/`RequestID`/`Username`（+ 不可达的 `ErrorMsg`）**要么完全不截断、要么按 byte 截断**，而列宽是 `varchar(500/500/50/100/1000)`：`RequestID` 列宽 50 却零截断，且 `AuditLog` 挂在**未认证**的 `routes.go:227`（`POST /api/auth/login`）→ 任意人发个超长 `X-Request-ID` 就让登录审计行消失（22001 → 整行 INSERT 被拒），审计链静默断档；`Path` 按 byte 截断则把 `"中"*200` 切成半个汉字 → 22021，同样丢行（T-48：sqlite 不复现，只能真 PG 测）。② `notification.markFailed` 顺序是 **`redact.Text` → `stripControlChars`**（反的）：`password=abc\nDEF` 被 Text 遮成 `password=***\nDEF`（值类止于 `\n`），Strip 再删掉 `\n`，等于把**未遮盖的尾部接回**一个已被认成凭据的串 = `password=***DEF` —— **真泄漏**，而同包 `sanitizeSnippet` 一直是对的（同一份判据两种写法）。**交付**：新增 `redact.StripControl`（M29-A）作为唯一实现，`notification.stripControlChars` 与 `handlers.sanitizeAuditUsername` 改为委托；audit 新增 `sanitizeField`/`truncateRunes`（按 **rune** 截断，口径同 `varchar(n)` 数的是字符）；`markFailed` 调序为 Strip → Text → ToValidUTF8 → 按 rune 截断；`worker.go:147/154/162` 三处日志出口补 `stripControlChars`。**真 PG 实测**（`scripts/db_smoke.sh` 新增 `TestDBSmoke_AuditFieldTruncation`）：修前 22001 丢行（把 `sanitizeField` 还原即复现 `ERROR: value too long for type character varying(500) (SQLSTATE 22001)`），修后落库 `path=500 runes / request_id=50 runes` 且两值均为合法 UTF-8。变异 M29-MA/MB/MC1/MC2/MD/ME/MF/MF2/MG **全红在断言上**（T-31：首版 B/C/D/E 四条变异因删掉 `redact.` 调用后 import 未使用而**红在编译上**，改成可编译变异后重跑；MF2 首轮**存活** → 补 `TestHandleAlertEvent_无收件人日志不伪造行`，见下条）。
- [x] **G-45 同步路径把第三方文本写进有长度约束的列 → 整批回滚**（2026-09-11 新增，M29 §1.7 普查发现，**2026-09-12 M33 交付**，见 `docs/FIX-PLAN-TRUNCATION.md` / `docs/IMPL-TRUNCATION.md`）— **【原登记（保留）】**「`internal/integration/` 对第三方字段**零截断**：Zabbix/GLPI/NetBox 的 `name`/`description`/`host` 等第三方可控字符串直接进 `CreateInBatches`，任何一条超 `varchar(n)` 就撞 22001，而 GORM 批插在同一事务里 → **整批回滚**，外在表现是「同步报成功但一条都没进来」；与 G-44 同族，修法是「在每个集成字段的映射处定义按字符截断 + 截断计数透出（同 M27-B 口径）」。」 **【结案·连带更正（逐条实测）】** ① 「报成功」**证伪**：三条手动路径（NetBox / Zabbix 告警 / GLPI）走 `apierr.Internal`（`integration_handler.go:84`）→ **HTTP 500**，原始 err 只进日志，响应里**不说是哪条、哪个字段**；② 「后台无人发现」**只对第四条路径成立**：`cmd/server/main.go:58` 起 `MetricSyncWorker`（`Tick: 5*time.Minute`）调 `SyncMetricsFromZabbix`，失败只有一行 `log.Printf` 且**无 HTTP 面、无前端入口** → 坏行从未落库 ⇒ 下一轮再被选中 ⇒ **永久卡死**；③ **TEXT 列不是安全区**：PG 照样拒 NUL（`22021`，字面量与 bind 参数都拒），而 `alerts.problem` / `tickets.description` 与有界列同处一条 `CreateInBatches` 事务 → 一条 NUL 同样整批回滚（**只挡长度挡不住这条**）。 **【修法（D-1..D-10）】** ① **D-1** 按 **rune** 截断（PG `varchar(n)` 数的是**字符**；按 byte 切会切出非法 UTF-8 → `22021`，等于把「超长」换成「编码非法」）→ `redact.TruncateRunes(s, max) (string, bool)`（导出；`max<=0` 原样返回 —— 列宽常量写错时宁可交给 DB 报错，也不静默把字段抹成空串；`len(s)<=max` 快路）；② **D-2/D-3** 分界线是**「是否第三方来源」**而非「列是否有界」：第三方字符串**一律** `StripControl`，**只有有界列**额外截断（否则 TEXT 的 NUL 漏网）；③ **D-4** 截断点选在同步循环**构造 model 字面量处**，`ConvertToAsset`/`ConvertToAlert`/`ConvertToTicket` 签名一字未动；④ **D-5** 计数键用 `<path>_field_truncations`（复数名词 = 计数），刻意避开既有 `zabbix_truncated`（源侧条数上限的 **0/1 标志**）；⑤ **D-6** 截断、**不跳过整行**（F15 实测 9 个字段与全部唯一索引零交集）；⑥ **D-7** 不改 schema（列宽是契约声明的形状，改宽只是把问题推后）；⑦ **D-8** 不动 `metric_sync` 的分批提交语义（前批已提交是有意的资源保护）；⑧ **D-9** 工具放 `redact` 并导出（既有两份私有 `truncateRunes` **不迁移** —— 重构没坏的东西属越界）；⑨ **D-10** 计数**函数局部 + 返回值透出**，禁落 `IntegrationService` 字段/包级（该 struct 是长生命周期共享对象，HTTP handler 与 worker goroutine 并发调用，而 `go test ./...` 不带 `-race`）。 **【落地实况】** D-1..D-5 已落：`redact.TruncateRunes` + `internal/integration/truncate.go`（9 个列宽常量 + `sanitizeText`（TEXT 列只剥不截，纯函数）+ `fieldCounter`（`truncate`/`text`/`count`/`String`，**截断与剥离分开记账**：截断进 API 计数、剥离只进日志明细）+ `logFieldSanitization`）、四条路径接入（NetBox `name`/`brand`/`model`/`sn`/`site_name`；Zabbix `trigger_name`/`host_name` 截断 + `problem` 只剥；GLPI `title` 截断 + `description` 只剥；指标 `key`）、`SyncAll` 与 handler 按 `type` 透出新键、worker 接住原本被 `_` 丢弃的计数；**D-6（前端 `Settings.tsx` 文案）/ D-7（G-55 的 `audit_logs.resource` 100→50）/ D-8（`openapi.yaml` 键清单 + `gen:api`）本轮未落地** → 新计数目前**只在响应体与后端日志可见**（前端仍不可见），契约侧仍不描述新键（→ G-55 / G-57）。 **【回归网】** `TestTruncateRunes`（U1：**管线形态** `TruncateRunes(StripControl(src), max)`，断言输出合法 UTF-8 + rune 数**精确等于** `min(src, max)`（中文夹具是唯一能抓 byte 切法的断言）+ 裸函数对非法字节走快路这一契约）；`truncate_test.go` 的 U2（剥控制字符，夹具含 `\x00`/`\x7f`/`\n`/`\r`/`\t`）、U5（未超长不计数、值**逐字未变**，含「正好等于上限」边界）、U7a（**反射**比对 9 个常量 == 模型 `gorm:"size:N"` tag，解析不到即失败）、日志守卫四态（有截断 / 只有剥离 / 都没有）；5 个测试文件的**签名连带**按编译器点出的位置逐个修（`upsert_test` ×21、`metric_sync_test` ×6、`zabbix_truncate_test` ×3、`zabbix_identity_test` ×4、`db_smoke_test` ×4 —— 最后一组在 `adeab55` 补）。 **【变异反证（T-31 可编译形态）】** M1（`redact.TruncateRunes` 改 byte 切法）红在 U1 的 **rune 计数**（中文夹具 255 vs 85）；M7b（`colAlertTriggerName = 400`，常量偏小）红在 U7a。 **【未落地 / 抓不到】** 真 PG 侧 U3/U4/U6/U7b/U8 与 `scripts/db_smoke.sh` 白名单（T-42）**未加** → 「落库被拒 → 截断后成功」目前**没有真 PG 守门**（sqlite 不强制 `VARCHAR(n)`，基座那几列干脆是 `TEXT` → 修前就是绿的，见 **T-61**）；M11（worker 计数日志）代码已实现但**无断言**（改回 `if _, _, err :=` 不会红）。 **【已知残留（接受，均非静默）】** `assets.name` 参与 `metric_sync` 的 `Where("name IN ?")` 关联查询 —— 截断后 Zabbix host 匹配不上，表现为 skipped + 计数 + 日志；`metric_snapshots.key` 截断可能把两个仅在 100 字符后不同的 key 合并成一条序列（该表**无**对应唯一索引 → 不报错，但截断计数仍 >0，可观测）；`\t`/`\n`/`\r` 的剥离是**策略性**保真损失（与审计口径一致，残余登记 G-57）。
- [ ] **G-46 结构化值（JSON 数组/对象）内的元素明文**（2026-09-12 新增，M30 §1.1 L13 / §6 残余表，**登记不修**）— `{"password":["SECRET"]}` → `{"password":***"SECRET"]}`、`{"password":{"a":"SECRET"}}` → 元素值明文。根因与 G-34 同族（值类的边界），但修法**不同**：规则 3 吃掉 `[`/`{` 之后需要**嵌套匹配**（数组/对象语法），正则做不到，属 §6 第 1 条「不换状态机」的范围。可达性：日志里打结构化 config（`notification_channels.config` 是明文 JSON）时才会出现；当前 `Text` 的调用面（日志 / `error_msg` / `httpx` 出口）拿到的是**错误文本**而非 JSON 原文，故定为**低危登记**。已用 `TestText_已知残余_钉住` 钉住当前输出（修它这里会红，从而必须同步本条与测试）。
- [ ] **G-47 Swagger UI 与 `/openapi.yaml` 匿名可达且无开关**（2026-09-12 新增，M31 §1.7 实测，**登记不修**）— `RegisterSwagger(r)` 挂在根路由（`routes.go:459`），`/openapi.yaml` 是 `//go:embed` 出来的静态产物（`swagger.go:30`），**两者都没有任何开关**：不随 `env` / `log.level` 变化，release 下同样可达。后果是 API 的**形状**（全部路径、参数、schema、枚举）对任何能连到该端口的人公开。定级依据：形状本身通常不算机密（OpenAPI 的常规用法），真正的风险面是**部署时把 8080 直接映射到 `0.0.0.0`** —— 那会同时绕过 nginx 的 TLS/安全头/限流。**本轮处置**：不改代码，改为在 `08-部署运维.md` §8.4.5 写死「不得直接暴露后端端口」+ 排查命令（`ss -tlnp | grep 8080`），把控制点放在网络层。要产品化需加配置开关 + 反代规则，属独立一轮。
- [ ] **G-48 `Asset.status` 两套词表并存（`inactive` vs `offline`/`retired`）**（2026-09-12 新增，M31 步 3 补契约时发现，**登记不修**）— 与 M16 priority 同型。契约 `openapi.yaml` 的 `Asset.status` enum 是 `[active, inactive, maintenance]`；`models/asset.go` 的注释写 `active, offline, maintenance, retired`；`service/asset_service.go:221` 的退役路径实际写 `'retired'`；前端 `types/index.ts:31` 镜像了契约那套（含 `inactive`），而 `AssetFormModal.tsx:15` 提供 `inactive`、`AssetTable.tsx`/`StatusTag.test.tsx` 用 `offline`/`retired`。**即：退役接口写进去的值，契约说它不合法、前端筛选也认不出**。修法要拍板权威词表（同 M16 的 (a) 以契约为准：迁移归一并改写入方），属独立一轮的需求 + 迁移，本轮只在 YAML description 里如实标注 `retired` 不在 enum 内。
- [x] **G-49 `/assets/export` 静默截断（固定前 500 条、丢弃总数、不分页）**（2026-09-12 M31 步 3 补契约时发现，**2026-09-12 M32 交付**，见 `docs/FIX-PLAN-EXPORT-FIDELITY.md` / `docs/IMPL-EXPORT-FIDELITY.md`）— **【结案】** 原状：`ExportAssets` 固定用 `AssetFilter{Page: 1, PageSize: 500}` 且**丢弃返回的 total**，资产超过 500 台时导出的 CSV **不完整且没有任何提示**，运维拿它做盘点/对账会得出错误结论（与 M27-B「截断必须透出计数」同族）。**修法（D-1..D-10）**：① 导出改走 service 新增的**专用全量路径 `ListAll`**（`List` 及其 `pageSize > 500 → 500` 硬顶**一字未动** —— 那是交互式分页的防拉爆闸，与导出语义不同）；② 新增响应头 `X-Total-Count = len(items)`（不另发 `COUNT(*)`，消除 COUNT/SELECT 竞态）+ 显式 `Content-Length`，两者供调用方自校验完整性；③ CSV 改为**先写满 `bytes.Buffer` → `Flush` → 查 `w.Error()` → `c.Data` 一次写出**（原实现边写边发且 `defer w.Flush()` 把错误全丢 → 取数/序列化中途失败会产出「半截 CSV + 已 200」，本身就是静默不完整）；④ 排序改 `created_at DESC, id DESC`（`id` 兜底让同刻批量导入的产物可 diff）；⑤ 该端点单独挂 `RateLimit(10/min)`（去掉上限后单请求成本 O(N)，组级 100/min 允许瞬时 100 个全表读）；⑥ CORS `Expose-Headers` 补 `X-Total-Count`。**显式决策**：不设行数上限（本端点无过滤参数，任何上限都只是把 500 换成另一个静默截断点；触发条件与资源策略见 G-51）、不加 capability 门禁（导出仍属读地板）、不扩 CSV 列（见 G-50）。**回归网**：新增 6 条集成用例（1001 行全量 / 501 与 500 边界对照 / JSON 分支同全量 / 空表 / `List` 的 500 硬顶保留反向钉子）+ 1 条 handler 错误路径用例（500 不带 CSV 头），夹具前三条 name 含逗号/换行/公式前缀以覆盖 `csv.Writer` 转义与 `safeCSV`。**变异反证 8/8 全红**（含 M3「`ListAll` 加 `.Limit(1000)`」—— 有限上限而非去掉，用例夹具取 1001 正是为抓它）。**连带更正**：`docs/FIX-PLAN-AUTHZ.md:155` 把「导出限 500 行」当安全属性引用，该子句随本轮失效（`sanitizeFilename` 半句仍为真，属 `postmortem` report 端点）；`routes_integration_test.go` 公开端点表的理由串同步。
- [ ] **G-50 导出 CSV 列不保真（固定 4 列，实体约 35 字段）**（2026-09-12 新增，M32 §3.5 登记，**登记不修**）— `/assets/export` 的 CSV 只有 `ID,Name,Type,Status` 四列，而 `models.Asset` 有约 35 个字段（`asset_tag`/`sn`/`brand`/`vendor`/`warranty_end`/`business_unit`/`retired_*` 等全不在内）。对账场景真正要的往往是 `asset_tag` 与质保到期，列不全同样会得出错误结论 —— 与 G-49 同族（都是「产物不保真」），但修法不同：需要拍板**导出列集**（全字段？可选列？跟列表页可见列？）并加 `?columns=` 之类的参数，属产品语义决策。**本轮显式不做**（D-9）：扩列会让每个新列都必须过 `safeCSV`（否则重新引入 DDE 面），且列集一变所有下游脚本的列序假设全废 —— 需要单独一轮的需求 + 下游影响评估。**约束（现在就生效）**：将来任何新增导出列**必须**过 `safeCSV`。
- [ ] **G-51 导出资源策略：false-positives 无界而 assets 已收紧，两端不一致**（2026-09-12 新增，M32 D-7/D-8 登记，**登记不修**）— 本轮把 `/assets/export` 从「限 500 行」放开到全量后，它的成本从 O(500) 变成 O(N)，于是**同一个仓库里两个导出端点的资源姿态相反**：`/assets/export` 现在无上限 + 独立限流 10/min，而 `/alerts/false-positives/export`（`alert_handler.go:403` → `alert_service.go:595-607`）**连 `Limit` 都没有**、直接 `Find` 全表，且只有组级 100/min。**决策记录**：本轮不给 assets 设上限（无过滤参数 → 上限只是把 500 换成另一个静默截断点，D-8），改以限流兜住并发；真正的**资源策略**（单次导出最大行数 / 超限转异步任务 / 导出配额）需要产品拍板，且必须**两个端点一起定**，否则又是新的不一致。**触发条件（D-8 明文）**：若生产 `assets` 行数达到万级、或导出端点出现超时/连接池告警，则升级为「异步导出任务 + 下载凭据」，届时 G-50 的列集也一并定。
- [ ] **G-52 导出端点的能力门禁与审计内容**（2026-09-12 新增，M32 D-6 登记，**登记不修**）— 两件事同族，都是「导出端点的安全姿态」：① **能力门禁** —— `/assets/export` 只要求认证（读地板），任何 `readonly` 账号都能一次拉走全量资产；数据量越大，「可读」与「可批量取走」的差别越实质。加门禁（如 `RequireCapability("export")`）属权限模型变更，要同步改角色矩阵、前端可见性、契约，故本轮**不加**（需求 §3.4 已论证：导出不返回任何列表页拿不到的行，**不构成提权**，只是省事）。② **审计内容** —— 导出**已有**审计（`AuditLog` 是 `protected` 的组级中间件，`routes.go:243`，豁免表 `middleware/audit.go:29-37` 不含它；M32 审查时曾误判为「无审计」，已在 `docs/IMPL-EXPORT-FIDELITY.md` §10 更正），真正缺的是**审计行不含导出条数与格式** —— 事后追查「谁在什么时候拉走了多少数据」时只有路径没有量。加字段要动审计 schema + 前端审计页展示，属独立一轮。
- [ ] **G-53 列宽是「三处副本」：迁移 DDL / 模型 `gorm:"size:N"` tag / `internal/integration/truncate.go` 常量 —— 改一处必漂**（2026-09-12 新增，M33 实现时登记，**登记不修（守卫待补齐）**）— 权威来源只有**迁移 DDL**；`truncate.go` 的 9 个 `col*` 常量与 `models` 的 `size:` tag 都是**冗余副本**。G-55 就是「第三处常量（`audit` 的 100）大于真实列宽（50）」漂了几轮没人发现的实例。**本轮已加 U7a**（纯 `go test`：反射取 `schema.Parse` 的 `Field.Size` 比对常量，解析不到即失败）；**U7b（常量 == `information_schema.columns.character_maximum_length`，真 PG）尚未落地** —— 即「常量 vs 真实列宽」这条腿目前**没有守门**，而它才是 M7（常量偏大）/ M7b（常量偏小）两条变异的直接判据（纯 `go test` 结构上抓不到常量与迁移的漂移）。**约束（现在就生效）**：任何改列宽的迁移必须同批改三处；新增第三方字段进常量表时同步进 U7a 清单。
- [ ] **G-54 `fieldCounter` 是本轮新引入的聚合器，与 M27-B 的「裸 int 计数器」路线分叉**（2026-09-12 新增，M33 §2 登记，**登记不修（有意分叉，非顺手造的抽象）**）— 本包既有先例全是裸计数器（`service.go` 的 `truncated`/`skipped`、`metric_sync.go` 的 `skipped`/`written`），`fieldCounter` 是全仓**第一个** `func (c *fieldCounter) String() string`（聚合器形态此前零命中）。分叉理由：需求要求日志含**逐字段明细**（`trigger_name×3`），且 **R3-b 要求「只剥不截」也必须可见**（`problem(stripped)×1`）—— 裸 int 装不下第二种明细，而丢一个 bool 就是**静默**，正是这一轮要防的失败模式。**约束**：必须**函数局部**（D-10），禁落 `IntegrationService` 字段/包级变量（该 struct 由 HTTP handler 与 worker goroutine 并发调用；`go test ./...` 不带 `-race`）。将来若要统一计数器风格（含 G-53 的常量表），这里是落点。
- [ ] **G-55 `audit_logs.resource` 的截断常量 100 > 真实列宽 50 —— 截了仍 22001，审计整行丢失**（2026-09-12 新增，M33 F16 发现，**登记不修，远期 target = 三处对齐 50**）— 迁移 `000001_init.up.sql:1097` 是 `resource VARCHAR(50)`（`:64` 那处是 `permissions` 表，别改错），而 `middleware/audit.go:164` 按 **100** 截、`models/user.go:106` 的 tag 也是 `size:100` —— 与 G-45 **同一机制**（**常量 > 列宽**）：截到 100 仍可能超 50 → `22001` → 整行 INSERT 被拒 → 审计链少一行。取值是**路由首个静态段**（短、非攻击者可控）→ 潜伏而非活缺陷。**状态**：迁移已是 50；**M33 的 D-7 未落地**（model tag 与截断常量都还是 100）。远期 target：`audit.go:164` → 50、`user.go:106` → `size:50`、把 `resource` 纳入 **U7b 的列宽校验清单**（否则下次再漂还是没人知道），并在真 PG 冒烟里补一条「首个静态段 > 50 字符的路由，请求后审计行**仍落库**」的断言（`TestDBSmoke_AuditFieldTruncation` 已在 `scripts/db_smoke.sh` 白名单内，**不需**改白名单）。
- [ ] **G-56 `SyncAll` 只透出三个 `*_field_truncations` 键 —— 四个键永不同时存在**（2026-09-12 新增，M33 审查 B-6 登记，**登记不修（文档/契约口径）**）— `SyncAll`（`service.go`）只调 `SyncFromNetBox`/`SyncFromZabbix`/`SyncFromGLPI`，**不调指标路径**（`SyncMetricsFromZabbix` 的调用点全集 = `integration_handler.go:71` 的 `type=zabbix_metrics` 与 `metric_sync.go:110` 的 worker）→ `zabbix_metrics_field_truncations` **只在单路径请求里出现**。因此契约、前端文案与下游脚本都必须按**「键随 `type` 而变」**表述，**不得**写成「同一次响应里有 4 个键」（读者会以为 `type=all` 漏了指标键）。失败分支**不写键**（与 `glpi_skipped` 同形，前端 `?? 0` 兜住）—— 这也意味着「读不到」与「没截断」在响应体里**同形**。**U8（`SyncAll` 层断言失败分支不写键）本轮未落地**，且它**不能**走 HTTP：失败时 `apierr.Internal` 不写 `data.synced`，HTTP 面观测不到。
- [ ] **G-57 `synced` 是自由 map：键名是跨语言裸字符串约定，类型系统兜不住**（2026-09-12 新增，M33 审查 B-6 登记，**登记不修**）— `openapi.yaml` 的 `synced` 是 `additionalProperties: {type: integer}`（**结构本轮刻意不动**），键清单只在 `SyncResult.description` 里**手写枚举**（现只列 `zabbix_truncated`/`glpi_skipped`）；Go 侧 `map[string]int` 字面量、前端 `res?.data?.data?.synced?.xxx ?? 0` 各写一遍 —— 与 **G-41 同族**：改名/拼错**不会有任何编译或类型错误**，前端 `?? 0` 兜底成「未截断」，比修之前更隐蔽。**M33 的 D-8 未落地**（description 未补 4 个新键与「标志 vs 计数」的区分，`frontend/src/services/api.types.ts` 未重生成）→ 契约侧目前仍不描述新键。**下一轮要求**：补 description（含「键随 `type` 而变」，见 G-56）+ `npm run gen:api`（生成物**禁手改**；CI 有漂移硬门禁 `.github/workflows/ci.yml:197-198`）。

- [x] **M16 工单优先级词表归一（normal vs medium）**（2026-09-10 新增并交付，见 `docs/FIX-PLAN-M16-PRIORITY.md`）— `tickets.priority` 用两套词表示「普通」：契约（openapi `Ticket.priority` enum）与手工建单表单用 `normal`，GLPI 同步与告警一键建单用 `medium`；该列无 CHECK 约束，两套值都能落库，后果是工单页按「普通」筛选查不到告警派生出来的票。**方案 (a)（用户拍板）：以契约为准** —— 迁移 `000023` 把存量 `medium` 归一为 `normal`（down 显式声明不可逆）；三个写入方（`integration/glpi.go` 映射 / `priorityFromSeverity` / `cmd/seed/main.go` 字面量）同批改 `normal`；`models/ticket.go` 注释同步；`TicketService.Create` 补 `priority=''` 兜底（`POST /tickets` 不传 priority 原会落空串，是**活路径**而非历史脏数据）。**回归网**：两个映射函数「输出 ∈ 契约词表」断言（GLPI 侧此前零覆盖）、dbsmoke 升级路径三条断言（存量归一 / high 对照行不动 / 全表无词表外值）、`TestDBSmoke_DownPreservesLegacyColumns` 补十次 Down + 首尾正向断言（链上全是 `assert.False`，多滚一层也能全绿）。**附带**：`migrate.Load()` 同号迁移由静默覆盖改为报错（T-41，up/down 两侧都守）。9 条变异反证全红在业务断言上。**残余（另立任务）**：`POST/PUT /tickets` 的 priority **取值**校验 —— `PUT` 收任意 `map` 直落 `Updates()`，mass-assignment 面比 priority 一个字段宽得多，单封它会造成「已封住」的错觉；前端两处同义词字典按设计保留一个版本做未迁移行的安全网。
- [x] **M26 同步导入保真：导入路径不发明时间、不静默丢票**（2026-09-11 新增并交付，见 `docs/FIX-PLAN-SYNC-FIDELITY.md` 需求 / `docs/IMPL-SYNC-FIDELITY.md` 实现与复审）— **原状三条缺陷**：① Zabbix 同步**从不写 `problem_start`**（`ConvertToAlert` 出来的值被丢掉），告警列表照样有数据（列表不渲染该列），但所有基于 `problem_start` 的 KPI/SLA 窗口恒空，界面上看不出来；② GLPI 同步把 `created_at`/`resolved_at`/`closed_at` 一律写成**同步时刻 `now`** —— 伪造事件：`diagnostic_service` 只看指针非空、不看 status，会凭空长出一条「工单已解决/已关闭」的时间线，并污染 dashboard 的 SLA 窗口；③ GLPI `statusMap`/`priorityMap` 缺键（status=6「待批准」、priority=0「未指定」）→ 落空串 → 配合新加的越界检查**整类票静默不入库**。**拍板 D-1..D-9**：D-1 越界档位跳过+计数+留日志（不兜底、不整批失败）；D-2 源缺失 → **留 NULL，不发明**；D-3 GLPI 时区 Asia/Shanghai；D-4 `ON CONFLICT` 配 `TargetWhere`；D-5 迁移 fail-closed 带前置自检；D-6 `SyncFromGLPI` 返回 `(synced, skipped, err)` + 新 `glpi_skipped` 键；D-7 不做 host_id/asset_id 关联；D-8 Zabbix `created_at` 保持 `now`；D-9 `AssignTicketNumbers` 改按「当天已占用标签集合」分配并跳过空洞。**交付**：① 新增 `integration/timeparse.go` —— tri-state（`timeAbsent`/`timeInvalid`/`timeOK`）纯函数 `parseUnixSeconds`/`parseGLPITime`，区分「源说没有这个时间」与「给了但解析不了」，`glpiLoc` 失败即 panic（不静默降级 UTC）；② `SyncFromZabbix` 写 `ProblemStart ← lastchange`；③ `SyncFromGLPI` 重写（时间戳按 D-2 + 越界跳过 + 事务内 COUNT 前后差计数 + `ON CONFLICT (external_id) WHERE …`）；④ `migrations/000026_tickets_glpi_external_id_unique.{up,down}.sql` —— 部分唯一索引（`WHERE source='glpi' AND external_id <> ''`）+ DO 前置自检（把「无 DETAIL 的 23505」变成带重复 ID 样本的异常）；⑤ `AssignTicketNumbers` 间隙容忍 + 逃生门占位；⑥ handler 透出 `glpi_skipped`，前端 Settings 同步完成提示带上它（D-6 的可见性落到运维真正看的位置）。**测试**：新增/改写 18 条（`timeparse_test.go` 5 条 + `upsert_test.go` 6 条 + `glpi_e2e_test.go` 词表 per-key 对照改为**运行时解析 `openapi.yaml`** + `hooks_test.go` 2 条 + `db_smoke_test.go` 3 条真 PG）。**变异反证 9/9 全红**：步骤 4 的 6 项（含 M5「`synced` 改用 `res.RowsAffected`」→ 虚报 2、M6「去掉 `TargetWhere`」→ 部分索引仲裁失效）均在 sqlite 基座红；步骤 5 的 3 项在**真 PG** 红（M-T16 索引谓词写宽 → 人工票被一起管住、M-T17 去掉 DO 自检 → 异常文本不含样本、**M-T18 去掉 `.UTC()` → 落库 `02:00:00` 变 `10:00:00`**）。**门禁**：`gofmt`/`go vet ./...`/`go build ./...`/`go test ./... -count=1` 全绿；`scripts/db_smoke.sh` **两条路径全绿**（新增 `TestDBSmoke_TicketsGLPIExternalIDUnique` / `_GLPITimeZoneWallClock` / `_Migration026BlockedByDuplicates`，Down 链由十二次改十三次并逐行核过）；前端 `tsc`/`eslint`/`vitest 327 passed`。**踩到并记录的新 trap**：`docs/TRAPS.md` **T-48**（sqlite 与 pgx 对 `time.Time` 的处理**相反** —— 任何 sqlite 用例结构上不可能测出 `.UTC()` 的有无；且断言里再 `.UTC()` 一次会抹平被测调用）与 **T-49**（gorm `res.RowsAffected` 在带 `RETURNING` 的批量插入上等于 `len(batch)`，不是实际插入数 —— 恰好就在 `ON CONFLICT` 存在的那一格虚报）。**同轮实现期自查发现（三轮细节审查均未捕获）**：`logTimeFallback` 在「留 NULL」的 D-2 分支上打「按回落处理」的日志 = **在日志里撒谎**，会把排查「`closed_at` 为什么是空」的人引向反方向 → 改名 `logTimeUnusable`、文案只陈述事实、处置归调用点。**残留风险（已登记，未修）**：① `Asia/Shanghai` 是**待核对常量**（D-3），GLPI 实际部署时核对 —— 注意 T18 已能在时区名被改错时变红，故这是业务事实核对、不是补测试；② 逐条路径 `generateTicketNumber` 仍用「当天条数法」（D-9 只改了批量路径），原因是 `internal/service/ticket_from_alert_test.go:200-210` 依赖该间隙做失败注入（`docs/IMPL-SYNC-FIDELITY.md` §3.6、§7 F-M1）；③ 并发同步之间不互斥（需求 §1.5 已登记超范围），由 `ticket_number` 唯一索引 + `TicketService.Create` 重试兜底。**范围说明**：本轮**只做导入保真**，不碰更新路径 —— 按 `docs/adr/0004-工单SoT决策.md`（ITmanager 是工单 SoT、GLPI 降级为只读参考）那条是设计而非缺陷。
