# 网络运维监控平台 - 部署运维

> **v3 修订块** · 状态 **draft** · 日期 2026-09-09 · 依据 `docs/v3-架构优化需求.md` · 分支 `docs/v3-optimize`
>
> 本章 v3 定位：部署范围**收敛**——ITmanager 只部署「人的操作台」所需组件；**NetBox / Zabbix 是外部系统**（ITmanager 只读对接），**GLPI 未部署**（工单 SoT = ITmanager 自身，降级为可选只读参考），**Keep v3 明确不部署**（见 `04-告警通知.md` §4.2）。
> 实际部署清单见 §8.2.1；docker-compose 中 GLPI 等辅助系统默认不启用（`profiles: ["aux"]`，§8.3.1）。

## 八、部署架构

### 8.1 部署方案选型

#### 8.1.1 推荐方案：Docker Compose

对于小规模系统（3机房、1000+VM），推荐使用 **Docker Compose** 部署，简单易维护。

| 部署方式 | 适用场景 | 复杂度 |
|----------|----------|--------|
| **Docker Compose** | 小规模、简单 | 低 |
| Kubernetes | 大规模、弹性 | 高 |

### 8.2 架构拓扑

```
┌─────────────────────────────────────────────────────────────────────┐
│                      物理架构（v3）                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                              │
│   ┌─────────────────────────────────────────────────────────┐     │
│   │              负载均衡 (Nginx)                          │     │
│   └────────────────────────┬────────────────────────────────┘     │
│                            │                                       │
│            ┌───────────────┼───────────────┐                     │
│            ▼               ▼               ▼                     │
│   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│   │   API Node1  │  │   API Node2  │  │   API Node3  │       │
│   │   (自建)     │  │   (自建)     │  │   (自建)     │       │
│   └──────────────┘  └──────────────┘  └──────────────┘       │
│            │               │               │                      │
│            └───────────────┼───────────────┘                     │
│                            │ 只读对接                              │
│            ┌───────────────┼───────────────┐                     │
│            ▼               ▼               ▼                     │
│   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│   │   NetBox     │  │   Zabbix     │  │   GLPI       │       │
│   │ 外部·资产SoT │  │ 外部·监控告警│  │ 未部署/可选  │       │
│   │              │  │              │  │ (只读参考)   │       │
│   └──────────────┘  └──────────────┘  └──────────────┘       │
│            │               │               │                      │
│            └───────────────┼───────────────┘                     │
│                            │                                       │
│                            ▼                                       │
│   ┌─────────────────────────────────────────────────────────┐     │
│   │      PostgreSQL (Docker · ITmanager 自建业务库)         │     │
│   └─────────────────────────────────────────────────────────┘     │
│                                                              │
│  注：① 「自建」= ITmanager 本项目组件；② NetBox / Zabbix 为独立系统 │
│  （可同机 Docker 亦可独立部署），ITmanager 只读对接，不重复建设；   │
│  ③ Keep（告警枢纽）v3 不部署；HolmesGPT 以只读 token 调用 ITmanager │
│  API（见 `09-AI辅助.md`）。                                         │
└─────────────────────────────────────────────────────────────────────┘
```

#### 8.2.1 v3 实际部署清单（2026-09-09）

| 组件 | v3 状态 | 三层定位 | 说明 |
|------|---------|----------|------|
| **ITmanager API**（Go） | ✅ **必须部署** | ① 操作台 | 本项目唯一自研服务 |
| **ITmanager Web**（React） | ✅ **必须部署** | ① 操作台 | 人机入口（钉钉小程序 / 扫码见 `11-扩展功能.md`） |
| **PostgreSQL** | ✅ **必须部署** | ① 操作台 | ITmanager 业务库（`audit_logs` 等） |
| **Redis** | ✅ **必须部署** | ① 操作台 | 缓存 / 队列 |
| **Nginx** | ✅ **必须部署** | ① 操作台 | 负载均衡 / 统一入口 |
| **NetBox** | ✅ **必须部署（外部系统）** | ② 资产真值 SoT | 独立系统，ITmanager 只读；专线走 Circuits、VM 走 vCenter sync |
| **Zabbix** | ✅ **必须部署（外部系统）** | 监控告警源 | 独立系统，ITmanager 只读对接 |
| **HolmesGPT** | 🟡 可选（外部系统） | ③ AI 分析层 | 只读调用 ITmanager toolset 端点（R1） |
| **vCenter → NetBox sync** | 🟡 可选（按需） | ② 资产真值 | `bb-Ricardo/netbox-sync`；先 dry-run 一周（R3） |
| **Graylog** | 🟡 **待确认** | 日志检索 | 与既有 Splunk 功能重叠，见 `11-扩展功能.md` §12 |
| **GLPI** | ❌ **不部署**（可选只读参考） | 工单 | v3 工单 SoT = ITmanager 自身（见 `05-运维工单.md`） |
| **Keep** | ❌ **明确不部署** | ③ 告警枢纽 | v3 只划职责边界，见 `04-告警通知.md` §4.2 |

> 「必须部署（外部系统）」表示该组件是 ITmanager 的依赖前提，但**不属于本项目自建范围**；ITmanager 对其一律只读。


#### 8.2.2 TLS 终止与最低版本（PCI DSS 4.2.1）

**要求**：Web 界面（含 `/api/` 与登录）在开放网络上只接受 **TLS 1.2 及以上**；TLS 1.0 / 1.1 一律拒绝。

**终止点**：TLS 在 **Nginx（统一入口）** 终止，容器内的 ITmanager Web 与 Go API 都是明文 HTTP。
因此 Go 后端（`backend/cmd/server/main.go` 的 `ListenAndServe`）**不得**直接暴露到任何不可信网络；
它只应被 Nginx 通过容器网络（`http://api:8080`）访问。

| 项 | 配置 | 说明 |
| --- | --- | --- |
| 协议版本 | `ssl_protocols TLSv1.2 TLSv1.3;` | 硬性下限，PCI DSS 4.2.1 |
| 密码套件 | 仅 ECDHE + AEAD（GCM / ChaCha20-Poly1305） | 前向保密 + 认证加密 |
| 会话票据 | `ssl_session_tickets off;` | 避免票据绕过前向保密 |
| HSTS | `Strict-Transport-Security: max-age=31536000; includeSubDomains` | 回退保护；确认全站 HTTPS 稳定后再考虑 `preload` |
| 明文入口 | `listen 80` → `301 https://$host$request_uri` | 80 不提供任何内容 |
| 证书 | 挂载到 `/etc/nginx/certs/{tls.crt,tls.key}` | 由部署方提供（内部 CA 或 ACME） |

**套用模板**：`frontend/nginx-tls.conf.example`（不是默认配置，需先备好证书）。

```yaml
# docker-compose.yml 的 web 服务
  web:
    volumes:
      - ./frontend/nginx-tls.conf.example:/etc/nginx/conf.d/default.conf:ro
      - ./certs:/etc/nginx/certs:ro
    ports: ["80:80", "443:443"]
```

> `http2 on;` 需 nginx ≥ 1.25.1；更早版本改成 `listen 443 ssl http2;` 并删掉该行。

**自证（部署后必须执行）**：

```bash
scripts/check-tls.sh itmanager.example.com
# 期望输出：TLS 1.0/1.1 已被拒绝、TLS 1.2 可协商 → 退出码 0
```

脚本用 `openssl s_client` **主动发起** TLS 1.0 / 1.1 握手并观察服务端拒绝（curl 只能看最终协商结果，无法降级尝试）。
退出码：`0` 合规 / `1` 不合规 / `2` 无法判定（缺 openssl 或本机缺 legacy provider 发不出弱版本握手——此时**不算通过**）。

**当前缺口**：仓库自带的 `frontend/nginx.conf` 仍是 `listen 80` 明文（模板为 opt-in）。
两个 Dockerfile 已随 G-9 补齐（`backend/Dockerfile` / `frontend/Dockerfile`，见 §8.3.1），
但**镜像内不含 TLS 终结**。生产部署请以本节的模板 + 自证脚本为准，不要依赖 compose 默认值。


#### 8.2.3 受信代理与客户端 IP 的可信边界（G-7）

**问题**：后端取「客户端 IP」用的是 gin 的 `ClientIP()`。gin 默认信任**所有**来源
（`0.0.0.0/0`、`::/0`），于是 `ClientIP()` 会返回 `X-Forwarded-For` 的**最左值**——
这个值由请求方自己填写，**攻击者完全可控**。它同时喂给四个地方：

| 用途 | 被伪造后的后果 |
| --- | --- |
| 登录限流桶键（5 req/min） | 每请求换一个 XFF 即得无限额度，防爆破形同虚设 |
| 审计行 `ip` | 取证字段失真，审计价值归零 |
| `users.last_login_ip` | 同上 |
| **API Key 的 IP 白名单** | 持有效 Key 者可**从任意 IP 使用**，白名单被架空 |

**怎么配**：`server.trusted_proxies` 列出**反向代理本身**的地址（CIDR 或裸 IP，逗号分隔）。

```yaml
server:
  trusted_proxies: ["172.28.0.10"]     # 反代容器 IP
  # 或 LB 网段（多跳时把路径上每个代理都列上）：
  # trusted_proxies: ["10.0.1.0/24", "172.28.0.10"]
```

- 环境变量覆盖：`NMP_SERVER_TRUSTED_PROXIES=172.28.0.10,10.0.1.5`。
- **默认空 = 不信任任何来源**：`ClientIP()` 取直连对端、忽略 XFF。
  直连部署（无代理）下这是**正确值**，不要为了「配全」而填一个宽网段。
- **信任的是代理本身，不是网段**。写 `172.16.0.0/12` 之类等于把同网段所有主机
  （含数据库、其它容器、跳板机）都变成可伪造 XFF 的来源——配了等于没修。
- 语义：从 XFF **最右**往左跳过受信代理，取第一个不可信 IP。
  即「代理追加的真实客户端」，攻击者塞在最左边的伪造值不起作用。
  **前提是代理确实追加了真实客户端地址**（本仓库 nginx 用 `$proxy_add_x_forwarded_for`，
  `frontend/nginx.conf:19-20`）。若换用「原样透传 XFF」的代理，最右项就是客户端可控值，
  必须改用 `X-Real-IP` 之类由代理覆写的头，否则这套语义失效。

**不配的代价**（部署在反代后却留空）：登录限流退化为**全站单桶**——
5 req/min 由所有用户共享，一个攻击者 5 次请求就能让全体用户 5 分钟内无法登录；
审计 IP 全变成代理地址；**配了 IP 白名单的 API Key 一律 403**。
启动时会打 WARN；运行期只要出现「带 XFF 但直连对端不在受信表内」就再打一条 WARN（仅一次）。

> **配错比不配更危险**：非空配置没有启动期 WARN，故障完全静默。所以运行期告警按
> 「对端是否在受信表内」判定，能抓到：填错 IP、多跳漏配、代理换 IP、宿主机反代直连容器。

**校验**：条目非法（`not-a-cidr`、`300.1.1.1`）、**过宽**（IPv4 前缀 < `/8`、IPv6 < `/16`）、
**IPv4-mapped 形式过宽**（`::ffff:0:0/96` 等效 IPv4 `/0`）、**主机位非零**
（`172.28.0.10/24` 会被归一成整个 `/24`）都会在启动期直接失败——
`0.0.0.0/1` + `128.0.0.0/1` 这类写法可以覆盖整个 IPv4，只拦 `/0` 是不够的。
校验前会去掉条目首尾空白，保证「校验所见 = 交给 gin 的值」。

**compose 部署**（`docker-compose.yml` 已内置）：前端容器固定 IP，后端只信任它。

```yaml
services:
  api:
    environment:
      - NMP_SERVER_TRUSTED_PROXIES=172.28.0.10
  web:
    networks:
      default:
        ipv4_address: 172.28.0.10
networks:
  default:
    ipam:
      config:
        - subnet: 172.28.0.0/24
```

> 该网络若已存在且未带 ipam 配置，需先 `docker compose down` 再起。
> 宿主机若已有其它网络占用 `172.28.0.0/24`（`docker network ls` + `docker network inspect`
> 自查），compose 会以「Pool overlaps」直接起不来——换一个冷门网段即可。
> 静态 IP 与 `--scale web=2` **互斥**（第二个副本会因地址冲突起不来）：多副本时改用
> 每副本各自的 IP 逐条列出，或改由前置 LB 承担可信边界。
> compose 自身还缺 Dockerfile 与 env 变量名对齐（见 TODO G-9 / G-10），
> 本节只给「受信代理」这一项的正确值。

**自证**：

```bash
# 1) 发一个伪造 XFF 的请求，再看审计行 IP 是否等于真实客户端（而不是 1.2.3.4）
curl -s -o /dev/null -H 'X-Forwarded-For: 1.2.3.4' https://itmanager.example.com/api/auth/login \
  -H 'Content-Type: application/json' -d '{"username":"probe","password":"x"}'
# 2) 查库：应看到真实客户端 IP
psql -c "SELECT ip, path, created_at FROM audit_logs ORDER BY created_at DESC LIMIT 1;"
```

未配受信代理时，审计行 IP 会是**代理地址**；配了但填错（如填了真实客户端网段）
则仍是伪造值——两种都能用这条命令看出来。


### 8.3 Docker Compose 配置

> **权威文件是仓库根目录的 `docker-compose.yml`**。本节只列要点与约束，不再维护第二份模板——
> 旧版本节的示例是「image 拉取 + 全量发布端口 + 非 `NMP_` 变量名」，与仓库文件已经漂移，
> 照着它部署必然失败，故废弃。

#### 8.3.1 服务清单与 profile

| 服务 | 默认启动 | 宿主端口 | 说明 |
| --- | --- | --- | --- |
| `postgres` | ✅ | `127.0.0.1:5432` | `timescale/timescaledb:latest-pg16`；只绑环回（`make deploy` 的 db-migrate/db-seed 在宿主机上跑）；进库：`docker compose exec postgres psql -U nmp -d network_monitor` |
| `redis` | ✅ | 不发布 | 无密码，发布等于把可写缓存交出去 |
| `api` | ✅ | `127.0.0.1:8080` | 唯一自研服务；**启动时自动执行迁移**（§8.3.4） |
| `web` | ✅ | `127.0.0.1:3000:80` | nginx：静态文件 + `/api/` 反代到 api。**只绑环回**——它提供的是明文 HTTP，对外必须前置 TLS 终结（宿主反代/LB 指向 `127.0.0.1:3000`，或改绑 + 套 §8.2.2 模板） |
| `netbox` `zabbix` `glpi` `graylog` | ❌ | 各自 `127.0.0.1` | 均在 `profiles: ["aux"]` 下；占位凭据、独立库未初始化，需显式 `--profile aux` |
| `elasticsearch` `mongoDB` | ❌ | **不发布** | 仅作为 graylog 的依赖，容器内互访；无需宿主端口 |

#### 8.3.2 启动

```bash
cp .env.example .env && chmod 600 .env
# 每个 secret 用：openssl rand -hex 32
$EDITOR .env
docker compose up -d --build        # 只起主链四件套
docker compose ps                   # 等 api/web healthy（web 有 HEALTHCHECK，见 G-19）
curl -fsS http://127.0.0.1:8080/readyz
```

`web` 默认只绑 `127.0.0.1:3000`（明文 HTTP，不对外）。对外提供必须前置 TLS 1.2+ 终结
（§8.2.2），并在 `.env` 里设 `NMP_SERVER_MODE=release`——否则登录 cookie 不带 `Secure`。

辅助系统（netbox/zabbix/glpi/graylog/...）：`docker compose --profile aux up -d`。
它们目前是占位凭据、库未初始化，**生产化前不要暴露**（TODO G-17）。

#### 8.3.3 环境变量名规则（踩过的坑）

compose 只负责**插值**，变量名必须与 backend 的 viper 键逐字一致：
`NMP_` + 配置路径（`.` → `_`）。例如 `database.password` → `NMP_DATABASE_PASSWORD`、
`integrations.netbox.url` → `NMP_INTEGRATIONS_NETBOX_URL`。
写成 `DATABASE_URL` / `NETBOX_URL` 这类名字**不会报错，只会被静默忽略**，
api 退回 `config.yaml` 里的 localhost 然后连不上库。

三个必需 secret 用 `${VAR:?}` 强制，缺值 compose 直接报错退出（不会静默用空值）：
`NMP_DATABASE_PASSWORD`、`NMP_AUTH_JWT_SECRET`、`NMP_AUTH_API_KEY_PEPPER`。

#### 8.3.4 迁移语义与单副本约束

`api` 启动时 `database.Init` **无条件**执行 `migrate.Up`（`internal/database/database.go:61-66`），
当前没有「跳过迁移」的开关。因此：

- 迁移随 api 启动，不是独立一步；失败则 api 起不来，`docker compose logs api` 直接给出原因。
- **api 只允许单副本**：迁移用非阻塞 `pg_try_advisory_lock`，多副本同时冷启动时抢不到锁的副本
  会启动失败并反复重启。要多副本先做 TODO G-14（迁移与运行时解耦）。
- 手工排查：`docker compose exec api ./migrate status` / `./migrate up`。
- **迁移期的锁窗口**：`migrate.Up` 把每个迁移文件放在一个事务里，`ALTER TABLE` / `DROP INDEX` /
  `CREATE INDEX` 都取 `ACCESS EXCLUSIVE` 并持有到 COMMIT —— 含这些语句的迁移在大表上会阻塞该表的读写
  （`CREATE INDEX CONCURRENTLY` 用不了：PG 不允许它在事务块里）。
  新迁移（如 `000014` 的 jsonb 列默认值 + 回填）已把 `UPDATE` 排在 `ALTER` 之前并设
  `lock_timeout`；大表升级前先 `SELECT count(*) FILTER (WHERE tags IS NULL), count(*) FROM assets`
  估回填量级，必要时选低峰执行。
- 首个管理员（空库**没有任何用户**，默认 admin 已从迁移中移除）：
  ```bash
  docker compose exec -e FIRST_ADMIN_USERNAME=admin \
    -e FIRST_ADMIN_PASSWORD='<12位以上强密码>' api ./admin-bootstrap
  ```

#### 8.3.5 端到端验证

`scripts/smoke-compose.sh`：随机项目名起真实容器 → 断言 `/readyz`、`schema_migrations` 条数=迁移文件数、
经 nginx 登录成功、**伪造 XFF 未被采信**（G-7 端到端）、容器内非 root，跑完自动清理。
CI 不跑完整栈（Go+Node 构建成本），本地与发布前各跑一次。

> NetBox / Zabbix 段保留在 compose 中仅为「同机快速起环境」；v3 中它们是**外部系统**（生产可独立部署），
> ITmanager 只读对接。注意 `zabbix-server-pgsql` 是 server 进程（trapper 10051），**不提供 Web/API**，
> `NMP_INTEGRATIONS_ZABBIX_URL` 必须指向 Zabbix Web/API 前端（如 `zabbix-web-nginx-pgsql`）。

### 8.4 组件配置

#### 8.4.1 NetBox 配置

```bash
# 环境变量
NETBOX_SECRET_KEY=your-secret-key
ALLOWED_HOSTS=netbox.company.com
DATABASE_URL=postgres://user:pass@postgres:5432/netbox
REDIS_URL=redis://redis:6379/0
```

#### 8.4.2 Zabbix 配置

```bash
# 环境变量
DB_SERVER_HOST=postgres
POSTGRES_USER=zabbix
POSTGRES_PASSWORD=zabbix
ZBX_CACHESIZE=512M
ZBX_HISTORYCACHESIZE=512M
```

#### 8.4.3 日志（`log.level` 与 SQL 日志）

`log.level` 同时控制两条**相互独立**的日志路径（走的是不同的 writer，级别词汇也不同源）：

| 路径 | 输出目标 | 受 `log.level` 控制的行为 |
|---|---|---|
| 应用日志（`pkg/logger`） | stdout（管道/文件时是 JSON 行） | 低于配置级别的行被丢弃 |
| gorm SQL 日志（`internal/database`） | stderr（标准库 `log`） | `debug`：逐条打印语句骨架；`info`/`warn`（默认）：只打慢查询（>1s）与错误；`error`：只打错误 |

取值 `debug` / `info` / `warn` / `error`（大小写不敏感）；未配置时按 `info`。

**SQL 日志里永不出现参数值**：gorm 的 `ParameterizedQueries` 恒开，且 `DB.Scan` 路径
（走 `traceRecorder` 的包级 `RecorderParamsFilter`）也已覆盖 —— 日志里的 SQL 只有占位符
（postgres `$1` / sqlite `?`），不含 bcrypt 哈希、API Key 哈希等任何参数值。
这是 PCI DSS「认证凭据不得出现在日志中」的硬要求，**不要**为了排障把它关掉。

排障需要具体参数值时：

```bash
# 1) 先把 log.level 设成 debug，从语句骨架确认是哪条 SQL（表/列/WHERE 形态齐全）
# 2) 用 psql 手工复现并看计划（口令走 PGPASSWORD/.pgpass，不要写进命令行参数）
PGPASSWORD="<数据库口令>" psql -h 127.0.0.1 -U nmp -d network_monitor \
  -c "EXPLAIN ANALYZE SELECT ... WHERE key_hash = '<值>'"
```

> **错误文本也已收口（G-28，2026-09-09；第二轮见 `docs/FIX-PLAN-ERROR-REDACT.md` §9）**：
> HTTP 客户端的 `*url.Error` 会带上**完整 URL**（钉钉的 `access_token` 在 query、飞书/Slack 的
> token 在 path、集成 URL 可能在 userinfo），这类错误过去会同时进应用日志、
> `notification_logs.error_msg`、`gin.DefaultErrorWriter` 与人工测试的 400 响应体。现在统一过
> `internal/redact`：URL 只留 `scheme://host`，`Authorization` 头与 `access_token=`/`password=`
> 这类键值只留键名。**排障时若日志里只剩 host 没有 path，这是有意为之**，用「哪个渠道 +
> 什么错误码」定位，不要为此把脱敏关掉。第二轮补上了集成同步的四处失败日志
> （`integration/service.go:282/289/296`、`metric_sync.go:111`）——做法是在 **`httpx` 出口**统一
> 脱敏（`redactedErr`），所以任何走 `httpx` 的调用点都自动受保护。
>
> 反过来说，**PG 的 `DETAIL`（`Key (col)=(value)`）不会出现在错误里** —— pgx v5 的
> `PgError.Error()` 只拼 `Severity: Message (SQLSTATE)`（`pgconn/errors.go:51-53`）。
> 这是实测结论，不要据此以为「参数值只可能从 SQL 日志漏」。
>
> **仍登记在册的同类面**：约 30 处 `apierr.BadRequest(c, …+err.Error())`（G-31）、
> gorm/pgx 编码错误文本（G-32）、`recovery.go` 的 `slog.Any("panic", err)`（G-31）、
> 脱敏规则的值边界窄形态（G-34）与「主机名命中敏感词时端口被抹掉」（G-35，过度脱敏）、
> 集成同步路径把第三方文本写进有长度约束的列（G-45，超长即整批回滚）。

> **出口净化的另一半：剥控制字符（M29，2026-09-12，`docs/FIX-PLAN-LOG-INJECTION.md`）** ——
> 脱敏只解决「别泄漏凭据」，不解决「别伪造行」：`net/http` 会把 `%0d%0a` **解码**成真正的 CR/LF
> 放进 `URL.Path`，而 `log.Printf` **不转义**，裸拼就能凭空多出一行无从分辨的假日志（CWE-117）。
> 现在日志/落库出口统一走 `redact.StripControl`，**顺序固定 Strip → Text**（反过来会把被控制字符
> 切开的凭据尾部接回去，是真泄漏，见 `docs/TRAPS.md` T-55）。
>
> **审计行的长度口径（会截断，不是丢行）**：`audit_logs` 的字符串列都有宽度上限，超长部分**被丢弃**
> 而不是让整行 INSERT 失败 —— `path` ≤ **500 字符**、`user_agent` ≤ 500、`username` ≤ 100、
> `request_id` ≤ 50（`error_msg` ≤ 1000，当前无写入者）。口径是**字符**不是字节（`varchar(n)` 数的是
> 字符），所以多字节路径是整字符截断，不会切出非法 UTF-8。排障时看到审计行的 `path` 正好 500 字符，
> 说明原始路径更长（此前这种请求的审计行会被 PG 整行拒绝、**静默不留痕**）。资源名 `resource` 取自
> 路由模式（≤100 字符），正常值远短于上限。

#### 8.4.4 容器日志轮转（`json-file` 上限）

Docker 默认的 `json-file` 日志驱动**不轮转**：容器日志写在
`/var/lib/docker/containers/<id>/<id>-json.log`，随时间单调增长、没有上限。

对本项目而言这不是「磁盘慢慢变紧」而是**会集中爆发**的故障（G-29）：

| 服务 | 日志来源 | 增长特征 |
|---|---|---|
| `api` | gin 访问日志 + 慢查询/错误 + 应用日志 | 与请求量成正比，长期运行最快 |
| `web` | nginx access log | 与请求量成正比 |
| `postgres` / `graylog` / `elasticsearch` | 自身运行日志 | 较慢，但不为零 |

盘写满后的连锁反应：`postgres` 写入失败（`could not write to file`）→ 容器被异常重启 →
而 `docker compose logs` 本身也写不进去，**排障手段与故障同时失效**。

因此在 `docker-compose.yml` 顶部用 YAML 锚点给**全部 10 个服务**（含 `aux` profile 下的
`mongoDB`）统一上限：

```yaml
x-logging: &default-logging
  logging:
    driver: json-file
    options:
      max-size: "10m"
      max-file: "5"
```

即 **50MB/服务**、10 个服务合计上限约 500MB。单文件 10MB 保证「最近的历史」仍够排障，
`max-file: 5` 提供轮转纵深。

**改这个文件时最容易踩的坑**：锚点里必须自带 `logging:` 这一层。`<<: *default-logging`
是把锚点的条目**平铺**进服务，若写成裸的 `driver`/`options`，它们会变成服务的**顶层属性**，
`docker compose config` 直接报：

```
services.postgres Additional property driver is not allowed
```

**验证**（`docker compose config` 因 `${NMP_...:?}` 强制变量，裸跑会直接报错退出，
必须给占位值）：

```bash
NMP_DATABASE_PASSWORD=x NMP_AUTH_JWT_SECRET=x NMP_AUTH_API_KEY_PEPPER=x \
  docker compose --profile aux config --format json | python3 -c "
import json,sys
svcs=json.load(sys.stdin)['services']
bad=[k for k,s in svcs.items() if s.get('logging',{}).get('options',{}).get('max-size')!='10m']
print('服务数:', len(svcs), '未配上限:', bad)
assert len(svcs)==10 and not bad"
```

> **不要用 `grep -c 'max-size'` 做这个检查**：`docker compose config` 会把 `x-logging`
> 锚点本身也回显出来，计数比服务数多 1；再加上 `aux` profile 默认不渲染，裸跑只出 4 个服务。
> 三种跑法得到的数（5 / 11 / 服务数+1）没有一个等于服务数，会给出**假绿**。
>
> 若把日志改送 journald 或远端采集（Graylog/Splunk），本锚点应相应调整，但**不要**
> 直接删掉上限——`json-file` 无上限这件事与「有没有远端采集」无关。

### 8.5 备份方案

#### 8.5.1 备份工具选型

| 工具 | 说明 | 推荐场景 |
|------|------|----------|
| **BorgBackup** | 增量备份、去重压缩 | 文件备份 |
| **pg_dump** | PostgreSQL逻辑备份 | 数据库备份 |
| **Velero** | Kubernetes备份 | K8s环境 |

#### 8.5.2 备份脚本示例

```bash
#!/bin/bash
# backup.sh

# 数据库备份
# ⚠️ 缺口：当前只 dump 了 zabbix 库；ITmanager 业务库与 NetBox 库（资产 SoT）未单独 dump。
#    库名/账号按实际填写，不要照抄示例。
pg_dump -h postgres -U zabbix zabbix > /backup/zabbix_$(date +%Y%m%d).sql
# pg_dump -h postgres -U <user> <itmanager_db> > /backup/itmanager_$(date +%Y%m%d).sql
# pg_dump -h postgres -U <user> netbox        > /backup/netbox_$(date +%Y%m%d).sql

# NetBox 备份（资产 SoT，重点）：netbox-docker 官方脚本产出 DB dump + media + config 的 tar 包
docker exec netbox /opt/netbox/backup.sh

# 增量备份
borg create /backup::$(hostname)-$(date +%Y%m%d) /data

# 清理7天前备份（⚠️ 见 §8.5.3：SoT 备份保留期应更长，7 天不足以覆盖「误删后才发现」）
find /backup -type f -mtime +7 -delete
```

#### 8.5.3 NetBox 备份（资产 SoT，v3 重点）

NetBox 是**资产真值唯一来源**，丢失损失最大，备份要求高于其他组件：

| 项 | 要求 | 现状 |
|----|------|------|
| **数据库** | NetBox 的 PostgreSQL 库 | ⚠️ 现有脚本未单独 dump（见上） |
| **media 附件** | 设备照片 / 上传附件目录 | 由 `docker exec netbox /opt/netbox/backup.sh` 覆盖（待确认镜像版本是否含 media） |
| **配置** | `configuration.py` / 环境变量 / secret key | 同上；`SECRET_KEY` 丢失会导致加密字段不可用 |
| **恢复演练** | 至少每季度恢复到隔离环境验证一次 | ❌ 未定义，**待确认** |
| **保留期 / 异地** | SoT 备份保留期应 ≥30 天，并异地存放（borg 仓库） | ⚠️ 现有 `-mtime +7 -delete` 仅 7 天 |

> **待确认**：`linuxserver`/`netboxcommunity` 镜像中 `backup.sh` 的实际覆盖范围（是否含 media 与 config）需在实施时实测一次，并做一次恢复演练闭环。

---

*本文档为拆分后的"部署运维"模块，原章节：九*
*优化：Docker Compose 部署方案，适配小规模系统*
