# 网络运维监控平台 - 详细开发计划

**项目名称**: Network Monitor Platform (NMP)
**项目周期**: 8周
**团队模式**: AI驱动开发 (Claude Code + 人类监督)

---

## 2026-06-16 状态更新

> 本次更新：补 2026-02-15 ~ 2026-06-16 期间实际完成的工作。详细测试现状见 [TESTING.md](TESTING.md)，待办事项见 [TODO.md](TODO.md)。

### Phase 0-5 实际状态（实际全已完成）

| Phase | 计划任务数 | 实际完成 | 状态 |
|-------|-----------|---------|------|
| Phase 0: 环境准备 | 5 | 5 | ✅ |
| Phase 1: 基础设施 | 14 | 14 | ✅ |
| Phase 2: 核心集成 | 12 | 12 | ✅ |
| Phase 3: 业务开发 | 13 | 13 | ✅ |
| Phase 4: 工单集成 + 前端 | 10 | 10 | ✅ (P4.9 改 JWT 非 LDAP) |
| Phase 5: 告警通知 | 9 | 9 | ✅ |
| **Phase 6: 测试** | 3 | 3 | ✅ (覆盖率 61.2% 达标) |
| Phase 6: 部署 | 4 | 0 | 🔴 待跟进 |
| Phase 6: 部署验证 | 2 | 0 | 🔴 待跟进 |
| Phase 7: 交付验收 | 6 | 1 | 🟡 P7.3 API 文档完成 |

### 2026-06-16 增量

**代码审查 + 修复 (21 bug)**：
- handlers 8 bug：登录锁定 race / 弱密码 / 改回旧密码 / rate_limit 越界 / IP 越界 / uuid 静默 / type=garbage / Name UNIQUE
- httpx 2 bug：half-open race / ctx 取消误触熔断
- service 17 bug：Severity int / Limit=0 / 重复 First / Bulk 1000 上限 / 死代码 / usedMap 错 / User 分页 / Dashboard 单条 SQL / 假数据
- cmd + 前端 + 中间件 4 bug：admin role 检测 / testdata schema 漂移 / asset_networks ipv6_address / localStorage 缺失

**测试覆盖**：
- backend: 230 → **401 passed** in 20 packages (+171)
- frontend: 14 → **53 passed** in 8 files (+39)
- **总测试: 454** (P6.1 目标 > 60% ✅ 实际 61.2%)
- **总增量: +116 tests**

**工具链**：
- Swagger UI (`/swagger/index.html`) + swagger-cli CI validate
- type-safe API client (3/13 服务方法已 typed)
- pre-commit hook 升级：gofmt + swagger-cli validate + .bak 拦截
- TESTING.md (4.7K) 现状报告
- 文档状态同步 (TODO.md / tasks.md / 12-优化建议.md / 开发计划.md)

### 待跟进 (Phase 6 部署 + Phase 7 验收)

- [ ] P6.4 生产环境 docker-compose
- [ ] P6.5 Nginx 反向代理
- [ ] P6.6 HTTPS (Let's Encrypt)
- [ ] P6.7 日志收集 (Filebeat)
- [ ] P6.8 生产部署
- [ ] P6.9 冒烟测试
- [ ] P7.1 用户手册
- [ ] P7.2 运维手册
- [ ] P7.4 用户培训
- [ ] P7.5 验收测试
- [ ] P7.6 上线切换

**测试 / 质量相关**：
- [ ] CI 升级：加 `go test -race` + frontend vitest 步骤
- [ ] CI 升级：加 coverage 阈值门禁
- [ ] 覆盖盲区：internal/database 0% / internal/middleware 36.5% / internal/integration 39.5%
- [ ] type-safe 推进：10/13 服务方法仍 `data: any`
- [ ] 前端 coverage 工具未启用
- [ ] 5 service 方法 type-safe 渐进 3/13

---

---

## 一、项目结构

```
network-monitor-platform/
├── docker-compose.yml              # 容器编排
├── Makefile                        # 构建脚本
├── README.md                       # 项目说明
│
├── backend/                        # 后端服务 (Golang)
│   ├── main.go                    # 入口
│   ├── config/                    # 配置
│   ├── internal/                  # 内部包
│   │   ├── api/                  # API层
│   │   ├── service/              # 业务逻辑
│   │   ├── integration/          # 第三方集成
│   │   │   ├── netbox/           # NetBox API
│   │   │   ├── zabbix/           # Zabbix API
│   │   │   └── glpi/            # GLPI API
│   │   └── models/               # 数据模型
│   ├── pkg/                      # 公共包
│   ├── migrations/                # 数据库迁移
│   ├── scripts/                   # 脚本
│   └── tests/                     # 测试
│
├── frontend/                      # 前端 (React + TypeScript)
│   ├── src/
│   │   ├── components/           # 组件
│   │   ├── pages/                # 页面
│   │   ├── hooks/                # Hooks
│   │   ├── services/             # API调用
│   │   ├── stores/               # 状态管理
│   │   ├── types/                # 类型定义
│   │   └── utils/                # 工具
│   ├── public/                   # 静态资源
│   ├── package.json
│   └── vite.config.ts
│
└── docs/                         # 文档
    ├── api/                      # API文档
    ├── deploy/                   # 部署文档
    └── guides/                   # 使用指南
```

---

## 二、开发阶段总览

| 阶段 | 周次 | 内容 | 交付物 |
|------|------|------|--------|
| **Phase 0** | Week 0 | 环境准备 | 开发环境 |
| **Phase 1** | Week 1 | 基础设施 | 脚手架 + API框架 |
| **Phase 2** | Week 2 | 核心集成 | NetBox/Zabbix集成 |
| **Phase 3** | Week 3 | 业务开发 | 资产管理+监控 |
| **Phase 4** | Week 4 | 工单集成 | GLPI集成 + 前端 |
| **Phase 5** | Week 5 | 告警通知 | 告警 + 通知 |
| **Phase 6** | Week 6 | 完善优化 | 测试 + 部署 |
| **Phase 7** | Week 7-8 | 交付验收 | 上线 + 培训 |

---

## 三、详细开发任务

### Phase 0: 环境准备 (Week 0) ✅

#### Day 1-2: 项目初始化

| 任务ID | 任务 | 验收标准 | 预估工时 |
|--------|------|----------|----------|
| P0.1 | 创建 Git 仓库，初始化项目结构 | `ls -la` 可见目录结构 | 1h |
| P0.2 | 配置开发环境 (Go 1.21+, Node 18+, Docker) | `go version`, `node -v` 正常 | 1h |
| P0.3 | 编写 `Makefile` 构建脚本 | `make help` 显示可用命令 | 1h |
| P0.4 | 配置 Git hooks (pre-commit) | 提交前自动检查 | 0.5h |
| P0.5 | 创建 `docker-compose.yml` 基础配置 | `docker-compose config` 验证通过 | 2h |

**验收脚本**:
```bash
#!/bin/bash
# verify_p0.sh
set -e

echo "=== 验证环境 ==="
go version
node -v
docker --version
docker-compose --version

echo "=== 验证项目结构 ==="
ls -la
ls backend/
ls frontend/

echo "=== 验证 Makefile ==="
make help

echo "=== 验证 Docker ==="
docker-compose config

echo "=== P0 完成 ==="
```

---

### Phase 1: 基础设施 (Week 1) ✅

#### Day 1-2: 后端脚手架

| 任务ID | 任务 | 验收标准 | 预估工时 |
|--------|------|----------|----------|
| P1.1 | 初始化 Go module | `go.mod` 存在 | 0.5h |
| P1.2 | 配置日志库 (zap + logrus) | 日志写入文件 | 1h |
| P1.3 | 配置 Viper 配置管理 | 支持 YAML 配置 | 1h |
| P1.4 | 创建 HTTP 服务器 (Gin) | `curl localhost:8080/health` 返回200 | 1h |
| P1.5 | 配置数据库连接 (GORM + PostgreSQL) | 连接成功 | 1h |
| P1.6 | 创建数据库迁移脚本 | `make migrate` 可执行 | 1h |

**验收标准**:
```bash
# 后端启动测试
make run-backend &
sleep 5
curl -s localhost:8080/health | jq '.status'
# 期望: "ok"

# 数据库连接测试
curl -s localhost:8080/health | jq '.database'
# 期望: "connected"
```

#### Day 3-4: 前端脚手架

| 任务ID | 任务 | 验收标准 | 预估工时 |
|--------|------|----------|----------|
| P1.7 | 初始化 React + TypeScript 项目 | `npm run dev` 可启动 | 1h |
| P1.8 | 配置 Ant Design 5 UI库 | 组件可正常使用 | 0.5h |
| P1.9 | 配置路由 (React Router) | `/dashboard`, `/assets` 可访问 | 1h |
| P1.10 | 配置状态管理 (Zustand) | 状态可跨组件共享 | 1h |
| P1.11 | 配置 API 客户端 (Axios) | 可调用后端 API | 1h |
| P1.12 | 创建基础布局组件 | Header/Sidebar/Footer | 1h |

**验收标准**:
```bash
cd frontend
npm run build
# 期望: 无错误，构建成功

npm run dev &
sleep 10
curl -s localhost:5173 | grep -q "html"
# 期望: 页面可访问
```

#### Day 5: API 设计 + 模型

| 任务ID | 任务 | 验收标准 | 预估工时 |
|--------|------|----------|----------|
| P1.13 | 设计 RESTful API 规范 (OpenAPI 3.0) | `docs/openapi.yaml` 存在 | 2h |
| P1.14 | 创建基础数据模型 (User, Role) | 代码编译通过 | 2h |

**API 规范模板**:
```yaml
# docs/openapi.yaml
openapi: 3.0.0
info:
  title: Network Monitor Platform API
  version: 1.0.0

paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: OK

  /api/v1/assets:
    get:
      summary: List assets
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: page_size
          in: query
          schema:
            type: integer
            default: 20
      responses:
        '200':
          description: Asset list
```

---

### Phase 2: 核心集成 (Week 2) ✅

#### Day 1-2: NetBox 集成

| 任务ID | 任务 | 验收标准 | 预估工时 |
|--------|------|----------|----------|
| P2.1 | 创建 NetBox API 客户端 | 代码编译通过 | 2h |
| P2.2 | 实现设备列表同步 (GET /api/dcim/devices/) | 返回设备数据 | 2h |
| P2.3 | 实现机柜列表同步 (GET /api/dcim/racks/) | 返回机柜数据 | 1h |
| P2.4 | 实现 IP 地址同步 (GET /api/ipam/ip-addresses/) | 返回IP数据 | 1h |
| P2.5 | 封装 NetBox 服务层 | `netbox.GetDevices()` 可用 | 2h |

**验收测试**:
```go
// tests/netbox_test.go
package integration

func TestGetDevices(t *testing.T) {
    client := netbox.NewClient("http://netbox:8000", "token")
    devices, err := client.GetDevices()
    assert.NoError(t, err)
    assert.Greater(t, len(devices), 0)
}
```

```bash
make test-integration-netbox
# 期望: PASS
```

#### Day 3-4: Zabbix 集成

| 任务ID | 任务 | 验收标准 | 预估工时 |
|--------|------|----------|----------|
| P2.6 | 创建 Zabbix API 客户端 | 代码编译通过 | 2h |
| P2.7 | 实现主机列表获取 (host.get) | 返回主机数据 | 2h |
| P2.8 | 实现告警列表获取 (problem.get) | 返回告警数据 | 2h |
| P2.9 | 实现监控指标获取 (history.get) | 返回历史数据 | 1h |
| P2.10 | 封装 Zabbix 服务层 | `zabbix.GetAlerts()` 可用 | 2h |

**验收测试**:
```bash
make test-integration-zabbix
# 期望: PASS
```

#### Day 5: 统一 API 网关

| 任务ID | 任务 | 验收标准 | 预估工时 |
|--------|------|----------|----------|
| P2.11 | 创建统一的数据聚合服务 | 代码编译通过 | 2h |
| P2.12 | 实现资产+状态联合查询 | 返回合并数据 | 2h |

---

### Phase 3: 业务开发 (Week 3) ✅

#### Day 1-2: 资产管理页面

| 任务ID | 任务 | 验收标准 | 预估工时 |
|--------|------|----------|----------|
| P3.1 | 创建资产列表页面 (Ant Design Table) | 页面可访问 | 2h |
| P3.2 | 实现资产列表 API (`GET /api/v1/assets`) | 返回资产数据 | 1h |
| P3.3 | 实现资产详情页面 | 显示完整信息 | 2h |
| P3.4 | 实现资产筛选/搜索功能 | 筛选生效 | 1h |
| P3.5 | 实现资产导出功能 (CSV/Excel) | 下载文件正常 | 1h |

**验收标准**:
```bash
# API 测试
curl -s localhost:8080/api/v1/assets | jq '.total'
# 期望: >= 0

# 前端测试
# 1. 访问 /assets 页面
# 2. 表格显示数据
# 3. 分页正常工作
# 4. 筛选功能正常
```

#### Day 3-4: 机柜可视化

| 任务ID | 任务 | 验收标准 | 预估工时 |
|--------|------|----------|----------|
| P3.6 | 创建机柜列表页面 | 页面可访问 | 1h |
| P3.7 | 实现机柜设备可视化 (D3.js/Three.js) | 3D/2D 机柜图 | 3h |
| P3.8 | 实现设备状态指示灯 (绿/黄/红) | 状态正确显示 | 2h |
| P3.9 | 实现悬浮显示指标 (ECharts) | 悬浮显示图表 | 2h |
| P3.10 | 实现点击跳转详情 | 跳转到详情页 | 1h |

**验收标准**:
```typescript
// 机柜组件测试
// 1. 访问 /racks 页面
// 2. 机柜图正常渲染
// 3. 设备状态灯正确显示 (绿=正常, 黄=警告, 红=告警)
// 4. 鼠标悬浮显示 CPU/内存图表
// 5. 点击设备跳转到监控详情
```

#### Day 5: 监控仪表盘

| 任务ID | 任务 | 验收标准 | 预估工时 |
|--------|------|----------|----------|
| P3.11 | 创建监控仪表盘页面 | 页面可访问 | 2h |
| P3.12 | 实现概览统计卡片 | 显示资产/告警/工单数量 | 2h |
| P3.13 | 实现告警趋势图 (ECharts) | 显示7天趋势 | 1h |

---

### Phase 4: 工单集成 + 前端完善 (Week 4) ✅

#### Day 1-2: GLPI 集成

| 任务ID | 任务 | 验收标准 | 预估工时 |
|--------|------|----------|----------|
| P4.1 | 创建 GLPI API 客户端 | 代码编译通过 | 2h |
| P4.2 | 实现工单列表获取 (GET /Ticket/) | 返回工单数据 | 2h |
| P4.3 | 实现工单详情获取 | 返回工单详情 | 1h |
| P4.4 | 实现告警自动创建工单 | 告警触发自动创建 | 2h |

#### Day 3-4: 工单页面

| 任务ID | 任务 | 验收标准 | 预估工时 |
|--------|------|----------|----------|
| P4.5 | 创建工单列表页面 | 页面可访问 | 2h |
| P4.6 | 实现工单详情页面 | 显示工单信息 | 2h |
| P4.7 | 实现工单创建页面 | 可手动创建工单 | 2h |

#### Day 5: 用户权限页面

| 任务ID | 任务 | 验收标准 | 预估工时 |
|--------|------|----------|----------|
| P4.8 | 创建登录页面 | 页面可访问 | 1h |
| P4.9 | 实现 LDAP 认证 | 登录成功 | 2h |
| P4.10 | 创建用户管理页面 | 页面可访问 | 1h |

---

### Phase 5: 告警通知 (Week 5) ✅

#### Day 1-2: 告警管理

| 任务ID | 任务 | 验收标准 | 预估工时 |
|--------|------|----------|----------|
| P5.1 | 创建告警列表页面 | 页面可访问 | 2h |
| P5.2 | 实现告警详情页面 | 显示告警详情 | 1h |
| P5.3 | 实现告警确认功能 | 状态变更为已确认 | 1h |
| P5.4 | 实现告警解决功能 | 状态变更为已解决 | 1h |

#### Day 3-4: 通知配置

| 任务ID | 任务 | 验收标准 | 预估工时 |
|--------|------|----------|----------|
| P5.5 | 创建通知渠道配置页面 | 页面可访问 | 2h |
| P5.6 | 实现钉钉 webhook 配置 | 告警可推送到钉钉 | 2h |
| P5.7 | 实现邮件通知配置 | 告警可发送邮件 | 1h |

#### Day 5: 告警规则

| 任务ID | 任务 | 验收标准 | 预估工时 |
|--------|------|----------|----------|
| P5.8 | 对接 Zabbix 告警规则 | 规则同步成功 | 2h |
| P5.9 | 创建告警统计页面 | 显示告警报表 | 2h |

---

### Phase 6: 测试 + 部署 (Week 6) 🟡 (测试 ✅ 部署 🔴)

#### Day 1-2: 单元测试

| 任务ID | 任务 | 验收标准 | 预估工时 |
|--------|------|----------|----------|
| ✅ P6.1 | 后端单元测试 (覆盖率 > 60%) | `make test-coverage` | 3h |
| ✅ P6.2 | 前端组件测试 | `npm test` | 2h |
| ✅ P6.3 | API 集成测试 | 所有 API 测试通过 | 3h |

**验收标准**:
```bash
# 后端测试
make test
# 期望: PASS, Coverage > 60%

# 前端测试
cd frontend && npm test -- --coverage
# 期望: PASS
```

#### Day 3-4: 部署配置

| 任务ID | 任务 | 验收标准 | 预估工时 |
|--------|------|----------|----------|
| P6.4 | 配置生产环境 docker-compose | `docker-compose -f prod.yml config` | 2h |
| P6.5 | 配置 Nginx 反向代理 | 负载均衡正常 | 1h |
| P6.6 | 配置 HTTPS (Let's Encrypt) | SSL 证书生效 | 1h |
| P6.7 | 配置日志收集 (Filebeat) | 日志可查询 | 1h |

#### Day 5: 部署验证

| 任务ID | 任务 | 验收标准 | 预估工时 |
|--------|------|----------|----------|
| P6.8 | 生产环境部署 | 所有服务正常运行 | 2h |
| P6.9 | 冒烟测试 | 核心功能正常 | 2h |

---

### Phase 7: 交付验收 (Week 7-8) 🟡 (P7.3 ✅)

| 任务ID | 任务 | 验收标准 | 预估工时 |
|--------|------|----------|----------|
| P7.1 | 编写用户手册 | 文档完整 | 4h |
| P7.2 | 编写运维手册 | 文档完整 | 4h |
| ✅ P7.3 | 编写 API 文档 | Swagger 可用 (`/swagger/index.html`) | 2h |
| P7.4 | 用户培训 | 培训完成 | 4h |
| P7.5 | 验收测试 | 所有功能正常 | 8h |
| P7.6 | 上线切换 | 生产环境切换 | 4h |

---

## 四、验收脚本

### 4.1 自动化验收脚本

```bash
#!/bin/bash
# verify.sh - 完整验收脚本

set -e

echo "========================================"
echo "开始验收测试"
echo "========================================"

# 1. 环境检查
echo "[1/10] 检查环境..."
./scripts/verify_env.sh

# 2. 后端服务
echo "[2/10] 检查后端服务..."
curl -s localhost:8080/health | jq -e '.status == "ok"'

# 3. 前端服务
echo "[3/10] 检查前端服务..."
curl -s localhost:5173 | grep -q "html"

# 4. NetBox 集成
echo "[4/10] 检查 NetBox 集成..."
curl -s localhost:8080/api/v1/assets | jq -e '.code == 0'

# 5. Zabbix 集成
echo "[5/10] 检查 Zabbix 集成..."
curl -s localhost:8080/api/v1/alerts | jq -e '.code == 0'

# 6. GLPI 集成
echo "[6/10] 检查 GLPI 集成..."
curl -s localhost:8080/api/v1/tickets | jq -e '.code == 0'

# 7. 单元测试
echo "[7/10] 运行单元测试..."
make test

# 8. 构建测试
echo "[8/10] 构建测试..."
make build

# 9. Docker 构建
echo "[9/10] Docker 构建..."
docker-compose build

# 10. 安全检查
echo "[10/10] 安全检查..."
./scripts/security_scan.sh

echo "========================================"
echo "验收完成！"
echo "========================================"
```

### 4.2 CI/CD 流程

```yaml
# .github/workflows/ci.yml
name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.21'

      - name: Run tests
        run: make test

      - name: Run coverage
        run: make test-coverage

      - name: Build
        run: make build

  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Node
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install and test
        run: |
          cd frontend
          npm install
          npm run build
```

---

## 五、AI 编程最佳实践

### 5.1 任务分解原则

```
1. 小步提交: 每个任务 < 2小时
2. 单一职责: 一个函数 < 50行
3. 测试先行: 先写测试，再写实现
4. 文档同步: 代码与注释同步更新
```

### 5.2 Claude Code 使用模式

```bash
# 1. 创建任务
claude task create "实现资产列表API" --spec "P3.1-3.2"

# 2. 执行开发
claude task run P3.1

# 3. 代码审查
claude review --diff HEAD~1

# 4. 运行测试
make test P3.1
```

### 5.3 代码规范

```go
// 标准包结构
package service

// 错误处理
func (s *Service) GetAsset(id string) (*Asset, error) {
    if id == "" {
        return nil, ErrInvalidID
    }

    asset, err := s.repo.GetByID(id)
    if err != nil {
        return nil, fmt.Errorf("get asset: %w", err)
    }

    return asset, nil
}
```

```typescript
// React 组件规范
interface AssetListProps {
  onSelect: (asset: Asset) => void;
}

export const AssetList: React.FC<AssetListProps> = ({ onSelect }) => {
  const [assets, setAssets] = useState<Asset[]>([]);
  const [loading, setLoading] = useState(false);

  useEffect(() => {
    loadAssets();
  }, []);

  return <Table dataSource={assets} loading={loading} />;
};
```

---

## 六、Milestone 检查点

| Milestone | 日期 | 验收条件 |
|-----------|------|----------|
| M1 | Week 1 结束 | 脚手架完成，可启动服务 |
| M2 | Week 2 结束 | 三大系统集成完成 |
| M3 | Week 3 结束 | 核心页面可访问 |
| M4 | Week 4 结束 | 全部页面完成 |
| M5 | Week 5 结束 | 告警通知完成 |
| M6 | Week 6 结束 | 测试通过，可部署 |
| M7 | Week 8 结束 | 验收通过，上线 |

---

## 七、风险应对

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|----------|
| 第三方 API 变更 | 中 | 中 | 版本锁定，定期同步 |
| 性能瓶颈 | 低 | 高 | 提前压测，预留容量 |
| 需求变更 | 中 | 中 | 敏捷迭代，版本管理 |
| 人员变动 | 低 | 高 | 文档完备，知识传承 |

---

*计划版本: v1.0*
*编制日期: 2026-02-14*
