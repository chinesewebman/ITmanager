# 网络运维监控平台 - 资产管理模块

> **v3 修订（2026-09-09）**：**NetBox 是资产真值唯一来源（SoT）**。新增**专线（Circuits）**与 **vCenter 纳管**两节（§2.7 / §2.8）。自建 `lines` / `vmware_*` / `idc` 表设计**已废弃**（0 行实现，见 §2.9）。依据 `docs/v3-架构优化需求.md` R2 / R3。

## 二、资产管理详细设计

### 2.1 资产管理的开源方案

#### 2.1.1 NetBox 选型

| 特性 | NetBox | 说明 |
|------|--------|------|
| **DCIM** | ✅ 完整 | 机柜、设备、物理连接 |
| **IPAM** | ✅ 完整 | IP地址、VLAN、ASN |
| **Circuits** | ✅ 完整 | 专线（P2P 原生；MPLS 每腿一条，终止在 Provider Network） |
| **Virtualization** | ✅ 完整 | VM / 集群 / ESXi 主机 |
| **API** | ✅ REST API | 完整 CRUD 接口 |
| **Webhooks** | ✅ 支持 | 事件触发通知 |
| **插件** | ✅ 丰富 | 可扩展功能 |
| **部署** | ✅ Docker | 快速部署 |

#### 2.1.2 系统架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                    资产管理层架构                                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                              │
│   ┌─────────────────────────────────────────────────────────┐    │
│   │                   自建业务平台                           │    │
│   │                   (前端展示层)                          │    │
│   └──────────────────────────┬──────────────────────────────┘    │
│                              │                                    │
│                              ▼                                    │
│   ┌─────────────────────────────────────────────────────────┐    │
│   │                   API 网关                             │    │
│   │              (鉴权、限流、路由)                         │    │
│   └──────────────────────────┬──────────────────────────────┘    │
│                              │                                    │
│            ┌─────────────────┼─────────────────┐                │
│            ▼                 ▼                 ▼                    │
│   ┌──────────────┐   ┌──────────────┐   ┌──────────────┐     │
│   │   NetBox    │   │    Zabbix    │   │  VMware API  │     │
│   │  (资产源)   │◄─►│  (监控数据)  │◄─►│  (虚拟机)    │     │
│   └──────────────┘   └──────────────┘   └──────────────┘     │
│          │                 │                                    │
│          └─────────────────┴────────────────────────────────────│
│                            │                                     │
│                            ▼                                     │
│   ┌─────────────────────────────────────────────────────────┐    │
│   │                   PostgreSQL 数据库                       │    │
│   │         (NetBox + Zabbix 数据存储)                     │    │
│   └─────────────────────────────────────────────────────────┘    │
│                                                              │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.2 自动发现模块

#### 2.2.1 发现来源

| 来源类型 | 工具 | 说明 |
|----------|------|------|
| **SNMP扫描** | Zabbix | 通过SNMP协议扫描网段内的设备 |
| **Ping探测** | Zabbix / fping | ICMP探测存活主机 |
| **ARP扫描** | NetBox Discovery | 获取局域网内MAC地址和IP对应关系 |
| **VMware API** | VMware vSphere API | 自动发现虚拟化环境中的VM |
| **API导入** | 手动/脚本 | 第三方系统API导入 |

#### 2.2.2 跨网段整合

```
┌─────────────────────────────────────────────────────────────┐
│                     发现任务调度中心                         │
├─────────────────────────────────────────────────────────────┤
│  发现任务A (192.168.1.0/24)  ─┐                            │
│  发现任务B (10.0.0.0/8)       │──> 统一归一化处理 ──> NetBox │
│  发现任务C (172.16.0.0/12)  ─┘                            │
└─────────────────────────────────────────────────────────────┘
```

- **多任务并行**：不同网段可并行发起发现任务
- **统一归一化**：所有来源的数据统一格式、去重、关联
- **增量更新**：发现结果与已有资产比对，自动识别新增/变更/下线

### 2.3 资产数据模型（基于NetBox）

#### 2.3.1 核心实体

| 实体 | 说明 | NetBox对应 |
|------|------|-------------|
| 机房 | 数据中心 | Site |
| 机柜 | 服务器机柜 | Rack |
| 设备 | 服务器/网络设备 | Device |
| 接口 | 网络接口 | Interface |
| IP地址 | 管理IP/业务IP | IP Address |
| VLAN | 网络VLAN | VLAN |
| 缆线 | 物理连接 | Cable |

#### 2.3.2 设备类型映射

| 设备类型 | Zabbix监控 | NetBox类型 |
|----------|------------|------------|
| 物理服务器 | SNMP/Agent | Server |
| 虚拟机 | VMware Tools | Virtual Machine |
| 交换机 | SNMP | Switch |
| 路由器 | SNMP | Router |
| 防火墙 | SNMP | Security Device |
| WAF | SNMP/API | Security Device |
| IPS | SNMP/API | Security Device |

### 2.4 资产发现流程

```
┌─────────────────────────────────────────────────────────────┐
│                    资产发现流程                                │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  1. 定时任务触发                                             │
│         │                                                    │
│         ▼                                                    │
│  2. 执行网段扫描 (Zabbix Discovery / Nmap)                  │
│         │                                                    │
│         ▼                                                    │
│  3. SNMP探测识别设备                                         │
│         │                                                    │
│         ▼                                                    │
│  4. 获取设备信息 (厂商/型号/序列号)                          │
│         │                                                    │
│         ▼                                                    │
│  5. 调用 NetBox API 创建/更新设备                            │
│         │                                                    │
│         ▼                                                    │
│  6. 调用 Zabbix API 创建监控项                              │
│         │                                                    │
│         ▼                                                    │
│  7. 发送发现报告 (邮件/钉钉)                                 │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 2.5 集成接口设计

#### 2.5.1 NetBox API 调用

> v3：示例语言统一为 **Go**（原 Python 示例与本项目技术栈不符，已替换）。方法名以实际实现为准。

```go
// 示例：创建设备（internal/integration/netbox.go 风格）
// 注意：实际请求走 internal/httpx（含 retry + 熔断），不要直接 net/http。
func (c *NetBoxClient) CreateDevice(ctx context.Context, dev NetBoxDevice) (*NetBoxDevice, error) {
	var out NetBoxDevice
	if err := c.do(ctx, http.MethodPost, "/api/dcim/devices/", dev, &out); err != nil {
		return nil, fmt.Errorf("netbox create device: %w", err)
	}
	return &out, nil
}
```

#### 2.5.2 Zabbix API 调用

```go
// 示例：创建监控主机（internal/integration/zabbix.go 风格）
type zabbixReq struct {
	JSONRPC string      `json:"jsonrpc"`
	Method  string      `json:"method"`
	Params  interface{} `json:"params"`
	Auth    string      `json:"auth"`
	ID      int         `json:"id"`
}

func (c *ZabbixClient) CreateHost(ctx context.Context, host map[string]interface{}) (json.RawMessage, error) {
	req := zabbixReq{JSONRPC: "2.0", Method: "host.create", Params: host, Auth: c.token, ID: 1}
	var out struct {
		Result json.RawMessage `json:"result"`
	}
	if err := c.postJSON(ctx, "/api_jsonrpc.php", req, &out); err != nil {
		return nil, fmt.Errorf("zabbix host.create: %w", err)
	}
	return out.Result, nil
}
```

### 2.6 SolarWinds Orion 9迁移模块（如有需求）

#### 2.6.1 迁移概述

对于从SolarWinds Orion 9系统迁移的场景，本平台提供完整的数据导出、配置转换和资产合并能力。

#### 2.6.2 导出数据范围

| 数据类型 | 映射到本平台 |
|----------|--------------|
| **节点信息** | NetBox Device |
| **接口信息** | NetBox Interface |
| **监控指标** | Zabbix Items |
| **告警定义** | Zabbix Triggers |

### 2.7 专线建模（v3 R2，field-level split 落地）

**结论**：专线以 NetBox `Circuit` + `CircuitTermination` 建模，ITmanager 只读渲染；自建 `lines` 系列四表已归档（保留在 `docs/schema-planned.sql:39-125`，仅作历史追溯，不实施）。

#### 2.7.1 数据流

```
NetBox (Circuit + CircuitTermination + Provider + ProviderNetwork)
   │  /api/circuits/  （ITmanager 通过 internal/integration/netbox.go 只读消费）
   ▼
ITmanager 专线页 ──── 只读渲染（A/Z 端点、带宽、电路类型、运营商）
   │ 关联键: circuit.id (int)
   ▼
ITmanager 商务元数据视图（合同期/月租/故障电话/业务归属/关键性）── 不做双写
```

**单一写入方 = NetBox**。ITmanager 侧所有"专线"数据要么是 NetBox 镜像缓存（短期、最终一致），要么是商务元数据（以 `circuit_id` 为关联键）。**绝不向 NetBox Custom Field 写**。

#### 2.7.2 字段级切分（v3 §7 自审 #1 落地，逐字段对照 `docs/schema-planned.sql:39-125`）

**A 组 — 物理真值 → NetBox Circuits**

| 原 `lines` 字段 | NetBox 落点 | 说明 |
|---|---|---|
| `id` (UUID) | NetBox 自动生成 | ITmanager 以 NetBox `circuit.id` (int) 为外键关联键 |
| `line_no` (`VARCHAR(50) UNIQUE`) | `Circuit.cid` (`VARCHAR(100)`) | NetBox 唯一标识，保留"业务编号"语义 |
| `line_type_id` | `Circuit.type` (FK → `CircuitType`) | NetBox CircuitType 已含 MPLS / VPN / Internet 等 |
| `carrier` | `Circuit.provider` (FK → `Provider`) | NetBox Provider 实体 |
| `bandwidth` (`VARCHAR(30)`) | `CircuitTermination.port_speed` (int, Kbps) | NetBox 用整数 Kbps，更标准 |
| `bandwidth_mbps` (`INTEGER`) | 同上（合并表达） | |
| `endpoint_a_idc_id` | `CircuitTermination.site` (FK → Site) | NetBox Site |
| `endpoint_a_rack_id` | `CircuitTermination.termination_a_rack` (FK → Rack) | NetBox Rack |
| `endpoint_a_device` | 通过 Cable 关联到 Device | NetBox Cable 拓扑更精确 |
| `endpoint_a_interface` | `CircuitTermination.termination_a_device` + Cable → Interface | |
| `endpoint_a_vlan` | `Interface.untagged_vlan` / `tagged_vlans` | |
| `endpoint_b_*` | 同 endpoint_a_* 的 Z 端 | NetBox Circuit **恰好两个端点**（A/Z） |
| `status` (`active`/`disabled`/`decommissioning`) | `Circuit.status`（枚举：`active`/`planned`/`provisioning`/`offline`/`decommissioning`） | 字段对齐度高 |
| `install_date` | `Circuit.install_date` (DATE) | |

> `endpoint_a_device` / `endpoint_a_interface` 在 NetBox 里不是 Circuit 的直接字段，而是通过 Cable 关联到 Device.Interface。NetBox 自动化发现时由 Cable 反映"这条物理专线接在哪台设备的哪个端口"，比自建字符串列强。

**B 组 — 商务元数据 → ITmanager 视图（NetBox 不覆盖）**

| 原 `lines` 字段 | ITmanager 落点 | 为什么不进 NetBox |
|---|---|---|
| `contract_no` (`VARCHAR(50)`) | `line_meta.contract_no` | 合同号是法务/财务字段，与资产真值无关 |
| `contract_start` / `contract_end` | `line_meta.contract_period` | NetBox 不管理合同生命周期 |
| `monthly_fee` (`DECIMAL(10,2)`) | `line_meta.monthly_fee` | 财务字段，不属于 CMDB |
| `contact_person` / `contact_phone` | `line_meta.carrier_contact` | 客户经理是商务关系，非拓扑 |
| `fault_phone` (`VARCHAR(20)`) | `line_meta.fault_hotline` | 运营商故障电话是运维速查项，需独立查询 |
| `purpose` (`VARCHAR(200)`) | `line_meta.business_purpose` | 业务用途为运维视角，与 NetBox 拓扑语义不同 |
| `business_unit` (`VARCHAR(50)`) | `line_meta.business_unit` | 业务归属按公司部门，非资产真值 |
| `is_critical` (`BOOLEAN`) | `line_meta.criticality` | 关键性是 SLO 视角，由 ITmanager 评 |

**关联形式**：`line_meta.circuit_id` (int, FK 概念) → NetBox `Circuit.id`。NetBox 端以周期/Webhook 同步将 circuit 列表拉到 ITmanager `circuit_cache`（只读），`line_meta` 与 `circuit_cache` 通过 `circuit_id` 关联。**不向 NetBox Custom Field 写**。

**C 组 — 审计与监控 → 拆分到既有通路**

| 原表 | 原字段 | 处置 |
|---|---|---|
| `line_changes` | `change_type` / `change_field` / `old_value` / `new_value` / `reason` / `operator_id` / `changed_at` | **物理真值变更**由 NetBox changelog 保留（不可篡改）；**运维视角变更**（如"业务归属从 A 部门调到 B 部门"）写入 ITmanager `audit_logs`，schema 已存在，无需新增表 |
| `line_monitors` | `monitor_type` / `target` / `interval` / `is_enabled` | **不建表**。专线监控通过 Zabbix host template 表达（与设备监控同链路），配置写 Zabbix，ITmanager 通过 `/api/v1/monitoring/hosts` 只读查询 |
| `line_types` | `code` / `name` / `description` | **不建表**。NetBox `CircuitType` 实体已覆盖 |

#### 2.7.3 join key 总表

| 关联关系 | 键 | 方向 |
|---|---|---|
| NetBox Circuit ↔ ITmanager `circuit_cache` | `circuit.id` (int) | NetBox → ITmanager（周期/Webhook 拉取） |
| ITmanager `circuit_cache` ↔ `line_meta` | `circuit_id` | 同库 join |
| ITmanager `audit_logs` ↔ NetBox Circuit | `circuit_id` + 时间窗 | ITmanager → NetBox（运维视角变更时反查原始变更） |

#### 2.7.4 NetBox Circuits 数据模型与 MPLS VPN 硬限制

| 对象 | 作用 |
|---|---|
| `Circuit` | 一条电路，**恰好两个端点** |
| `CircuitType` | 电路类型（官方示例含 **MPLS/VPN**） |
| `CircuitTermination` | A 端 / Z 端，接到**站点**或 **Provider Network** |
| `Provider` | 运营商 |
| `Provider Network` | 运营商网络（如「运营商 A 的区域 MPLS 网络」） |

**MPLS VPN 建模硬限制**（非偷懒，是官方设计）：NetBox **只建物理电路，不支持虚拟电路建模**。MPLS 是逻辑隧道，因此多点 MPLS VPN 每条腿单独建一条 Circuit，一端终止在 Provider Network 内：

```
多点 MPLS VPN（3 站点）：

  Circuit-1: 站点A ◄────────► ProviderNetwork(运营商A MPLS)
  Circuit-2: 站点B ◄────────► ProviderNetwork(运营商A MPLS)
  Circuit-3: 站点C ◄────────► ProviderNetwork(运营商A MPLS)

  每条「腿」单独建一条 Circuit，一端终止在 Provider Network 内。
  VPN 本身的拓扑关系（谁和谁通）用 Tag / Custom Field / 外部文档补充。
```

物理段可发现、可计费、可排障；逻辑拓扑变更频繁，不适合硬编码进 CMDB。

#### 2.7.5 明确不做（NOT doing）

| 不做 | 理由 |
|---|---|
| 自建 `lines` CRUD（含 handler/service/route/test） | 与 NetBox Circuits 重复（v3 §5 R2 行） |
| 双写到 NetBox Custom Field | 单一写入方原则；Custom Field 不支持复杂校验 / 变更审计 / 跨表查询 |
| 实施 NetBox 部署本身 | NetBox 部署独立任务，本节只定边界 |
| 把 `line_meta` 落到实际 DDL | 商务元数据字段描述在此；具体表/视图的 DDL 推迟到 NetBox 部署任务 |
| 用 NetBox Webhook 实现实时同步 | Webhook 复杂度（去重、丢包补偿、回放）超出 R2 范围；先 polling |
| `line_monitors` 的 ITmanager 自建版本 | 监控走 Zabbix host template，与设备监控统一链路 |

### 2.8 vCenter 纳管（v3 新增，对应 R3）

**结论：vCenter → NetBox 同步，ITmanager 从 NetBox 读 VM。不直连 vCenter。**

```
vCenter ──(bb-Ricardo/netbox-sync)──► NetBox ──► ITmanager（只读）
   │                                     ▲
   └── Zabbix VMware 模板（指标）─────────┘（监控，非资产）
```

| 项 | 说明 |
|---|---|
| 工具 | 社区版 `bb-Ricardo/netbox-sync`（pyVmomi + vSphere Automation SDK） |
| 部署 | Docker / K8s CronJob |
| 孤儿识别 | NetBox 打 `NetBox-synced` 标签，自动清理 |
| 官方替代 | NetBox Labs 官方 vCenter 集成（Orb Agent Pro）**仅商业版** |

**⚠️ 风险与缓解**：VM 频繁增删（快照 / 克隆 / 临时机）会产生大量孤儿对象 → **先 dry-run 跑一周**，确认删除策略再开自动清理；删除前保留人工确认窗口。

#### 2.8.1 字段对照表（NetBox VirtualMachine → ITmanager asset view，v3 R3 落地）

| NetBox VirtualMachine 字段 | ITmanager 资产视图展示 | 渲染规则 |
|---|---|---|
| `name` | 资产名 / 主机名 | 必填；与 `assets.name` 一致 |
| `vcpus` | CPU 核心数 | 整数 |
| `memory` (MB) | 内存（GB，自动 /1024） | 小数 1 位 |
| `disk` (GB) | 磁盘容量（GB） | 整数 |
| `status` (`active`/`offline`/`staged`) | 状态徽章 | active→绿 / offline→灰 / staged→黄 |
| `cluster` (FK) | 集群名 | 来自 NetBox `Cluster.name` |
| `site` (FK) | 站点 | 来自 NetBox `Site.name`（与 R2 站点共用同一张 Site 表） |
| `tenant` (FK) | 业务归属 | 来自 NetBox `Tenant.name`（与 R2 业务归属一致） |
| `tags` | 标签列表 | 至少含 `NetBox-synced`（孤儿识别关键） |
| `custom_fields` | 自定义字段（ITmanager 只读展示） | JSON 字符串，不解析 |
| `last_updated` | 最近同步时间 | 时区 = `Asia/Shanghai`，格式 `YYYY-MM-DD HH:MM:SS` |
| **不展示** | vCenter `config.hardware.uuid` (BIOS UUID) | ITmanager 不暴露 |
| **不展示** | vCenter `runtime.host` (ESXi host) | 走 NetBox 关联，不直读 vCenter |

> **与 R2 协调**（§2.7 已落）：同一张 ITmanager 资产视图同时呈现 line (R2) + VM (R3)，运维人员不需要切换 view。**单一 SoT = NetBox**，两边都不双写。

#### 2.8.2 渲染规则（asset view 行为）

1. **资产列表**：`GET /api/assets` 增加 `kind=vm` 过滤参数（与 `kind=device/rack/line` 共用，参考 routes.go 已实现的 kind 过滤）。
2. **资产详情**：`GET /api/assets/{id}` 返回结构对 VM 类型增加 `vm_fields: {vcpus, memory_gb, disk_gb, cluster, site, tenant, ...}` 字段（与现有 `line_fields` 字段对仗）。
3. **孤儿 VM**：标签含 `NetBox-synced` 但 NetBox 那边已无对应 VirtualMachine 记录 → 状态显示「**NetBox 失联**」红色徽章（不等同于「离线」）。具体识别与清理流程见 03-监控采集.md §3.7 vCenter 孤儿 VM 处理 runbook。
4. **不引入新表**：VM 数据走 NetBox（不在 ITmanager 落地）；只在 ITmanager 缓存 `last_seen_at` 用于孤儿识别告警（**未来扩展**，本轮不实施）。

### 2.9 已废弃的自建设计（保留作历史）

| 已废弃 | 原因 |
|---|---|
| 自建 `lines` / `line_types` / `line_changes` / `line_monitors` 四表 | 与 NetBox Circuits 重复，且 0 行实现 |
| 自建 `vmware_vms` / `vmware_hosts` / `vmware_connections` / `vmware_sync_history` 四表 | 与 NetBox Virtualization 重复，且 0 行实现 |
| 自建 `idc` 表 | 与 NetBox Site 重复，且 0 行实现 |

> 表结构定义保留在 `docs/schema-planned.sql`（标注 planned），**不删除**，保留可追溯性。

---

*本文档为拆分后的"资产管理"模块，原章节：二*
*优化：集成 NetBox 作为资产管理核心*
