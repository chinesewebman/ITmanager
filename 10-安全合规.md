# 网络运维监控平台 - 安全合规设计

> **v3 修订块** · 状态 **draft** · 日期 2026-09-09 · 依据 `docs/v3-架构优化需求.md` · 分支 `docs/v3-optimize`
>
> 本章 v3 增补：新增 **§10.5 AI 层的访问边界**（内容与 `09-AI辅助.md` §9.3 一致）——AI 层（Keep / HolmesGPT）对 ITmanager **一律只读**。
> 硬约束：只读 token + 最小 scope、写操作端点不暴露、数据出境边界、调用留审计。
> 既有 §10.1–§10.4 与 v3 三层定位**无冲突**，保留；§10.4 另有一处与实现的**不一致标注**（见 §10.4.3，仅标注不删原文）。

## 十、安全合规设计

### 10.1 SNMP通讯字安全检查

#### 10.1.1 合规标准

| 等级 | 通讯字类型 | 处理方式 |
|------|------------|----------|
| 🔴 **不合规** | public、private、manager、default、admin、root、12345 等常见默认字符串 | 记录并告警，要求整改 |
| 🟡 **警告** | 短于8位、纯数字、纯字母 | 建议修改，但不强制 |
| 🟢 **合规** | 复杂组合（大小写+数字+特殊字符，长度≥12位） | 正常通过 |

#### 10.1.2 合规检查流程

```
发现任务执行
     │
     ▼
遍历网段IP ──► SNMP探测（尝试通讯字列表）
     │
     ▼
  通讯成功？
     │
   ┌─┴─┐
   │   │
   是  否
   │   │
   ▼   ▼
检查通讯字 ◄────────── 下一IP
是否合规
   │
 ┌─┴─┐
 │   │
合规 不合规
 │   │
 ▼   ▼
标记"合规" 标记"不合规" + 生成整改工单
```

### 10.2 敏感数据加密

#### 10.2.1 加密存储字段

| 存储位置 | 字段 | 加密方式 |
|----------|------|----------|
| Zabbix | SNMP Community | Zabbix原生加密 |
| 数据库 | 敏感配置 | AES-256-GCM |
| 配置文件 | API密钥 | 环境变量/密钥管理 |

#### 10.2.2 密钥管理

```
┌─────────────────────────────────────────────────────────────┐
│                      密钥管理架构                             │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐ │
│   │  主密钥     │────►│  应用密钥   │────►│  数据加密   │ │
│   │ (HSM/KMS)  │     │ (KEK)       │     │ (DEK)       │ │
│   └─────────────┘     └─────────────┘     └─────────────┘ │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 10.3 访问控制

#### 10.3.1 权限模型

采用 RBAC (基于角色的访问控制) + LDAP 组映射。

```
┌─────────────────────────────────────────────────────────────┐
│                      权限检查流程                             │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│   用户 ──► LDAP验证 ──► 角色映射 ──► 权限检查            │
│           │                  │                              │
│           ▼                  ▼                              │
│       验证密码          分配权限                           │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

#### 10.3.2 默认角色

| 角色 | 权限 |
|------|------|
| **管理员** | 完全控制 |
| **运维工程师** | 资产管理、监控配置、告警处理 |
| **只读用户** | 查看所有资源 |

### 10.4 安全审计

#### 10.4.1 审计日志内容

| 字段 | 说明 |
|------|------|
| timestamp | 操作时间 |
| user_id | 用户ID |
| username | 用户名 |
| action | 操作类型 |
| resource | 资源类型和ID |
| ip_address | 客户端IP |
| result | 操作结果 |

#### 10.4.2 审计日志存储

```
┌─────────────────────────────────────────────────────────────┐
│                    审计日志存储                               │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│   各系统日志 ──► Filebeat ──► Graylog ──► 存储           │
│                                                              │
│   审计内容:                                                 │
│   - 用户登录/登出                                          │
│   - 权限变更                                               │
│   - 配置修改                                               │
│   - 数据导出                                               │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

#### 10.4.3 与 `audit_logs` 表实现的不一致（v3 标注，原文保留不删）

> ⚠️ **不一致，待确认后由实现方处理。本节上方原文保留。**

| 项 | 建表定义 | Go 模型 / 写入代码 |
|------|----------|--------------------|
| 出处 | `schema.sql:383`、`backend/migrations/000001_init.up.sql:1085`（行号为 2026-09-09 快照） | `backend/internal/models/user.go:100-114`、`backend/internal/middleware/audit.go:45` |
| 时间字段 | `timestamp` | `created_at` |
| 客户端 IP | `ip_address` | `ip` |
| 事件 / 结果 | `event_type` / `result` | `method` + `path` / `status` |
| 其它 | `changes` / `old_values` / `new_values` / `risk_level` | `request_id` / `error_msg` |

`middleware.AuditLog` 按模型字段写入（挂在 protected 组，`backend/internal/api/routes.go:199`），与建表列名不匹配 → 审计落库**很可能失败**（失败仅 `slog.Warn`，不阻断请求），需实测确认。

- §10.4.1 的字段清单（`timestamp` / `user_id` / `username` / `action` / `resource` / `ip_address` / `result`）与**建表定义一致**，与**代码模型不一致**。
- §10.4.2 的「Filebeat → Graylog」链路**未实现**：当前审计落 `audit_logs` 表（DB），查询端点 `GET /api/audit-logs`（`backend/internal/api/routes.go:212`）。

### 10.5 AI 层的访问边界（v3 · 对应需求文档 R1）

> 内容与 `09-AI辅助.md` §9.3 一致。总原则：**AI 层（Keep / HolmesGPT）对 ITmanager 一律只读**；写操作（确认告警、建单）必须由人在 ITmanager 里点（`docs/v3-架构优化需求.md` §2）。

#### 10.5.1 只读 token + 最小 scope

| 要求 | 落地方式 | 现状 |
|------|----------|------|
| 为 HolmesGPT **单独签发** API Key | `api_keys` 表（`schema.sql:102`） | ✅ 端点 `POST /api/auth/api-keys`（`backend/internal/api/handlers/api_key_handler.go`） |
| **不得复用 admin 账号** | 专属服务账号 + 专属 Key | ✅ 机制具备（按 R1 执行时落实） |
| 最小 scope | `api_keys.permissions` | ✅ **已实现**（2026-09-09）：`AuthMiddleware` 的 API Key 分支按 `key.Permissions` 做方法级 scope 校验（`apiKeyAllows`：GET/HEAD/OPTIONS 需 `read`/`write`/`admin`，其它方法需 `write`/`admin`；**空权限 fail-safe 视为只读**）；关联用户被禁用时 Key 立即失效。缺陷 **D-7** 已闭环 |
| 来源限制 / 限速 | `api_keys.ip_whitelist`（已强制校验，`auth.go:147`）+ `rate_limit` | ✅ |
| 有效期 | `api_keys.expires_at` | ✅ |

#### 10.5.2 写操作端点不暴露给 toolset

toolset 只允许 `GET`（`09-AI辅助.md` §9.4 `methods: ["GET"]`）。以下**已实现**的写端点**不得**进入 toolset（`backend/internal/api/routes.go:245-266`）：

| 端点 | 动作 |
|------|------|
| `PUT /api/alerts/:id/ack` · `POST /api/alerts/bulk-ack` | 确认告警 |
| `PUT /api/alerts/:id/resolve` · `POST /api/alerts/bulk-resolve` | 解决告警 |
| `POST /api/alerts/:id/mark-fp` | 标记误报 |
| `POST /api/tickets` · `PUT /api/tickets/:id` | 建单 / 改单 |
| `POST /api/alerts/bulk-delete` | 删除告警 |

> **路径前缀注**：`09-AI辅助.md` §9.2 / §9.4 初版写作 `/api/v1/...`，实际路由**无 `/v1` 前缀**（`backend/internal/api/routes.go:175` 注册 `/api`；OpenAPI `backend/internal/api/openapi.yaml:25` `servers.url` = `.../api`）。**已修正**：`09-AI辅助.md` §9.2/§9.4 统一改为 `/api/...`。

#### 10.5.3 数据出境边界

| 类别 | 可出境（发往 LLM / AI 层） | 不出境 |
|------|---------------------------|--------|
| 资产 | 资产 ID / 名称 / 类型 / 机柜位置 | 资产上的凭据字段 |
| 告警 | 告警名称 / 级别 / 时间 / 状态 | — |
| 业务上下文 | 诊断时间线聚合、Runbook 推荐、KPI 聚合值 | 工单正文中的个人信息（字段级清单待确认） |
| 认证与密钥 | — | `users.password_hash`、`users.mfa_secret`、`api_keys.key_hash`、SNMP community 及各类 token |

> 与 `09-AI辅助.md` §9.3 第 3 条一致：用**云端 LLM** 时告警与资产信息会外发 → 优先**本地 Ollama 或内网 DeepSeek**。

#### 10.5.4 调用留审计

- 所有 toolset 调用经 `AuthMiddleware` + `AuditLog` 中间件（`backend/internal/api/routes.go:197-199`）落 `audit_logs`，与 `09-AI辅助.md` §9.3 第 4 条一致。
- ⚠️ 审计写入本身的列名不一致问题见 §10.4.3（待修复）。

#### 10.5.5 与 `09-AI辅助.md` §9.3 的对照

| §9.3 约束 | 本章落点 |
|-----------|----------|
| 只读 token + 最小 scope，不复用 admin 账号 | §10.5.1 |
| 写操作不暴露 | §10.5.2 |
| 数据外流边界 | §10.5.3 |
| 审计 | §10.5.4 + §10.4.3 |

---

*本文档为拆分后的"安全合规设计"模块，原章节：十五*
*优化：整合 LDAP 认证、审计日志 (Graylog)*
