# 网络运维监控平台 - 运维工单模块

> **v3 修订块** · 状态 **draft** · 日期 2026-09-09 · 依据 `docs/v3-架构优化需求.md` · 分支 `docs/v3-optimize`
>
> 本模块 v3 定位：**ITmanager 自己的 `tickets` 表是工单唯一真值（SoT）**；GLPI **未部署**，降级为「可选只读参考」。
> 决策记录：`docs/adr/0004-工单SoT决策.md`。
> **明确不做**：ITIL 审批流 / SLA 引擎 / 满意度调查 / 工单工作流引擎 / 工单子步骤（见 §5.3）。

## 五、运维工单详细设计

### 5.1 工单 SoT 决策（v3）

#### 5.1.1 前提（2026-09-09 实测，非推测）

| 事实 | 证据 |
|------|------|
| **GLPI 没有部署** | `docker-compose.yml:116` 有 `glpi:` 服务定义，但环境里没有实例；`GLPI_URL` 为空即视为未启用（`integration_handler.go:107`） |
| **GLPI 客户端只能读** | `backend/internal/integration/glpi.go` 仅 `InitSession` / `GetTickets` / `KillSession`，**没有创建/更新工单的方法** |
| **同步方向是 GLPI → 本地** | `SyncFromGLPI`（`integration_handler.go:71-73`）+ `GLPITicket.ConvertToTicket()`（`glpi.go:156`） |
| **ITmanager 有完整工单 CRUD** | `TicketService` = List/Get/Create/Update（`ticket_service.go:27-32`），路由 `routes.go:261-266` |

结论：**「ITmanager 只读 GLPI」这条路在本环境里等于把工单功能挂在一个不存在的系统上。**

#### 5.1.2 决策：A 方案 —— ITmanager 工单为 SoT

| 方案 | 内容 | 优点 | 代价 |
|------|------|------|------|
| **A. ITmanager 为 SoT ✅ 已采纳** | `tickets` 表是唯一真值，工单在 ITmanager 内创建/流转；GLPI 若将来上线，只做只读参考或单向导入 | 无跨系统写冲突；不依赖未部署的系统；工单与告警/资产/诊断时间线同库，可直接关联 | 工单能力自己维护（范围已限定在 §5.3） |
| B. GLPI 为 SoT | ITmanager 只读 GLPI，写操作回 GLPI API | 复用成熟 ITIL 流程 | GLPI 未部署 = 功能挂在空中；需双向同步 + 账号打通；与 §2 三层定位（ITmanager = 人的操作台）冲突 |

#### 5.1.3 边界

- **ITmanager 负责**：工单的创建、查询、状态流转（open → in_progress → resolved → closed）、指派、与告警/资产关联。
- **GLPI 负责（若上线）**：无。仅作为**只读参考数据源**，拉取历史工单供查看/导入。
- **AI 层（HolmesGPT）**：只读。**不暴露建单/改单端点给 toolset**；建单必须由人在 ITmanager 里点（与 `09-AI辅助.md` §9.3、`10-安全合规.md` 一致）。

#### 5.1.4 触发复评的条件（出现才重新评估）

- 组织统一部署了 GLPI，且**要求所有工单入口归口到 GLPI**；
- 出现跨部门/多组织的 ITIL 刚性流程需求（审批链、多级派单）；
- 工单量增长到需要独立 SLA 引擎与超时升级。
- 届时**优先评估「GLPI 为 SoT + ITmanager 只读」**，而不是双写——双写必然产生「双真相」（与 R4 告警划界同一原则）。

### 5.2 现有实现（以代码为准）

| 项 | 现状 | 位置 |
|----|------|------|
| 表 | `tickets` | `migrations/000001_init.up.sql:670` ⚠️ 与 GORM 模型不一致，见 §5.5 D-1 |
| 模型 | `models.Ticket` | `backend/internal/models/ticket.go:11` |
| 服务 | List / Get / Create / Update | `backend/internal/service/ticket_service.go:27-32` |
| 端点 | `GET /tickets`、`GET /tickets/:id`、`POST /tickets`、`PUT /tickets/:id` | `backend/internal/api/routes.go:261-266` |
| 分页 | cursor（`created_at,id` 二元组）+ offset 兼容 | `ticket_service.go:66-80` |
| 唯一性 | `ticket_number` 唯一索引 | `models/ticket.go:13` |

枚举（以代码注释为准）：

| 字段 | 取值 |
|------|------|
| `ticket_type` | `incident` / `request` / `problem` / `change` |
| `priority` | `low` / `medium` / `high` / `critical` |
| `status` | `open` / `in_progress` / `resolved` / `closed`（默认 `open`） |
| `source` | `manual` / `email` / `api` / `glpi`（默认 `manual`） |
| `tags` | jsonb 数组字符串（默认 `[]`） |

**当前没有实现的能力**：工单评论 / 处理记录（无 `ticket_comments` 表、无端点）。

### 5.3 明确不做（NOT doing）

迁移里 `tickets` 之外还有 **8 张 `ticket_*` 表 + 1 张 `step_progress_history`**，全部 **0 行 Go 代码**（2026-09-09 实测）。v3 不做：

| 不做 | 对应表 | 理由 |
|------|--------|------|
| ITIL 审批流 | `ticket_approvals` | 零实现；单团队运维不需要多级审批 |
| SLA 引擎 | `ticket_sla` | 零实现；SLA 目标由值班/升级流程承载 |
| 满意度调查 | `tickets.satisfaction` | 零使用方 |
| 工单工作流引擎 | `ticket_workflows` / `ticket_templates` | 零实现；状态机已够用 |
| 工单子步骤 / 进度百分比 | `ticket_steps` / `ticket_progress` / `step_progress_history` | 零实现；用状态 + 指派即可 |
| 工单分段 / 交接 | `ticket_segments` / `ticket_handoffs` | 零实现；值班交接用 `oncall_*`（已实现） |
| 附件 | `tickets.attachments` | 不在 v3 范围 |
| GLPI 写回（建单/改单） | — | GLPI 未部署；且客户端本就没有写方法 |

> 这些表**只归档不删除**（定义保留在迁移里，可追溯），但文档不再把它们描述为「已设计待实现的能力」。

### 5.4 与告警的联动

| 项 | v3 状态 |
|----|---------|
| `alerts.ticket_id` 字段 | 存在（`models/alert.go:43`），注释写「GLPI 工单」，但**全仓库无写入方** → 缺陷 D-3 |
| 告警 → 工单路径 | 人在 ITmanager 里从告警详情**手动建单**；`ticket_id` 应指向 `tickets.id`（本系统），不再指向 GLPI |
| 自动建单 | **不做**。AI 层只读；写操作必须由人点（与 §2 统一约束一致） |

### 5.5 已知缺陷（修复前不得声称工单「可用」）

#### D-1 三套 schema 不一致（**阻断级**）

| 来源 | 列数 | 关键差异 |
|------|------|----------|
| 迁移 `000001_init.up.sql:670-695` | 24 | `ticket_no NOT NULL UNIQUE`、`creator_id UUID NOT NULL REFERENCES users(id)`、`alert_id`、`progress`、`satisfaction`、`planned_*`/`actual_*`、`result` |
| GORM 模型 `models/ticket.go:11-36` | 24 | 用 `ticket_number`（非 `ticket_no`）、`requester_*`、`tags`、`external_id`、`source`、`resolution`、`closed_at`；**无 `creator_id`** |
| 测试 schema `models/hooks_test.go:62-87` | 24 | **与模型完全一致** → CI 发现不了漂移 |

交集只有 10 列（`id`/`ticket_type`/`priority`/`title`/`description`/`asset_id`/`status`/`assignee_id`/`created_at`/`updated_at`），各 14 列独有。

**后果**：生产建库走迁移（`cmd/server/main.go:34` 注入 `MigrationsFS` → `database.go:72-73` 执行 `migrate.Up`；`AutoMigrate` 仅开发兜底），而 GORM 的 INSERT 会引用迁移里不存在的列（`ticket_number`/`requester_id`/`tags` 等）→ **`POST /tickets` 必然失败**；即使列存在，`ticket_no` / `creator_id` 的 `NOT NULL` 也会拒绝写入。

**修复方向**（另立任务，不在本轮文档改动内）：先决定以哪边为准 → 新增迁移对齐 → 再改模型与测试 schema。若 `tickets` 表为空（很可能，因为写入路径必然失败）可重建；非空则必须写数据迁移。**禁止对生产库直接 AutoMigrate。**

#### D-2 工单号碰撞

`generateTicketNumber`（`models/ticket.go:54-58`）用**全表** `Count()%26` 生成后缀：

```go
db.Model(&Ticket{}).Count(&count)
return "TICKET-" + time.Now().Format("20060102") + "-" + string(rune('A'+count%26))
```

`Count()` 是全表计数且与日期无关，而后缀只有 26 个取值 → 同一天第 27 张工单与第 1 张**同号**；跨天也会因为总数模 26 撞上历史号码。`ticket_number` 有唯一索引，撞号时第 27 张插入失败（`ErrAlreadyExists`）。

**修复方向**：改成「当日序号」（`WHERE created_at::date = today` 计数）或序列/随机后缀，并对唯一冲突做重试。**必须在 D-1 之后做**，否则改的是模型而不是生产库实际表。

#### D-3 `alerts.ticket_id` 悬空

字段注释指向 GLPI 工单，但**无任何写入方**（实测仅定义处出现）。按 A 方案，它应指向 `tickets.id`。修复随 §5.4 的「一键建单」一起做。

### 5.6 GLPI 只读参考（可选，默认关闭）

| 能力 | v3 状态 |
|------|---------|
| 配置项（URL + App-Token + User-Token） | 保留；`GLPI_URL` 为空 = 未启用 |
| 连通性测试 `POST /integrations/glpi/test` | 保留 |
| 只读拉取 `SyncFromGLPI`（GLPI → 本地 upsert） | 保留；GLPI 未部署时不会被触发 |
| 创建 / 更新 GLPI 工单 | **不存在，且不计划做** |
| `docker-compose.yml` 的 `glpi:` 服务 | 定义保留，**默认不启用**（见 `08-部署运维.md` v3 清单） |

### 5.7 后续可选方向（不在 v3 范围）

- 工单评论 / 处理记录（需新表 + 端点）
- 与 `runbooks` 的关联（Runbook 已实现，可做「工单 → 推荐 Runbook」）
- KPI 报表的 MTTR 口径依赖 `resolved_at` / `closed_at`——**模型有、迁移没有**，须等 D-1 修复后才准。

---

*本文档为拆分后的"运维工单管理"模块，原章节：十二(上)*
*优化：集成 GLPI 工单系统，支持与 Zabbix 告警联动*
*v3（2026-09-09）：改为 ITmanager 工单为 SoT，GLPI 降级为可选只读参考；补记 D-1/D-2/D-3 三个缺陷。*
