# 网络运维监控平台 - 需求概述

## 一、需求概述

### 1.1 项目背景

随着企业IT基础设施规模不断扩大，网络设备、服务器、应用系统的数量和复杂度持续增长。传统的被动式运维模式已无法满足需求，亟需构建一套具备自动发现、实时监控、资产管理、告警通知、可视化展示能力的综合性网络运维监控平台。

### 1.2 核心目标

| 目标 | 描述 | 实现方案 |
|------|------|----------|
| **全栈发现** | 自动发现网络中的设备、服务器、应用，跨网段整合 | Nmap + NetBox |
| **资产管理** | 完整记录物理信息、配置信息、软件信息，支持多网卡/IP | NetBox (DCIM/IPAM) |
| **实时监控** | 通过SNMP/Agent采集状态数据，实时掌握运行状况 | Zabbix |
| **智能告警** | 多渠道通知（钉钉/企业微信/邮件/语音），支持阈值触发 | Zabbix + 钉钉Webhook |
| **可视化** | 机柜图、拓扑图、历史趋势图，现代GUI界面 | NetBox Rack View + 自建前端 |

> **v3 定位修订（2026-09-09）**：1.1 节「综合性网络运维监控平台」与上表「全栈发现」等**大而全平台**表述已在 v3 收敛——ITmanager 不再自建全部能力，只做「人的操作台」；资产真值归 NetBox，AI 分析归 Keep + HolmesGPT。详见 §1.5。

### 1.3 系统规模

| 资源类型 | 数量 | 说明 |
|----------|------|------|
| 机房 | 3 | 多地域分布 |
| 机柜 | 几十个 | 每个机房约10-20个 |
| 虚拟机 | 1000+ | VMware虚拟化 |
| 物理服务器 | 几十台 | Dell/HP/Lenovo |
| 网络设备 | 若干 | 交换机/路由器/防火墙/WAF/IPS |
| 专线 | 几十条 | P2P + MPLS VPN，当前零纳管（v3 R2 补台账） |

### 1.4 设计原则

| 原则 | 说明 |
|------|------|
| **稳定优先** | 选用成熟开源组件，减少自研风险 |
| **轻量架构** | 小规模系统，避免过度设计 |
| **开箱即用** | 优先采用已有解决方案，减少开发量 |
| **便于维护** | 组件标准化，文档完整 |
| **易于扩展** | 模块化设计，支持功能扩展 |

### 1.5 v3 定位修订（2026-09-09）

> 依据 [docs/v3-架构优化需求.md](docs/v3-架构优化需求.md)。基线 `git describe` = **v2.1.2-12-gdad2b6f**（README/TODO/CHANGELOG 版本号已于 2026-09-09 对齐，见 R5）。

**一句话**：ITmanager 不再做「全栈自建平台」，收敛为**人的操作台**——资产真值归 NetBox、AI 分析归 HolmesGPT、告警枢纽归 Keep。

```
┌──────────────────────────────────────────────────────────┐
│  ① 人的操作台 —— ITmanager（本项目）                      │
│  拓扑图 · 诊断时间线 · 告警确认 · Runbook · 复盘 PDF      │
│  值班排班 · KPI 报表 · 全局搜索                           │
└────────────────────────▲─────────────────────────────────┘
                         │ 只读消费
        ┌────────────────┴────────────────┐
        │                                 │
┌───────┴──────────────┐      ┌───────────┴──────────────┐
│ ② 资产真值 —— NetBox │      │ ③ AI 分析层               │
│ DCIM/IPAM/Circuits   │      │ Keep（告警枢纽）          │
│ 专线/设备/机柜/VM    │      │ HolmesGPT（根因侦探）     │
└──────────────────────┘      └──────────────────────────┘
```

| 层 | 拥有什么 | 不做什么 |
|---|---|---|
| ① ITmanager | 人的**工作流**：看、确认、排班、复盘 | 不存资产真值、不做 AI 推理 |
| ② NetBox | 资产**真值**：设备/机柜/IP/专线/VM | 不做工作流、不做告警 |
| ③ Keep + HolmesGPT | 告警**收敛**与**根因** | 不做资产台账、不自动执行写操作 |

**统一约束**：AI 层对本项目一律只读。写操作（确认告警、建单）必须由人在 ITmanager 里点。

**v3 明确不做**：自建 LLM 问答/知识库 · 自建专线 CRUD · gRPC 内部通信 · 迁移 Nautobot · v3 部署 Keep。

**已实现主线（v3 继续维护）**：资产 / 告警 / 告警抑制 / 工单 / 拓扑 / 值班升级 / Runbook / 指标快照。

---

*本文档为拆分后的"需求概述"模块，原章节：一*
