# 网络运维监控平台 - 告警通知模块

> **v3 修订块** · 状态 **draft** · 日期 2026-09-09 · 依据 `docs/v3-架构优化需求.md` · 分支 `docs/v3-optimize`
>
> 本模块 v3 定位：**跨源告警去重/关联/富化/路由归 Keep；ITmanager 保留人工抑制 + 值班升级**（见 §4.2，对应需求文档 **R4**）。
> v3 **不部署 Keep**，仅划定职责边界；先做 R1（ITmanager 直连 HolmesGPT）。

## 四、告警通知详细设计

### 4.1 告警方案：基于 Zabbix

#### 4.1.1 Zabbix 告警优势

| 特性 | 说明 |
|------|------|
| **阈值触发** | 支持多种运算符和函数 |
| **时间触发** | 支持时间周期配置 |
| **依赖触发** | 支持告警依赖关系 |
| **升级机制** | 支持告警升级策略 |
| **抑制机制** | 支持告警抑制/聚合 |
| **通知渠道** | 支持多种通知方式 |

#### 4.1.2 告警级别定义

| 级别 | 名称 | 颜色 | 描述 |
|------|------|------|------|
| ** Disaster** | 灾难 | 🔴 红色 | 系统不可用 |
| **High** | 高 | 🟠 橙色 | 严重故障 |
| **Average** | 中 | 🟡 黄色 | 服务降级 |
| **Warning** | 警告 | 🔵 蓝色 | 潜在问题 |
| **Information** | 信息 | 🟢 绿色 | 通知信息 |

### 4.2 与 Keep 的职责划界（v3 · 对应需求文档 R4）

> 状态：**边界已划定；v3 不部署 Keep**。
> 理由：告警量尚未证实需要独立告警枢纽；本轮先做 R1（ITmanager 直连 HolmesGPT），Keep 的引入留待「跨源去重/关联」需求被证实后再评估。
> 本节只做职责切分，不改变现有实现。

#### 4.2.1 分工表

| 能力 | 归属 | 说明 |
|------|------|------|
| 跨源告警去重 / 关联 / 富化 / 路由 | **Keep** | 汇聚多监控源（Zabbix / Splunk / Wazuh 等）做收敛 — **Splunk 直连 Keep/HolmesGPT,ITmanager 集成栈不接 Splunk**（见 09-AI辅助.md §9.1） |
| 告警状态写入（problem / ack / resolved） | **Keep（唯一写入方）** | 单一写入方，避免「双真相」 |
| 人工抑制规则（维护窗口等） | **ITmanager（保留）** | ✅ 已实现：`alert_suppressions` |
| 值班排班 + 升级 | **ITmanager（保留）** | ✅ 已实现：`oncall_*`（值班表 + 升级策略） |
| 告警只读展示 / 诊断时间线 | **ITmanager** | 只读消费，不落本地告警状态 |

#### 4.2.2 状态一致性约束

- **Keep 是告警状态的唯一写入方**；ITmanager 只读展示。
- ITmanager 的 **ack（确认）动作改为调用 Keep API**，**不在本地写告警状态**（避免「ITmanager 已 ack、Keep 仍 open」）。
- **Keep 未部署期间（v3 现状）**：告警状态仍以 Zabbix 为准，ITmanager 不新增告警状态写入路径。

#### 4.2.3 数据流示意

```
┌──────────────────────────────────────────────────────────┐
│  Zabbix / Splunk / Wazuh ...  （多监控源）                 │
└────────────────────────┬─────────────────────────────────┘
                         │
                ┌────────▼─────────┐  ← v3 不部署，仅划界
                │  Keep 告警枢纽    │    去重/关联/富化/路由
                │  状态唯一写入方   │
                └────────┬─────────┘
                         │ 只读
        ┌────────────────▼──────────────────┐
        │  ITmanager 操作台                  │
        │  只读展示 · 人工抑制 · 值班升级     │
        │  ack → 调用 Keep API（不落本地）    │
        └───────────────────────────────────┘
```

**对应验收**：需求文档 §4 **R4**；A-1（不再描述与 Keep 重复的自建能力）。

### 4.3 通知渠道实现

#### 4.3.1 支持的渠道

| 渠道 | 类型 | Zabbix支持 | 适用场景 |
|------|------|------------|----------|
| **钉钉** | Webhook | ✅ 原生 | 实时告警 |
| **企业微信** | Webhook | ✅ 脚本 | 实时告警 |
| **邮件** | SMTP | ✅ 原生 | 日常通知 |
| **短信** | API | ✅ 脚本 | 紧急告警 |
| **电话** | API | ✅ 脚本 | P0级别告警 |
| **Webhook** | HTTP | ✅ 原生 | 第三方集成 |

#### 4.3.2 钉钉通知配置

```yaml
# Zabbix 钉钉告警脚本
# /usr/lib/zabbix/externalscripts/dingding.sh

#!/bin/bash

WEBHOOK_URL="https://oapi.dingtalk.com/robot/send?access_token=xxxxx"
SECRET="SECxxxxxxxx"

# 生成签名
timestamp=$(date +%s000)
sign=$(echo -n "${timestamp}\n${SECRET}" | openssl dgst -sha256 -hmac "${SECRET}" -binary | base64)
sign=$(python3 -c "import urllib.parse; print(urllib.parse.quote('$sign'))")

# 发送消息
curl -X POST "$WEBHOOK_URL&timestamp=$timestamp&sign=$sign" \
  -H 'Content-Type: application/json' \
  -d '{
    "msgtype": "markdown",
    "markdown": {
      "title": "Zabbix告警",
      "text": "### '${ALERT_SUBJECT}'\n\n"'"${ALERT_MESSAGE}"'""
    }
  }'
```

#### 4.3.3 邮件通知配置

```yaml
# Zabbix 邮件告警配置
AlertScriptsPath=/usr/lib/zabbix/externalscripts

# 配置媒体类型
# 类型: Email
# SMTP服务器: smtp.company.com
# SMTP端口: 587
# SSL/TLS: STARTTLS
# 认证: 用户名/密码
# 发件人: zabbix@company.com
```

### 4.4 告警处理流程

#### 4.4.1 告警生命周期

```
┌─────────────────────────────────────────────────────────────┐
│                    告警生命周期                               │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────┐    ┌─────┐    ┌─────┐    ┌─────┐    ┌─────┐     │
│  │触发 │───►│通知 │───►│确认 │───►│处理 │───►│关闭 │     │
│  └─────┘    └─────┘    └─────┘    └─────┘    └─────┘     │
│    │                                              ▲         │
│    │                                              │         │
│    └──────────────── 复训 <───────────────────────┘         │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

#### 4.4.2 告警状态

| 状态 | 说明 | 可转换到 |
|------|------|----------|
| **Problem** | 已触发，等待处理 | Acknowledged, Resolved |
| **Acknowledged** | 已确认 | Resolved |
| **Resolved** | 已解决 | Problem (复训) |

#### 4.4.3 告警升级策略

```
级别: Disaster (P1)
┌─────────────────────────────────────────────────────┐
│  0min   ──► 通知值班工程师 (钉钉)                  │
│  15min  ──► 通知运维主管 (钉钉+短信)             │
│  30min  ──► 通知运维经理 (钉钉+短信+电话)        │
│  60min  ──► 通知总监 (电话) + 升级P0            │
└─────────────────────────────────────────────────────┘

级别: High (P2)
┌─────────────────────────────────────────────────────┐
│  0min   ──► 通知值班工程师 (钉钉)                  │
│  30min  ──► 通知运维主管 (钉钉)                   │
│  60min  ──► 通知运维经理 (短信)                   │
└─────────────────────────────────────────────────────┘
```

### 4.5 告警规则配置示例

#### 4.5.1 服务器CPU告警

```yaml
# Zabbix Trigger 配置
名称: Server CPU usage is too high
表达式: avg(/Linux server CPU/system.cpu.util,5m) > 80
严重性: Warning
描述: CPU使用率连续5分钟超过80%
```

#### 4.5.2 网络设备端口down

```yaml
名称: Network interface {ITEM.NAME} is down
表达式: nodata(/SNMP Generic/net.if.status[{#IFNAME}],10m) = 1 and last(/SNMP Generic/net.if.status[{#IFNAME}]) = 2
严重性: High
描述: 接口 {#IFNAME} 处于down状态
```

#### 4.5.3 虚拟机资源告警

```yaml
名称: VM {$VM.NAME} CPU usage high
表达式: avg(/VMware VM/vm.cpu.usage[{$VM.UUID}],5m) > {$VM.CPU.THRESHOLD}
严重性: Average
描述: 虚拟机CPU使用率超过阈值
```

### 4.6 告警与工单联动

#### 4.6.1 自动创建工单

> 示例语言：**Go**（项目技术栈为 Go + Gin + GORM，见 `07-技术架构.md`）。
> 原 Python 示例已作废——它不是项目代码，且 Zabbix 侧的 webhook 媒体类型并非 Python；历史版本可在 git 记录中追溯。

```go
// ITmanager 侧：告警 → 自动创建 GLPI 工单
// 复用项目已有 httpx 客户端（backend/internal/httpx/httpx.go），
// GLPI 客户端形态见 backend/internal/integration/glpi.go。

type alertEvent struct {
	Subject  string `json:"subject"`
	Message  string `json:"message"`
	Severity string `json:"severity"`
	Trigger  string `json:"trigger"`
}

func createTicketFromAlert(ctx context.Context, cli *httpx.Client, a alertEvent) ([]byte, error) {
	payload, err := json.Marshal(map[string]any{
		"input": map[string]any{
			"name":            a.Subject,
			"content":         a.Message,
			"urgency":         mapUrgency(a.Severity),
			"category":        detectCategory(a.Trigger),
			"requesttypes_id": 1, // 监控告警
		},
	})
	if err != nil {
		return nil, err
	}

	// path 约定与 backend/internal/integration/glpi.go 一致：base URL 已含 /apirest.php
	body, status, err := cli.DoWithHeaders(ctx, http.MethodPost, "/api/Ticket",
		bytes.NewReader(payload), map[string]string{
			"App-Token":     glpiAppToken,
			"Session-Token": glpiSession,
			"Content-Type":  "application/json",
		})
	if err != nil {
		return nil, err
	}
	if status >= http.StatusMultipleChoices {
		return nil, fmt.Errorf("glpi create ticket: status %d", status)
	}
	return body, nil
}
```

---

*本文档为拆分后的"告警通知"模块，原章节：七*
*优化：基于 Zabbix 告警系统，整合钉钉/短信/邮件通知*
